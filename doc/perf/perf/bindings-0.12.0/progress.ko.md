# bindings 0.12.0 성능 개선 진행 시트

> 시작일: 2026-08-19
>
> 기준 Core: `0.12.0`
>
> 작업 브랜치: `codex/bindings-0.12.0-performance`
>
> 계획서: [bindings 0.12.0 성능 개선 계획](./bindings-library-performance-improvement-plan-core-0.12.0.ko.md)
>
> 원본 템플릿: [bindings 성능 개선 계획 Core 템플릿](../bindings-library-performance-improvement-plan-core-template.ko.md)

이 시트의 독자는 성능 개선 작업자다. 현재 통과한 gate와 다음 paired 측정 대상을 이 문서에서
확인하고, 실행 과정과 후보 검토는 같은 폴더의 `log/`에 기록한다. 상세한 size·transport·pattern
결과 표와 완료 기준은 계획서가 소유한다.

## 1. 작업 범위

같은 Core `0.12.0` release runtime으로 `bindings/c/perf`를 기준으로 삼고, 각 binding의
공식 perf runner 결과를 paired 비교한다. 개선은 현재 public API와 ownership·error contract를
유지한 binding 내부 구현을 대상으로 한다.

| 순서 | Binding | Perf 경로 | Single | Multi |
|------|---------|-----------|--------|-------|
| 1 | C++ | `bindings/cpp/perf` | 미측정 | 미측정 |
| 2 | .NET | `bindings/dotnet/perf` | 미측정 | 미측정 |
| 3 | Java | `bindings/java/perf` | 미측정 | 미측정 |
| 4 | Node | `bindings/node/perf` | 미측정 | 미측정 |
| 5 | Go | `bindings/go/perf` | 미측정 | 미측정 |
| 6 | Rust | `bindings/rust/perf` | 미측정 | 미측정 |
| 7 | Python | `bindings/python/perf` | 미측정 | 미측정 |

## 2. 초기 gate

| Gate | 상태 | 근거 또는 다음 작업 |
|------|------|---------------------|
| 버전 3곳 일치 | 확인됨 | `VERSION`, `core/CMakeLists.txt`, `core/include/zlink.h`가 모두 `0.12.0`이다. |
| 실제 release runtime과 package provenance | 확인됨 | `scripts/local-package/core/fetch-release.sh --version 0.12.0 --platform linux-x64`로 받은 `core/v0.12.0` GitHub release가 checksum과 `release-provenance.txt`로 검증돼 `/home/hep7hep7/.cache/zlink/core/0.12.0/linux-x64`에 있다. `share/zlink/core-package-provenance.json`의 `runtime.sha256`(`aaff83d34ca1833d566feb39141ab5b4660d22429a551067de71b4f8967ba365`)이 `lib/libzlink.so.0.12.0`의 실제 sha256과 일치함을 재확인했다(`log/2026-08-23-cpp-single-smoke.md` §2). |
| 공식 runner inventory | 수정 후 통과 | `log/2026-08-23-inventory-gate-c-cpp.md` — 최초 판정은 미통과(C++ multi 기본 pattern 목록에 Python driver가 모르는 SPOT 3종이 남아 기본 실행이 깨짐)였고, 같은 문서의 "수정 결과" 절에 기록된 코드 수정(SPOT 관련 코드 완전 제거)으로 통과로 전환했다. |
| Multi size 정책 | 확인됨(통과) | `log/2026-08-23-inventory-gate-c-cpp.md` §5·§7.2 — C runner, C++ runner, `doc/perf/PERF_MULTI_TEST_POLICY.md`, 이 계획서 표 네 곳 모두 4 KiB 포함과 `MULTI_STREAM` 예외가 일치한다. |
| 무시되는 runner option | 수정 후 통과 | `log/2026-08-23-inventory-gate-c-cpp.md` — 최초 판정은 미통과(C++ single/multi의 `--buf`/`--sndbuf`/`--rcvbuf`가 CLI에서는 성공 처리되지만 socket에 미적용)였고, 같은 문서의 "수정 결과" §2에 기록된 대로 C++ 공개 바인딩에 `send_buffer()`/`recv_buffer()` accessor를 추가해 실제 적용되도록 배선한 뒤 통과로 전환했다. |
| memory guard와 실제 client 수 | 미확인 | C와 binding report의 client 수, STREAM client 수, cap 발생 여부를 확인한다. C/C++ multi의 guard 메커니즘 차이(C: pattern skip, C++: client 수 cap)는 `log/2026-08-23-inventory-gate-c-cpp.md` §4.2에 기록돼 있고 별도 정책 결정이 남아 있다. |
| 재현 환경 manifest | 기록됨 | `log/2026-08-23-cpp-single-smoke.md` §2 — host, kernel, CPU, core runtime prefix·sha256, session tag(`bindings-0.12.0-smoke-20260823`)를 기록했다. |

## 3. 현재 작업 위치

| 항목 | 상태 | 값 |
|------|------|-----|
| 현재 언어 | C++ | inventory gate 통과 후 C++부터 시작했다. |
| 현재 suite | single | `PAIR`/`tcp`와 `PUBSUB`/`tcp` 모두 **보류로 확정**됐다(각각 84.92%, 87.76%). `PUBSUB`은 자체 개선 pass + Sol 리뷰 pass(LTO/IPO A/B no-go) + §7.2 5-run 최종 판정까지 완료했다. `DEALER_DEALER`/`tcp`는 첫 공식 paired 측정 **19.02%가 결함 상태의 무효값**으로 판명돼 D1(caller-thread fast path) 수정 후 재측정했고, 재측정 결과도 **미달(53.59%)**로 기록됐다. 이어서 pattern 전용 **자체 개선 pass(D2~D5)** 를 수행해 64B `async()` 메시지당 1,882 ns → 1,079 ns로 줄였고, 공식 재측정 결과 **미달(63.36%)**(직전 53.59% 대비 +9.77%p)로 갱신됐다. 남은 절차는 Sol 리뷰 pass다. |
| 현재 pattern | `DEALER_DEALER` | C single `STANDARD_PATTERNS`(`PAIR,PUBSUB,DEALER_DEALER,...`)와 계획서 §9.1.1 표에서 `PAIR`/`PUBSUB` 다음 pattern. `PAIR`/`tcp`는 보류(84.92%), `PUBSUB`/`tcp`는 보류(87.76%)로 확정됐다. `DEALER_DEALER`/`tcp`는 첫 공식 측정에서 미달(19.02%, **무효** — 바인딩 내부 결함, `routed_admission_state.cpp:145 enqueue()`가 백프레셔 없는 정상 케이스에도 admission을 항상 reactor 스레드에 위임)로 기록됐다가 D1 caller-thread fast path 수정(`log/2026-08-23-cpp-dealer-dealer-ceiling.md`) 반영 후 공식 release runtime으로 재측정했다. 재측정 결과는 aggregate throughput mean **53.59%**(size별 19.84~82.92%)로 여전히 목표(95%/완화 90%)와 개별 최소 기준(85%) 모두 미달 — **미달(53.59%)**로 확정. 이후 자체 개선 pass(D2~D5, `log/2026-08-23-cpp-routed-send-improvement.md`)를 수행해 공식 재측정에서 **미달(63.36%)**(31.79 / 38.23 / 66.66 / 75.98 / 83.46 / 84.05%, latency 1.210배)로 갱신했다. byte-HWM 백프레셔 가설은 A/B와 park 계수기로 기각됐다(bookkeeping-dominated). Sol 리뷰 pass는 아직 수행하지 않았다. |
| 현재 transport | `tcp` | Single은 `tcp`부터 선택한다는 규칙에 따름. |
| paired C 기준 | **통과(공식) — `core/v0.12.0`이 수정 반영본으로 재릴리스됨** | `core/v0.12.0` release asset이 재릴리스됐다(provenance `source.revision = f99703c2190b0f6c670be49f67315d904886c742`, 현재 HEAD와 일치). `--core-version 0.12.0`으로 C reference smoke를 재실행하면 `status: complete`(`PAIR`: `log/2026-08-23-cpp-pair-tcp-official.md` §2.1, `PUBSUB`: `log/2026-08-23-cpp-pubsub-tcp-official.md` §2.1). TCP connection-ready deadlock은 재현되지 않는다. |
| binding before (`PAIR`) | **공식(release) 측정 완료, 보류 확정 — `PAIR`/`tcp` 전체 크기** | 공식 release runtime(`--core-version 0.12.0`) 기준으로 C++ single smoke와 전체 크기(`--runs 3`) paired 측정이 모두 `status: complete`로 끝났다(`log/2026-08-23-cpp-pair-tcp-official.md`). throughput aggregate mean 84.92%(목표 95%/완화 90% 미달), latency aggregate mean 1.201배(상한 2.0배 이내) — 자체 pass·Sol 리뷰 pass 완료 후 잔여 후보 없어 **보류(84.92%) 확정**(계획서 §9.1.1). |
| binding before (`PUBSUB`) | **공식(release) 측정 완료(3회) — `PUBSUB`/`tcp` 전체 크기, 보류 확정** | 첫 회(`log/2026-08-23-cpp-pubsub-tcp-official.md`): 89.40% / 1.062배. 자체 pass 후 재측정(`log/2026-08-23-cpp-pubsub-improvement-pass.md` §8): 86.15% / 1.120배. **§7.2 `최종·경계 판정` 5-run**(`log/2026-08-23-cpp-pubsub-final-judgment.md`, session tag `bindings-0.12.0-official-pubsub-final5-20260823`, default duration, runs 5, CPU pin 없음): throughput aggregate mean **87.76%**(91.37 / 94.00 / 92.25 / 76.21 / 84.40 / 88.34%), latency aggregate mean **1.095배**(통과). 세 회차 모두 `status: complete`이고 Effective Options(`lang` 제외)·auto-HWM이 일치한다. 65536B는 환경 지배 셀이지만 aggregate에 그대로 포함한다(Sol §2). 자체 pass·Sol 리뷰 pass가 모두 끝나고 잔여 후보가 없으므로 계획서 §8에 따라 **보류(87.76%) 확정**. |
| binding before (`DEALER_DEALER`) | **공식(release) 재측정 완료(D1 수정 반영) — `DEALER_DEALER`/`tcp` 전체 크기, 미달(53.59%)** | 첫 회(무효, 결함 상태): throughput aggregate mean 19.02%(`log/2026-08-23-cpp-dealer-dealer-tcp-official.md` §2~§5). 원인 진단·수정: `routed_admission_state.cpp:145 enqueue()`가 백프레셔 없는 정상 케이스에도 admission을 항상 reactor 스레드에 위임해 메시지마다 스레드 왕복 2회가 직렬화됐다(`log/2026-08-23-cpp-dealer-dealer-ceiling.md`) — D1 caller-thread fast path로 수정. **재측정(session tag `bindings-0.12.0-official-dd2-20260823`)**: 공식 release runtime(`--core-version 0.12.0`) smoke(status complete, C 2,586,714 msg/s / C++ 565,597 msg/s, 64B)와 전체 크기(`--runs 3`) paired 측정 모두 `status: complete`(30/30). throughput aggregate mean **53.59%**(19.84 / 25.80 / 41.11 / 71.76 / 80.11 / 82.92%, 목표 95%/완화 90%/개별 최소 85% 모두 미달), latency aggregate mean **1.169배**(상한 2.0배 이내, 통과). throughput 비율이 크기에 걸쳐 단조 증가해 국소적 65536B dip이 아니므로 `/usr/bin/time -v` 환경-지배 재확인은 적용하지 않았다. `DEALER_DEALER` 전용 자체 pass·Sol 리뷰 pass 미수행 → **미달(53.59%)**로 기록(계획서 §9.1.1). `log/2026-08-23-cpp-dealer-dealer-tcp-official.md` §재측정(수정 후). |
| 자체 개선 pass (`PAIR`) | 완료(공식 측정 반영, 보류 확정) | C++ hot path 후보 C1·C3·C4·C5·C6이 구현된 채로 공식 측정을 실행했다(`log/2026-08-23-cpp-c1-c3-c4-c5-implementation.md`의 로컬 core 예비 판정과 방향 일치). Sol 리뷰 pass(C2/C9/C10 no-go 확정, `log/2026-08-23-sol-review-pair-tcp.md`)까지 마쳐 `PAIR`/`tcp`는 보류로 확정됐다. C2는 미구현(no-go 확정 유지). commit 하지 않았다. |
| 자체 개선 pass (`PUBSUB`) | **완료(재측정 반영)** | `log/2026-08-23-cpp-pubsub-improvement-pass.md`. 65536B는 환경(glibc heap trim) 지배 셀로 판정(TASK A), 64B는 경로 치환 ablation으로 gap의 79.9%가 공용 send builder 계층임을 확인(TASK B). `PUBSUB` **고유** 비용은 없다 — topic 처리 약 5 ns/publish, subscription matching은 hot path 밖, recv 계층은 C API 치환보다 오히려 빠르다. P1(측정 의미 정렬)·P2(중복 제거)만 구현하고 P3~P7은 no-go(§5 하단 표). contract 12/14·sample 6/7로 before/after 동일. commit 하지 않았다. |
| Sol 리뷰 pass (`PUBSUB`) | **완료(보류 확정)** | `log/2026-08-23-sol-review-pubsub-tcp.md` + 후속 실행 `log/2026-08-23-cpp-pubsub-final-judgment.md`. (1) P3(builder 계층)·builder state stack/SBO 전환·3-call 병합·`weak_ptr::expired()` 호이스팅은 모두 **no-go 재확인**, (2) 남은 유일 후보 **LTO/IPO를 실제로 배선하고 A/B 실행 → no-go**(LTO-ON 89.16% vs LTO-OFF 89.50%, latency 1.079 vs 1.060배; 공식 측정 구성은 `ENABLE_LTO=OFF` 유지), (3) 65536B는 aggregate 포함 + `환경 지배` 주석으로 기록, (4) P2의 contract-order caveat 수정(topic validation을 `callback_state()`의 lazy allocation보다 먼저 수행하도록 복원), (5) §7.2 5-run 최종 판정 1회 실행 → **보류(87.76%)**. |
| 커밋과 push | 미착수 | 검증된 변경만 별도 승인 범위에서 처리한다. |

## 4. Paired 측정 기록

두 report가 모두 `status: complete`이고 같은 manifest·session tag·조건을 사용할 때만 결과를
계획서의 상세 표에 반영한다. 부분 결과, runtime mismatch, client 수 불일치는 판정값으로 사용하지
않고 원인을 `log/`에 기록한다.

| 날짜 | 언어 | Suite / pattern / transport | 단계 | 결과 | C report | Binding report | 로그 |
|------|------|-----------------------------|------|------|----------|----------------|------|
| 2026-08-19 | 전체 | 작업 초기화 | 계획 작성 | 측정 전 | - | - | 이 시트와 계획서 |
| 2026-08-23 | C++ | single / `PAIR` / `tcp` | smoke | **실패** — C reference가 `status: partial`(`timeout`)로 끝나 C++는 실행하지 않음 | `perf_c_single_linux_20260823_115107_bindings-0.12.0-smoke-20260823.txt` | 없음(미실행) | `log/2026-08-23-cpp-single-smoke.md` |
| 2026-08-23 | C++ | single / `PAIR` / `tcp` | 공식 smoke(재릴리스 core) | **complete/complete** — C 2,528,435.000 msg/s / 52.353 ms, C++ 2,081,149.000 msg/s / 61.587 ms (64B, 1s, runs=1) | `perf_c_single_linux_20260823_132304_bindings-0.12.0-official-20260823.txt` | `perf_cpp_single_linux_20260823_132311_bindings-0.12.0-official-20260823.txt` | `log/2026-08-23-cpp-pair-tcp-official.md` §2.1 |
| 2026-08-23 | C++ | single / `PAIR` / `tcp` | 공식 전체 크기(64~262144B, runs=3, 후보 판정 단계) | **complete/complete, 종합 미달(84.92%)** — throughput aggregate mean 84.92%(목표 95%/완화 90% 미달, size별 80.93~93.52%), latency aggregate mean 1.201배(상한 2.0배 이내) | `perf_c_single_linux_20260823_132408_bindings-0.12.0-official-20260823.txt` | `perf_cpp_single_linux_20260823_132544_bindings-0.12.0-official-20260823.txt` | `log/2026-08-23-cpp-pair-tcp-official.md` §2.2~§5 |
| 2026-08-23 | C++ | single / `PAIR` / `tcp` | **C6-후속 확인** (64B·65536B 셀만, runs=3) | **complete/complete** — 64B **80.38%**(공식 80.93%, 회귀 없음), 65536B **88.22%**(공식 70.90%). 65536B 상승은 C6 효과가 아니라 이 셀의 큰 run-to-run 분산이다(원인은 glibc heap-trim page fault storm — 진단 log §6.1.4). latency 1.187배 / 1.161배. **§9.1.1 전체 행을 대체하지 않는다** | `perf_c_single_linux_20260823_135504_c6-followup-20260823.txt` | `perf_cpp_single_linux_20260823_135549_c6-followup-20260823.txt` | `log/2026-08-23-c6-fix-and-64k-diagnosis.md` §4.3 |
| 2026-08-23 | C++ | single / `PAIR` / `tcp` | **C8 진단 ablation**(비공식, 32768·65535·65536·65537·131072B) | 65536B gap 중 **binding send/recv 호출 계층의 몫은 잡음과 구분 불가**(`cboth`−`base` = −1.4%~+14.4%, 분포 완전 중첩). 65536 경계 **계단 없음**(70.42/70.09/70.19%). 원인은 glibc heap trim page fault storm(C++ 1.3M vs C 1.6k, 메시지당 약 8회) — trim만 끄면 C++ 47.8~49.7k로 **C 상회(102.1%)**, 반대로 C에 강제하면 C도 41.2k로 저하. **새 후보 없음** | (throwaway 계측, 공식 report 아님) | (throwaway 계측) | `log/2026-08-23-c6-fix-and-64k-diagnosis.md` §5~§7 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | 공식 smoke(재릴리스 core) | **complete/complete** — C 1,429,786.000 msg/s / 22.165 ms, C++ 1,392,531.000 msg/s / 42.964 ms (64B, 1s, runs=1) | `perf_c_single_linux_20260823_141530_bindings-0.12.0-official-pubsub-20260823.txt` | `perf_cpp_single_linux_20260823_141537_bindings-0.12.0-official-pubsub-20260823.txt` | `log/2026-08-23-cpp-pubsub-tcp-official.md` §2.1 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | 공식 전체 크기(64~262144B, runs=3, 후보 판정 단계, 첫 회) | **complete/complete, 종합 미달(89.40%)** — throughput aggregate mean 89.40%(목표 95%/완화 90% 미달, size별 78.29~93.75%), latency aggregate mean 1.062배(상한 2.0배 이내). `PUBSUB` 전용 자체 pass·Sol 리뷰 pass 미수행이므로 `보류`가 아닌 `미달`로 기록 | `perf_c_single_linux_20260823_141558_bindings-0.12.0-official-pubsub-20260823.txt` | `perf_cpp_single_linux_20260823_141752_bindings-0.12.0-official-pubsub-20260823.txt` | `log/2026-08-23-cpp-pubsub-tcp-official.md` §2.2~§5 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | **65536B 환경 진단**(비공식, `/usr/bin/time -v`, 5초 3회 인터리브) | 같은 셀에서 C++ minor page fault 1.13~1.36M vs C 1,986~2,185(약 **540배**), system time만 +1.2초, voluntary ctx switch는 동일. `MALLOC_TRIM_THRESHOLD_=134217728`만 주면 C++ fault가 173k(최소 7.1k)로 붕괴하고 비율이 **94.31%**로 상승, 반대로 `MALLOC_MMAP_THRESHOLD_`로 C를 같은 체제에 넣으면 **C가 C++보다 느려진다(C++/C = 122.20%)**. → **환경(프로세스 allocator) 지배 셀로 판정, `PAIR`과 동일. 새 후보 없음** | (throwaway 계측) | (throwaway 계측) | `log/2026-08-23-cpp-pubsub-improvement-pass.md` §2~§4 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | **64B 경로 치환 ablation**(비공식, 5초, 8회/5회 median) | 64B gap 중 **publish 계층 58.9%**, **subscribe 계층 −2.3%**(binding이 C API 치환보다 빠름). publish 계층 내부 분해: **builder + pooled state 기계장치 79.9%**, `message_t::from()` 18.9%. 마이크로 계측으로 builder 계층 = **27.8 ns/publish**(mutex 3.7 + pool 4.2 + 나머지 19.9). **`PUBSUB` 고유 비용 없음** — topic 처리 약 5 ns, subscription matching은 hot path 밖 | (throwaway 계측) | (throwaway 계측) | `log/2026-08-23-cpp-pubsub-improvement-pass.md` §5~§6 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | **자체 pass 후 공식 재측정**(64~262144B, runs=3, P1·P2 반영) | **complete/complete, 종합 미달(86.15%)** — size별 91.72 / 92.17 / 89.86 / 70.18 / 84.37 / 88.60%, latency aggregate mean 1.120배. **64B는 87.94%→91.72%로 개선**. 대형 크기 3셀 하락은 allocator 체제 변동(실효 auto-HWM은 두 회차 동일함을 `diff`로 확인). 65536B 제외 5셀 aggregate 89.34%(기록용) | `perf_c_single_linux_20260823_144931_bindings-0.12.0-official-pubsub-pass2-20260823.txt` | `perf_cpp_single_linux_20260823_145128_bindings-0.12.0-official-pubsub-pass2-20260823.txt` | `log/2026-08-23-cpp-pubsub-improvement-pass.md` §8~§9 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | **LTO/IPO A/B — variant A (`ENABLE_LTO=OFF`)**(64~262144B, runs=3, C paired 재측정) | **complete/complete** — aggregate throughput mean **89.50%**(86.24 / 93.45 / 91.26 / 87.54 / 90.09 / 88.40%), latency **1.060배** | `perf_c_single_linux_20260823_151612_bindings-0.12.0-ltoab-off-20260823.txt` | `perf_cpp_single_linux_20260823_151807_bindings-0.12.0-ltoab-off-20260823.txt` | `log/2026-08-23-cpp-pubsub-final-judgment.md` §3 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | **LTO/IPO A/B — variant B (`ENABLE_LTO=ON`, 라이브러리+perf 실행파일 IPO)**(64~262144B, runs=3, C paired 재측정) | **complete/complete, 개선 없음 → LTO no-go** — aggregate throughput mean **89.16%**(88.97 / 93.46 / 93.22 / 85.19 / 83.13 / 90.99%), latency **1.079배**. 64B·1024B·262144B는 오르지만 65536B·131072B가 내려가 aggregate가 −0.34%p, latency도 악화. contract 12/14·sample 6/7로 LTO 빌드 miscompile 없음(출력이 LTO-OFF와 `diff` 완전 동일) | `perf_c_single_linux_20260823_152115_bindings-0.12.0-ltoab-on-20260823.txt` | `perf_cpp_single_linux_20260823_152311_bindings-0.12.0-ltoab-on-20260823.txt` | `log/2026-08-23-cpp-pubsub-final-judgment.md` §3~§4 |
| 2026-08-23 | C++ | single / `PUBSUB` / `tcp` | **§7.2 최종·경계 판정**(64~262144B, **runs=5**, default duration, CPU pin 없음, `ENABLE_LTO=OFF` 최종 빌드) | **complete/complete, 보류(87.76%) 확정** — size별 91.37 / 94.00 / 92.25 / **76.21(환경 지배)** / 84.40 / 88.34%, latency aggregate mean **1.095배**(통과). Effective Options(`lang` 제외) `diff` 완전 일치, auto-HWM은 pass2와 바이트 단위 동일. 65536B 제외 5셀 90.07%는 진단용이며 판정 입력이 아니다 | `perf_c_single_linux_20260823_152615_bindings-0.12.0-official-pubsub-final5-20260823.txt` | `perf_cpp_single_linux_20260823_152923_bindings-0.12.0-official-pubsub-final5-20260823.txt` | `log/2026-08-23-cpp-pubsub-final-judgment.md` §5~§6 |
| 2026-08-23 | C++ | single / `DEALER_DEALER` / `tcp` | 공식 smoke(재릴리스 core) | **complete/complete** — C 2,558,848.000 msg/s / 48.432 ms, C++ 9,829.000 msg/s / 0.115 ms (64B, 1s, runs=1) | `perf_c_single_linux_20260823_153908_bindings-0.12.0-official-dd-20260823.txt` | `perf_cpp_single_linux_20260823_153914_bindings-0.12.0-official-dd-20260823.txt` | `log/2026-08-23-cpp-dealer-dealer-tcp-official.md` §2.1 |
| 2026-08-23 | C++ | single / `DEALER_DEALER` / `tcp` | 공식 전체 크기(64~262144B, runs=3, 첫 회) | **complete/complete, 종합 미달(19.02%)** — throughput aggregate mean 19.02%(size별 0.37~55.77%, 목표 95%/완화 90%/개별 최소 85% 모두 크게 미달), latency aggregate mean 0.031배(상한 2.0배 이내, C보다 훨씬 낮음 — 처리량 상한의 부작용). throughput이 크기와 무관하게 약 9,000~10,000 msg/s로 평탄한 pattern 전역 현상(국소적 65536B dip 아님). `DEALER_DEALER` 전용 자체 pass·Sol 리뷰 pass 미수행이므로 `보류`가 아닌 `미달`로 기록. **⚠ 2026-08-23 후속: 이 행의 19.02%는 바인딩 내부 결함이 있는 상태의 값이며 무효다 — 재측정 대상.** 평탄한 약 10K msg/s 상한의 근본 원인은 `routed_admission_state.cpp:145 enqueue()`가 DEALER/ROUTER 비동기 send의 물리 admission을 백프레셔가 없는 정상 케이스에도 항상 reactor 스레드에 위임해 메시지마다 스레드 왕복 2회(admission reactor + continuation dispatcher)가 직렬화된 것이었다(`log/2026-08-23-cpp-dealer-dealer-ceiling.md`). caller-thread fast path 수정 후 local core 탐색 측정에서 64B 9,732 → 544,992 msg/s(×56), 65536B 10,056 → 31,329 msg/s(×3.1)로 C와 같은 크기 의존 곡선을 회복했다. 수정 반영본으로 공식 release runtime 전체 크기 paired 재측정을 수행해야 한다 | `perf_c_single_linux_20260823_153937_bindings-0.12.0-official-dd-20260823.txt` | `perf_cpp_single_linux_20260823_154113_bindings-0.12.0-official-dd-20260823.txt` | `log/2026-08-23-cpp-dealer-dealer-tcp-official.md` §2.2~§5, `log/2026-08-23-cpp-dealer-dealer-ceiling.md` |
| 2026-08-23 | C++ | single / `DEALER_DEALER` / `tcp` | **재측정 공식 smoke(D1 수정 반영, 재릴리스 core)** | **complete/complete** — C 2,586,714.000 msg/s / 51.139 ms, C++ 565,597.000 msg/s / 0.107 ms (64B, 1s, runs=1). 64B throughput 비율 약 21.87% — 결함 상태(0.38%) 대비 대폭 회복 | `perf_c_single_linux_20260823_160132_bindings-0.12.0-official-dd2-20260823.txt` | `perf_cpp_single_linux_20260823_160137_bindings-0.12.0-official-dd2-20260823.txt` | `log/2026-08-23-cpp-dealer-dealer-tcp-official.md` §재측정(수정 후) |
| 2026-08-23 | C++ | single / `DEALER_DEALER` / `tcp` | **재측정 공식 전체 크기(D1 수정 반영, 64~262144B, runs=3)** | **complete/complete, 종합 미달(53.59%)** — throughput aggregate mean 53.59%(size별 19.84 / 25.80 / 41.11 / 71.76 / 80.11 / 82.92%, 목표 95%/완화 90%/개별 최소 85% 모두 미달), latency aggregate mean 1.169배(상한 2.0배 이내, 통과). throughput 비율이 64B→262144B로 단조 증가 — 국소적 65536B dip 아님(`/usr/bin/time -v` 재확인 미적용). `DEALER_DEALER` 전용 자체 pass·Sol 리뷰 pass 미수행이므로 `보류`가 아닌 `미달`로 기록. 첫 회 19.02%는 무효로 남기고 이 행이 유효한 공식값이다 | `perf_c_single_linux_20260823_160218_bindings-0.12.0-official-dd2-20260823.txt` | `perf_cpp_single_linux_20260823_160354_bindings-0.12.0-official-dd2-20260823.txt` | `log/2026-08-23-cpp-dealer-dealer-tcp-official.md` §재측정(수정 후) |
| 2026-08-23 | C++ | single / `DEALER_DEALER` / `tcp` | **자체 개선 pass 후 공식 smoke(D2~D5 반영, 재릴리스 core)** | **complete/complete** — C 2,541,295.000 msg/s / 51.670 ms, C++ 883,777.000 msg/s / 59.436 ms (64B, 1s, runs=1), 비율 약 34.8% | `perf_c_single_linux_20260823_163625_bindings-0.12.0-official-dd3-20260823.txt` | `perf_cpp_single_linux_20260823_164330_bindings-0.12.0-official-dd3-20260823.txt` | `log/2026-08-23-cpp-routed-send-improvement.md` §8 |
| 2026-08-23 | C++ | single / `DEALER_DEALER` / `tcp` | **자체 개선 pass 후 공식 전체 크기(D2~D5 반영, 64~262144B, runs=3)** | **complete/complete, 종합 미달(63.36%)** — throughput aggregate mean 63.36%(size별 31.79 / 38.23 / 66.66 / 75.98 / 83.46 / 84.05%, 직전 dd2 53.59% 대비 +9.77%p, 목표 95%/완화 90%/개별 최소 85% 모두 미달), latency aggregate mean 1.210배(상한 2.0배 이내, 통과). 비율이 크기에 따라 단조 증가 — 65536B 국소 dip 아님. Sol 리뷰 pass 미수행이므로 `보류`가 아닌 `미달`로 기록 | `perf_c_single_linux_20260823_164526_bindings-0.12.0-official-dd3-20260823.txt` | `perf_cpp_single_linux_20260823_164704_bindings-0.12.0-official-dd3-20260823.txt` | `log/2026-08-23-cpp-routed-send-improvement.md` §8~§9 |

## 5. 개선 후보 기록

후보는 public API, ownership, error semantics와 측정 의미를 유지하는지 확인한 뒤 기록한다.
성능 향상뿐 아니라 allocation·copy·dispatch·callback·poller 비용의 책임 위치와 POSDDD
위험 신호를 함께 적는다.

| 후보 | 대상 | 변경 요약 | Before | After | 판정 | 로그 |
|------|------|-----------|--------|-------|------|------|
| C1 raw send `weak_ptr` 왕복 제거 | C++ binding `pair_socket_t::send()` / `submit_raw_send_state()` | **구현함(설계 조정).** `raw.callbacks`를 비소유 `socket_callback_state_t*` + 수명 토큰 `weak_ptr callbacks_anchor`로 분리하고, 토큰을 thread_local pooled state에 소켓 주소 키로 캐시(`bind_callback_state()`). 동기 terminal은 `live_callback_state()`(atomic load 2회)만 쓰고 async/admission만 `share_callback_state()`로 shared_ptr 승격. hot loop에서 send당 lock 접두 RMW 4회 소멸. weak 토큰 자체는 유지 — 제거하면 `test_routed_async_builder_does_not_outlive_socket_anchor`의 파괴-후-terminal 계약이 dangling이 된다 | C++ 2,060,151.400 msg/s / latency 64.775 ms vs C 2,556,849.200 msg/s / 54.485 ms (PAIR/tcp/64B, 5초 `--runs 3` 배치 3회의 median, local core, 비율 80.57%) | C++ 2,140,546.800 msg/s / latency 56.903 ms (동일 조건·동일 세션, C 대비 **83.72%**, throughput +3.90%p 상대 개선) | **채택 후보 (예비 판정)** | `log/2026-08-23-cpp-c1-c3-c4-c5-implementation.md` |
| C2 단일 part send mutex 범위 축소 | C++ binding `submit_raw_send_state()` | 소켓 단위 `outbound_record_attempt_mutex`를 다중 native 호출 원자성이 실제로 필요한 경로(admission record·async 재개)로 한정. 순서 보장 책임을 admission state로 이동. gap 기여 추정 ~3% | 동일(위 C1 Before) | - | **no-go 확정 (2026-08-23 Sol 2차 리뷰)** — 현재 gate는 단순 mutex가 아니라 send attempt와 `close()`의 lifecycle 선형화 지점이다. 제거하면 binding-side `invalid_state/EINVAL`이 아니라 native `ETERM`/`ESHUTDOWN`/invalid-handle 오류 또는 accepted send가 관측될 수 있어 **오류 종류와 관측 시점이 모두 보존되지 않는다.** Core도 close·admitted API에 stricter lifecycle gate를 요구한다 | `log/2026-08-23-sol-review-pair-tcp.md` |
| C3 동기 send terminal의 pooled state 기계장치 축소 | C++ binding `operation_state_t` / `reset_for_reuse()` / send builder 체인 | **부분 구현.** `reset_for_reuse()`의 `RAW_SEND_HOT_PATH` 특수 분기 삭제, `routing_target_t::reset()`의 256 B memset 제거, callback 캐시를 per-op reset에서 분리. **스택 축소 state는 no-go** — 공개 builder 타입들이 `operation_builder_base_t`의 `unique_ptr<operation_state_t>` 멤버를 private 상속으로 보유하므로 계획서 §5의 공개 type 변경 금지에 저촉하고, 체인 단계마다 대형 state를 move 하게 되어 손해다 | C++ 2,060,151.400 msg/s / latency 64.775 ms vs C 2,556,849.200 msg/s / 54.485 ms (PAIR/tcp/64B, 5초 `--runs 3` 배치 3회의 median, local core, 비율 80.57%) | C++ 2,140,546.800 msg/s / latency 56.903 ms (동일 조건·동일 세션, C 대비 **83.72%**, throughput +3.90%p 상대 개선) (C1·C4·C5와 합산 측정) | **구조개선(성능 중립~소폭 개선) (예비 판정)** | `log/2026-08-23-cpp-c1-c3-c4-c5-implementation.md` |
| C4 recv guard/재질의 부기 축소 | C++ binding `detail::recv_single_part_message()` / `recv_part_out_guard_t` | **구현함.** valid·empty 출력 message에 대해 guard 객체·`prepare()`의 `init()` 재확인·`_has_saved`/`_committed` 분기를 모두 건너뛰는 inline fast path 추가, 성공 경로에서 `refresh_payload_presence()`의 재검증 분기를 `message_access_t::has_payload` 직접 대입으로 대체, source rid 대입 중복 제거. **`zlink_msg_size` 호출 1회는 유지** — `zlink_recv_part()`가 크기를 out param으로 돌려주지 않아 완전 제거는 Core API 변경(범위 밖)을 요구한다 | C++ 2,060,151.400 msg/s / latency 64.775 ms vs C 2,556,849.200 msg/s / 54.485 ms (PAIR/tcp/64B, 5초 `--runs 3` 배치 3회의 median, local core, 비율 80.57%) | C++ 2,140,546.800 msg/s / latency 56.903 ms (동일 조건·동일 세션, C 대비 **83.72%**, throughput +3.90%p 상대 개선) (C1·C3·C5와 합산 측정) | **채택 후보 (예비 판정)** | `log/2026-08-23-cpp-c1-c3-c4-c5-implementation.md` |
| C5 C++ perf 하네스 설정을 C reference와 정렬 | `bindings/cpp/perf/single/src/perf_pair.cpp` | **구현함.** C `perf_pair`에 없는 `tcp_no_delay(true)` 2건과 `recalculate_single_auto_hwm(ctx)` 제거로 측정 의미 정렬(계획서 §5). 다른 pattern 파일은 건드리지 않아 hunk가 분리돼 있다. 부수 회귀 확인: `PUBSUB` +7.80%, `DEALER_DEALER` −0.58%(잡음), `ROUTER_ROUTER`는 변경 전에도 `status: partial`(기존 문제) | C++ 2,060,151.400 msg/s / latency 64.775 ms vs C 2,556,849.200 msg/s / 54.485 ms (PAIR/tcp/64B, 5초 `--runs 3` 배치 3회의 median, local core, 비율 80.57%) | C1+C3+C4만: 2,169,972.000 msg/s / C5까지: C++ 2,140,546.800 msg/s / latency 56.903 ms (동일 조건·동일 세션, C 대비 **83.72%**, throughput +3.90%p 상대 개선) — C5 단독 효과는 잡음 수준 | **구조개선(측정 의미 정렬, 성능 중립) (예비 판정)** | `log/2026-08-23-cpp-c1-c3-c4-c5-implementation.md` |
| C6 `_has_payload` native move invariant 복구 | C++ binding `bindings/cpp/src/Runtime/Native/native_message_parts.hpp` | **구현함(성능 후보 아님, correctness 선행 수정).** native payload를 `message_t`로 되돌리는 모든 helper(`restore_part_from_native()`, `restore_parts_from_native()` 2종, `assign_parts_from_native()` 2종, `take_parts_from_native()`, `move_parts_to_native()` rollback)를 `adopt_native_part()` 하나로 중앙화하고, move 성공 시 `refresh_payload_presence()`로 payload 유무 metadata를 복원한다. `move_to_native()`/`init()`이 `_has_payload=false`로 지운 사실을 아무도 되돌리지 않아, 실제 payload를 가진 message가 empty로 광고되고 C4 recv fast path가 이를 empty로 오인해 수신 실패 시 payload를 파괴할 수 있었다. 추가 `zlink_msg_size()`는 비-hot 복구/materialization 경로에만 발생하며 ownership·public API는 불변 | 수정 전 바이너리로 A/B 프로그램 실행 시 **18건 실패** — 복구된 message가 `has_payload=false`가 되고, 이를 `recv()`에 다시 넣으면 실패 경로에서 payload가 실제로 소멸(`valid()=false`) | 수정 후 **전부 통과.** contract 12/14(기존 실패 2건 동일), samples 6/7(기존 실패 1건 동일), ASan/UBSan contract 12/14(sanitizer 보고 0건). 정상 경로 회귀 확인: PAIR/tcp 64B 80.38%, 65536B 88.22% | **채택 예정 (correctness)** | `log/2026-08-23-c6-fix-and-64k-diagnosis.md` |
| C9 large-message pool 하한을 64KiB로 확장 | C++ binding `src/Runtime/Messaging/message.cpp` | pooled external buffer + release callback을 정확히 65536B 및 128/256KiB 경계에서 활성화 | - | - | **no-go 확정 (2026-08-23 Sol 2차 리뷰)** — 계약상 가능할 수 있으나 **성능상 no-go**다. global mutex, exact-size linear search, 다른 thread에서 실행될 수 있는 release callback, 8MiB 혼합 allocation cap, static pool 파괴와 늦은 message 파괴의 lifetime 위험이 있고 payload copy 자체는 제거하지 못한다. 계획서도 pool 재도입을 후보에서 제외하고 있으며, 과거 64KiB floor A/B도 65536/131072B latency 개선을 보이지 않아 폐기됐다. 이번 ablation에서 65536B gap의 대부분이 binding 밖(core/transport/환경 분산)으로 확인돼 근거가 더 약해졌다 | `log/2026-08-23-sol-review-pair-tcp.md`, `log/2026-08-23-c6-fix-and-64k-diagnosis.md` |
| C10 receive 성공 후 `zlink_msg_size()` 제거 | C++ binding `detail::recv_single_part_message()` | C4 성공 경로의 마지막 native size query 제거 | - | - | **no-go 확정 (2026-08-23 Sol 2차 리뷰)** — empty frame과 payload frame을 구분할 수 없어 `_has_payload=true` 고정은 **계약 위반**이다. 정확한 대안은 Core receive API가 수신 크기를 반환하도록 바꾸는 것뿐이며 이번 범위 밖이다. A/B는 실제 후보로 수행하지 않고 size query의 CPU 비용 microbenchmark로만 제한한다 | `log/2026-08-23-sol-review-pair-tcp.md` |

### 5.1 `PUBSUB`/`tcp` 자체 pass 후보 (2026-08-23)

| 후보 | 대상 | 변경 요약 | Before | After | 판정 | 로그 |
|------|------|-----------|--------|-------|------|------|
| P1 `recalculate_single_auto_hwm()` 제거 | `bindings/cpp/perf/single/src/perf_pubsub.cpp` | **구현함.** C reference `bindings/c/perf/single/src/perf_pubsub.cpp`에 없는 context auto-HWM 재계산 호출을 제거해 측정 의미를 정렬했다(C5의 `PUBSUB` 후속 — C5가 `perf_pair.cpp`만 고치고 다른 pattern 파일은 남겨 뒀다). 이 호출은 C++ 쪽에 **유리하게** 작용하고 있었으므로(ablation `norecalc` −1.3%) 계획서 §5에 따라 제거가 옳다. 제거 후 report의 `## Auto-HWM Detail`이 첫 회와 바이트 단위로 동일함을 `diff`로 확인 | 64B `base` 1,377,775 msg/s(5초 8회 median, ablation) | `norecalc` 1,359,630 msg/s(−1.3%, 잡음 수준) | **구조개선(측정 의미 정렬, 성능 중립)** | `log/2026-08-23-cpp-pubsub-improvement-pass.md` §7.1 |
| P2 publish 조립·config 오류 변환 중복 제거 | C++ binding `bindings/cpp/src/Runtime/Sockets/pubsub.cpp` | **구현함.** (1) `pub_socket_t::publish()`와 `xpub_socket_t::publish()`의 문자 단위로 동일한 7줄 본문을 파일 지역 `make_publish_state()` 하나가 소유하게 했다(PUB/XPUB은 socket type만 다르고 publish operation 조립 방법은 같다는 사실이 한 번만 적힌다). (2) `sub_socket_t`/`xsub_socket_t`의 subscription 접근자 6개가 반복하던 `throw config_error_t(config_result_from_errno(zlink_errno()), zlink_errno())`를 `throw_last_config_error()` 하나로 모았다. 공개 헤더 미변경, signature·type·ownership·error 동작 불변, hot path 호출 시퀀스 불변(같은 TU static 함수로 인라인) | contract 12/14, sample 6/7 | **contract 12/14(실패 2건 출력이 `diff` 완전 동일), sample 6/7, 빌드 경고 0** | **구조개선(중복 제거, 성능 중립)** | `log/2026-08-23-cpp-pubsub-improvement-pass.md` §7.2~§7.3 |
| P3 send builder 계층 자체 축소 | `send_operation_t` / `operation_builder_base_t` | 64B gap의 79.9%, publish 호출당 약 20 ns가 여기에 있다 | - | - | **no-go** — 비용의 원천이 공개 fluent builder 계약(`publish()`→`message()`→`submit()`)과 공개 builder type이다. 계획서 §5가 공개 interface·type 변경을 금지하고, C3에서 스택 state 전환이 이미 no-go로 확정됐다. **Sol 리뷰 1순위 쟁점으로 넘긴다** | 같은 로그 §6.2~§6.3, §7 |
| P4 topic 처리 최적화 | `pub_socket_t::publish` / `raw_command_t::topic` | topic 문자열 대입·`clear()`·`empty()`/`c_str()`와 `validate_no_embedded_null` | - | - | **no-go** — 합계 약 5 ns/publish(전체 처리량의 0.1% 미만)이고 `validate_no_embedded_null`은 error semantics 계약이라 제거 불가 | 같은 로그 §6.4 |
| P5 recv/subscribe 계층 축소 | `read_subscription_message` / `lazy_message_parts_t` | 수신 경로의 guard·materialize 비용 | - | - | **no-go** — ablation에서 binding recv가 C API 직접 치환보다 **2.3% 빠르다.** 회복할 gap이 없다 | 같은 로그 §6.1 |
| P6 65536B 전용 최적화 | — | - | - | - | **no-go** — 환경(프로세스 allocator) 지배 셀로 판정. `PAIR` C8·C9와 동일 결론 | 같은 로그 §2~§4 |
| P7 `MALLOC_*` 튜닝 도입 | 라이브러리 또는 perf 하네스 | - | - | - | **no-go(확정)** — 프로세스 전역 allocator 정책은 application의 책임(POSDDD 책임 경계 위반)이고, C++ 하네스만 설정하면 측정 의미가 갈라진다. `PAIR` pass에서 이미 판정 | 같은 로그 §4 |

### 5.2 `PUBSUB`/`tcp` Sol 리뷰 pass 후보 (2026-08-23)

| 후보 | 대상 | 변경 요약 | Before | After | 판정 | 로그 |
|------|------|-----------|--------|-------|------|------|
| S1 P2 contract-order 복원 | C++ binding `bindings/cpp/src/Runtime/Sockets/pubsub.cpp` | **구현함(correctness, 성능 중립).** P2의 `make_publish_state()`가 인자로 `callback_state()`를 먼저 평가할 수 있어 topic validation보다 lazy callback-state 할당이 앞설 수 있었다. helper를 callback state **호출 가능 객체**를 받는 형태로 바꿔 `validate_no_embedded_null(topic)` → `acquire_state()` → `native_handle()` → `bind_callback_state(callback_state())` → topic 대입이라는 P2 이전의 관측 가능한 단계 순서를 그대로 복원했다. 중복 제거 구조(PUB/XPUB 단일 소유자)는 유지 | contract 12/14, sample 6/7 | **contract 12/14(실패 2건 동일: `request_reply`, `socket`), sample 6/7(`dealer_router_recv_sample` 기존 실패), 빌드 경고 0** | **채택(계약 순서 보존)** | `log/2026-08-23-cpp-pubsub-final-judgment.md` §2 |
| S2 binding library + 최종 consumer LTO/IPO | `bindings/cpp/CMakeLists.txt`, `bindings/cpp/perf/CMakeLists.txt` | **배선만 구현(기본 `OFF`).** 기존 `ENABLE_LTO`는 C++ binding에서 어떤 IPO 속성에도 연결돼 있지 않아 runner의 `-DENABLE_LTO=OFF`가 무의미했다. `check_ipo_supported()` 확인 후 `zlink_cpp` 라이브러리와 perf 실행파일 양쪽에 `INTERPROCEDURAL_OPTIMIZATION`을 설정하도록 배선했다. 기본값 `OFF`에서는 어떤 target에도 속성을 설정하지 않아 기존 산출물과 동일하다 | LTO-OFF aggregate **89.50%** / latency 1.060배 (paired, runs=3) | LTO-ON aggregate **89.16%** / latency 1.079배 (paired, runs=3, 같은 조건) | **no-go(측정 결과)** — aggregate 개선 없음(−0.34%p)에 65536B·131072B 회귀와 latency 악화가 동반된다. Sol 기준("회귀 없이 aggregate를 개선할 때만 채택")에 미달하므로 **공식 측정 구성은 `ENABLE_LTO=OFF` 유지**, perf runner도 변경하지 않는다. 배선 자체는 옵션이 실제로 동작하도록 남긴다 | `log/2026-08-23-cpp-pubsub-final-judgment.md` §3~§4 |
| S3 builder state stack/SBO 전환 · 3-call 병합 · `weak_ptr::expired()` 호이스팅 | `operation_builder_base_t` / `send_operation_t` / `operation_state.hpp` | - | - | - | **no-go 확정(Sol 리뷰)** — 각각 공개 type layout/lifetime 계약, builder 보관·이동·소멸과 terminal error 시점 계약, 서로 다른 두 lifetime 경계(pooled state의 stale 재검증 vs builder가 socket보다 오래 사는 경우) 때문이다 | `log/2026-08-23-sol-review-pubsub-tcp.md` §1 |

### 5.3 `DEALER_DEALER`/`tcp` 결함 수정 (2026-08-23)

| 후보 | 대상 | 변경 요약 | Before | After | 판정 | 로그 |
|------|------|-----------|--------|-------|------|------|
| D1 routed admission caller-thread fast path | C++ binding `bindings/cpp/src/Runtime/Messaging/routed_admission_state.cpp:145` `routed_admission_state_t::enqueue()` | **구현함(기능 결함 수정).** DEALER/ROUTER send의 유일한 terminal은 `async()`인데(`operation_contracts.hpp:244`), `enqueue()`가 백프레셔가 전혀 없는 정상 케이스에도 물리 admission을 **항상** reactor 스레드에 위임했다. 결과적으로 메시지마다 admission reactor hop + continuation dispatcher hop 2회가 직렬화돼, 하네스가 `co_await` 하는 순간 크기와 무관한 약 10K msg/s(메시지당 약 100 µs) 상한이 생겼다. 새 operation이 해당 target의 **유일한** pending work일 때(`first_for_target`)에만 스케줄 상태를 세팅하고 `_mutex`를 놓은 뒤 호출 스레드에서 `pump()`를 직접 실행한다. target별 FIFO 불변(앞선 operation이 없을 때만 탐), 비블로킹(`ZLINK_SEND_FLAGS_DONTWAIT`, 백프레셔면 reactor와 동일하게 `waiting`으로 park), 공개 시그니처·타입·오류 의미 불변. 즉시 수락 시 결과가 이미 terminal이라 `await_ready()`가 `true`가 되어 coroutine이 suspend 하지 않는다 | `DEALER_DEALER`/tcp 64B **9,732.0** / 65536B **10,056.4** msg/s, `DEALER_ROUTER`/tcp 64B **9,748.0** msg/s (local core, duration 5, runs 1) | `DEALER_DEALER`/tcp 64B **544,992.0**(×56.0, C 대비 20.29%) / 65536B **31,328.8**(×3.1, C 대비 71.18%), `DEALER_ROUTER`/tcp 64B **575,476.2**(×59.0). 크기 무관 평탄 현상 소멸, C와 동일한 크기 의존 곡선 회복. `PAIR`/tcp 64B 2,130,965~2,206,781 msg/s(기존 범위, 회귀 없음). contract **12/14**·sample **6/7**로 before와 실패 항목·실패 지점 완전 동일 | **채택 예정 (기능 결함 수정)** | `log/2026-08-23-cpp-dealer-dealer-ceiling.md` |

### 5.4 `DEALER_DEALER`/`tcp` 자체 개선 pass — routed async send 메시지당 비용 (2026-08-23)

일회용 rdtsc 계측으로 64B `async()` 1,882 ns/msg를 분해한 결과 Core submit은 502 ns(27%)뿐이고
admission 부기(할당·lock 구간·`std::function`·ticket)가 약 840 ns/msg였다. 아래 D2~D5는 서로
맞물려 있어 한 묶음으로 넣었다(모두 `zlink::detail` 내부, 공개 헤더·perf 하네스 무변경).

| 후보 | 대상 | 변경 요약 | Before | After | 판정 | 로그 |
|------|------|-----------|--------|-------|------|------|
| D2 pending record 생성 전 호출 스레드 admission | `routed_admission_state.cpp` `enqueue()` / `admit_on_caller_thread()` / `park_locked()` | target에 앞선 작업이 없으면 예약(`inline_attempts`)만 잡고 `_mutex`를 놓은 뒤 호출 스레드에서 attempt를 실행한다. Core가 수락하면 `pending_operation_t`·큐 슬롯·deadline 엔트리·ticket을 **하나도** 만들지 않는다. 백프레셔일 때만 record를 만들어 target 큐 **맨 앞**에 park. FIFO(예약이 후속 enqueue를 뒤로 줄 세움), 비블로킹, wake 유실 방지(`wake_version` 재검사 + 예약 중에도 waker가 버전을 올림), terminal 이벤트 경합(`terminal_epoch`), `shutdown()` 정지(`_active_pumps`) 모두 유지. D1의 인라인 `pump()`는 이 경로로 대체돼 제거 | 64B 546,414.8 / 1024B 485,623.8 / 65536B 33,028.4 msg/s (local core, duration 5, runs 3 median) | 64B **898,193.0**(+64.4%) / 1024B **791,163.0**(+62.9%) / 65536B 33,411.6(+1.2%). `DEALER_ROUTER` 64B 551,135.8 → **922,150.2**(+67.3%). `PAIR` 64B 2,092,819 → 2,095,283(회귀 없음) | **채택 (성능 + POSDDD)** | `log/2026-08-23-cpp-routed-send-improvement.md` |
| D3 target별 레코드 통합 | `routed_admission_state.cpp` `routed_target_state_t` | 같은 key로 병렬 관리되던 `_pending_by_target`/`_wake_versions`/`_ready_set`(+`_ready_targets` key 복사)를 레코드 1개로 통합하고, 작업이 비어도 레코드를 재사용한다(서로 다른 transport pair가 32개를 넘을 때만 idle 정리). 메시지마다 생겼다 사라지던 map 노드·`std::deque` 노드 churn 제거 | (D2와 합산 측정) | (D2와 합산 측정) | **채택 (성능 + POSDDD: 병렬 컨테이너 → 단일 책임 레코드)** | 동일 |
| D4 콜백 3종 → `routed_admission_request_t` | `routed_admission_state.hpp`, `send_operations.cpp`, `request_reply.cpp` | `enqueue_routed_admission()`이 attempt/accepted/terminal `std::function` 3개 대신 request 인터페이스 shared_ptr 1개를 받는다. 호출자는 이미 send 1건당 힙 레코드를 갖고 있으므로 그 레코드가 곧 request가 된다 — 메시지당 `std::function` 힙 할당 2건과 `pending_operation_t`의 3개 함수 슬롯 제거 | (D2와 합산 측정) | (D2와 합산 측정) | **채택 (성능 + POSDDD: 소유 레코드가 스스로 요청을 표현)** | 동일 |
| D5 대기할 때만 timeout 조회 + terminal이면 ticket 생략 | `send_operations.cpp`, `routed_admission_state.hpp` | `sndtimeo`는 백프레셔로 기다릴 때만 의미가 있으므로 `routed_admission_request_t::deadline()`으로 옮겨 지연 조회(기준 시각은 operation 시작 시각 유지). 인라인 terminal에 도달한 send는 취소할 record가 없으므로 빈 ticket을 반환하고 `set_cancel()`(힙 할당) 생략 | (D2와 합산 측정) | (D2와 합산 측정) | **채택 (성능 + POSDDD: 쓰지 않는 정책을 조회하지 않음)** | 동일 |
| D6 단일 파트 직접 submit | `send_operations.cpp` `managed_send_attempt_t::attempt()` | 파트가 1개면 borrowed 어댑터(`zlink_msg_init`+`copy`+`close`) 대신 동기 terminal처럼 caller native handle을 그대로 넘기고 성공 시 `mark_sent()` | 64B 955,058 msg/s | 64B 988,188 msg/s (+3.5%) — 그러나 `test_cpp_contract_exact_target_retry` 실패(`delivered_to_A=0 rerouted_to_B=0`): `zlink_dealer_send_transport_pair_part`는 백프레셔 시 caller 메시지를 재시도 가능 상태로 남기지 않는다 | **no-go (contract 위반, 되돌림)** | 동일 §4 |

byte-HWM 백프레셔 가설 검증(사용자 지시): HWM 64배 완화 A/B에서 C는 64B 2,679,175 → 3,467,441(+29.4%)인데
C++는 개선 전(555,071 → 550,754) · 개선 후(903,600 → 895,364) 모두 ±2% 무반응이고, admission 계수기에서
64B 4,782,515건 중 park **0건**, 1024B 3,982,163건 중 park 19건(전부 `<1ms` edge wake), deadline 만료 0건이었다.
check-then-park 경로는 같은 `_mutex` 아래 `wake_version` 관찰-비교로 닫혀 있다(예약 중에도 waker가 버전을 올린다).
→ **bookkeeping-dominated, lost wake 없음**. 256B의 25 ms latency는 wake 지연이 아니라 느린 생산자가 채워 둔
파이프의 큐잉 지연이다.


## 6. 다음 작업

현재 다음 순서로 진행한다.

1. `core/v0.12.0` release runtime과 package provenance를 준비한다. Core source는 다시 build하지 않는다.
2. C와 각 binding runner의 inventory gate를 통과시킨다.
3. C++의 첫 `suite + pattern + transport`를 선택하고 64 B smoke를 C와 binding에서 순서대로 실행한다.
4. smoke가 complete이면 같은 조건의 모든 message size를 paired 측정하고 결과를 계획서와 이 시트에 기록한다.
5. 목표 미달이면 hot path 후보, 자체 pass, Sol 리뷰 pass 순서로 진행한다.

## 7. 갱신 규칙

- 실행 명령, profiler 결과, 후보 비교와 no-go 사유는 `log/`에 날짜별 파일로 남긴다.
- 계획서의 상세 표에는 complete paired report의 조건, 비율, latency, Effective Options, auto-HWM,
  client 수와 최종 상태만 기록한다.
- `미측정`, `미달`, `통과(비율%)`, `보류(비율%)`, `해당 없음`은 계획서의 상태 규칙을 따른다.
- 변동값이나 partial report만으로 상태를 `통과` 또는 `보류`로 바꾸지 않는다.
- 작업 브랜치 생성·전환, commit과 push는 별도 요청 또는 승인 없이 수행하지 않는다.

# session spec ↔ node 구현 대조

검토 기준: f1a2f416f6aa98274af32b9be61e5665dc3c43e7
검토 범위: framework/languages/node/packages/framework/src/{runtime/streams, runtime/host, runtime/spots, runtime/actors, runtime/execution, runtime/foundation, runtime/protocol, runtime/backend, runtime/locations, contracts/Configuration, contracts/Streams, contracts/Common}, framework/languages/node/packages/stream-connector/src/Runtime, framework/languages/node/test/contract, framework/languages/node/test/m6b, framework/languages/node/test/e2e

## R 규칙 대조

| R# | 판정 | 근거 (파일:줄) | 비고 |
|---|---|---|---|
| R1 | 일치 | `packages/framework/src/runtime/streams/stream-session-runtime.ts:1014` (`this.options.socket.recv(ZLINK_RECV_DONT_WAIT)`); `packages/framework/src/runtime/backend/node/node-socket-backend-adapter.ts` — Core packet/raw callback 등록 경로 없음 | recv-mode 전용, 정적 검사로 Core callback 미사용 확인 |
| R2 | 일치 | `.../streams/stream-session-runtime.ts:1170-1176` (`await this.options.applicationJobQueue.acquire(signal)` — packet enqueue 전 대기, receive loop 블록); `.../runtime/host/application-job-queue.ts:126-137` (permit state machine) | admission 실패 시 `drainReceiveState`가 멈춤; recv 재시도 없음; packet 폐기·재전달 없음 |
| R3 | 일치 | `.../streams/*.ts` 전체에 `HandlerFilter`/filter 참조 없음; filter 기구는 `.../runtime/channels/{handler-filter-scope,channel-dispatchers,channel-dispatch-pipeline}.ts`에만 존재 | STREAM dispatch(`dispatchPacket`, `.../streams/stream-session-runtime.ts:486-580`)는 filter 기구 호출 안 함 |
| R4 | 일치 | `.../streams/session-context.ts:150-151` (`get routingId() { return this.stream.routingId; }`); `.../streams/stream-session-runtime.ts:521,529,538,556,571`에서 `this.context.routingId` 사용 | recv routingId가 context를 거쳐 dispatch까지 전달 |
| R5 | 일치 | `.../streams/stream-frame-factory.ts:90` (`name: kind===Response\|\|Error ? '' : packetName`); `.../streams/session-requests.ts:79-94`(`requestSeq`만으로 complete/fail); client: `packages/stream-connector/src/Runtime/ZlinkStreamConnector.ts:198,283-292` (`decodePayload<TPayload>` — caller 지정 타입) | packet name 없음, sequence 단독 매칭, typed decode client 지정 |
| R6 | 일치 | `.../streams/stream-session-runtime.ts:432-467` (`close`/`dispose`/`drainClose`가 `this.disposed`/`this.disconnected`로 가드); `.../streams/managed-stream.ts:55,140,163,183,268,350` (`transportClosed` 플래그) | 이중 완료·중복 시작 방지 |
| R7 | 일치 | `packages/framework/src/contracts/Configuration/Registration.ts:42` (`createCodecRegistry(options.codecs)` — registration 1회 호출) | server root별 registry 1개 |
| R8 | 일치 | `.../streams/stream-session-runtime.ts:561-577`(handler 예외 → `dispatchErrors?.report`+`onError`+`replyDispatchError`, session.onError 아님); `:763-768`(session-scoped transport 오류만 `session.onError` 호출); `:1267-1319`(`onMonitorEvent` — 해석 불가 disconnect는 monitoring 전용 경로) | 4단 오류 경계 확인 |
| R9 | 일치 | `.../streams/protocol.ts:68-75` (`ZLinkStreamCloseReasonCode`: ClientClose/IdleTimeout/HeartbeatTimeout/ServerDrain/ProtocolError/TransportError — stream-connector §6.3 닫힌 집합과 동일); `.../streams/stream-session-runtime.ts` `cleanup()`에서 `zlink.stream.connections.closed` 계기 발행 | enum 값 일치, 계기 발행 확인 |
| R10 | 일치 | `packages/framework/src/contracts/Configuration/RegistrationBuilders.ts:208-219`(`addStreamNode`), `:713-719`(`registerSession`) — 명시적 builder만; `contracts/Handlers/*.ts`에 `@Stream...` decorator 없음 | 명시 등록만 존재 |
| R11 | 일치 | 기본값 `InternalDefaults.ts:4`(`64*1024`); 음수 검증 `RegistrationValidators.ts:186-192,546-549`; 0→-1 `.../streams/index.ts:213`; prefix 제외 `.../streams/stream-frame-reassembler.ts:20`(`STREAM_FRAME_PREFIX_BYTES=6`),`:196-205`; EMSGSIZE `.../streams/stream-frame-reassembler.ts:96-102`(`ZLinkStreamMessageSizeError.code='EMSGSIZE'`), `handleMalformedFrame`(`.../streams/stream-session-runtime.ts:1230-1238`)에서 응답 없이 연결 종료 | 모든 하위 조항 일치 |
| R12 | 일치 | `RegistrationBuilders.ts:713-718`(`registerSession`이 `streamNode.session!==undefined`이면 예외) | |
| R13 | 일치(위임) | cert+key 필수 `RegistrationValidators.ts:557-559`; 기본 `requireClientCertificate=false` `RegistrationBuilders.ts:704`; `.../streams/index.ts:214-221`에서 Core socket으로 전달 | Node TS는 설정 검증·전달만; 실제 TLS handshake·client-cert 거부는 Core(네이티브) 담당 — TS 소스로 독립 재검증 불가 |
| R14 | 불일치 | 7/8 조건 검증됨(이름 공백/중복, endpoint 없음, node당 2+ session `RegistrationBuilders.ts:713-718`, cert/key 공백, TLS 없이 client cert 요구는 구조적으로 불가능 `RegistrationBuilders.ts:704-711`). **"같은 session type을 서로 다른 node에 중복 등록"** 검사는 어디에도 없음 — `registerSession`은 현재 node의 슬롯만 확인 | G6의 모호성과 직결: R14를 "같은 type을 다른 node에 등록"으로 읽으면 미검증(불일치), "한 node에 2개"로 읽으면 R12와 동일 규칙(일치). 이 판정은 전자 해석을 취함 — 판정 자체는 하지 않고 사실만 보고 |
| R15 | 일치 | `contracts/Streams/IZLinkSession.ts:6-19`(callback 표면에 Spot mutation 메서드 없음); `IZLinkSessionActor.ts:4-17`(bind/bindOrGet/relay/notifyDisconnected만 제출) | |
| R16 | 일치 | command 상수 `.../runtime/foundation/service-wire-constants.generated.ts:22,29,31,41`(actorSend:24, boundSessionSend:36, boundSessionBind:38, boundSessionReplaced:51); `contracts/Streams/IZLinkSession.ts:28`(공개 표면엔 routingId만, 내부 generation·fence·codec 없음) | |
| R17 | 일치 | `.../runtime/foundation/service-stateful-runtime.ts:1779-1786`(unbind가 tombstone+`retiredGeneration` 제출); `:3450-3465`(`enqueueSessionBindingTombstone`가 `bindingGeneration===retiredGeneration` 등 전부 일치할 때만 적용, 아니면 no-op) | late tombstone은 generation 불일치로 거부 |
| R18 | 일치 | `contracts/Configuration/RegistrationBuilders.ts:696-702`(`enableActorDispatch()` — MeshName 인자 없음); `RegistrationValidators.ts:564-576`(Location Store 없음/objectRole 있는 spotNode 없음 → `ZLinkConfigurationException`); 테스트 `test/contract/startup-validation.test.js:302-336` | 내부 `bind(ZLinkActor\|ActorRef)`도 `resolveActorRef`로 환원 후 동일 경로 — hidden local-only 경로 없음 |
| R19 | 일치 | Actor→1 route: `runtime/streams/actor-session-binding-registry.ts:135,167-223`(actorId 키 Map, 기존 있으면 `replace`로 이전 context `unbindLocal`); session→N: `runtime/streams/session-local-actors.ts:6-14`, `contracts/Streams/IZLinkSessionActor.ts:5`(`bound[]`) | E2E `e2e/ToActorMessaging/Server/Session/main.ts:70` |
| R20 | 일치 | `runtime/streams/bound-actor-relay-sender.ts:145,163,180`(`routes.requireRoute` 메모리 조회); relay 파일들에 Store/`actorLookup` 참조 0건 | E2E `e2e/SpotActorTransfer/Server/Session/main.ts:74-121` |
| R21 | 일치(표현 차이) | command 24 header: `runtime/foundation/service-stateful-wire-codec.ts:1156-1181`(actor fence=ObjectGeneration·AuthorityOwnerGeneration·OwnerLeaseGeneration + tail sessionRid·bindingGeneration·sequence); 원본 correlation은 STREAM payload header로 보존 `runtime/streams/bound-actor-relay-sender.ts:184-190` | "binding token"은 wire에서 `bindingGeneration`으로 실현, 로컬 문자열 token은 wire 미탑재(표현 차이일 뿐). **스펙 미정 관찰**: session sequence는 per-session이 아니라 node runtime 단일 카운터(`service-stateful-runtime.ts:389`) |
| R22 | 일치 | `runtime/foundation/service-stateful-runtime.ts:2074-2110`(`actorSend`가 Actor mailbox에 직접 enqueue); Actor별 mailbox `runtime/actors/actor-mailbox.ts:16-30,52-74`; scheduler owner-local `runtime/execution/serial-scheduler.ts:60-63,148-182` | `ApplicationJobQueue`는 admission permit이지 실행 순서 queue 아님 |
| R23 | 일치 | terminal-once: `runtime/foundation/operation-registry.ts:87-133`(take 후 완료, 중복 no-op); 재전송 없음: `runtime/host/actor-packet-relay.ts:481-513`는 로컬 캐시 hint만 재해석, Store 재조회·다른 owner 전송 없음 | 테스트 `test/m6b/m6b-runtime.contract.ts:3953-3975` |
| R24 | 일치 | reply target은 bind 시 context에 고정(`actor-session-binding-registry.ts:262-272`) 후 캡처 stream에 직접 씀(`runtime/streams/bound-session-response-target.ts:33-68`); close 시 `cleanup`→`unbind`(`registry.ts:274-310`) | 전용 테스트 없음. 캡처 실패 시 current route fallback(`actor-packet-relay.ts:972-1005`)이 있으나 dispatch가 `requireCurrentToken`으로 gate되어 정상 경로에선 도달 어려움 |
| R25 | 일치 | destroy→binding 삭제 `service-stateful-registry.ts:339-344`; 새 incarnation에서 이전 binding 무효 `registry.ts:373-376`(generation 필터); 재bind는 `sameActorRef`(objectGeneration 포함) 불일치 시 `replaceBinding` `session-actor-coordinator.ts:251-280,584-588` | 테스트 `test/m6b/m6b-runtime.contract.ts:1526,1602` |
| R26 | 일치 | ActorId/ObjectGeneration/MeshName/NodeRid: `contracts/Common/ActorRef.ts:3-8`; NodeGeneration·AuthorityOwnerGeneration·OwnerLeaseGeneration: `actor-session-binding-registry.ts:122-129`; session owner RID·lifecycle gen·binding gen·token: `service-stateful-registry.ts:49-58`; sequence: `service-stateful-runtime.ts:389,1279` | `ownerNodeGeneration`·`sessionOwner*` 필드가 타입상 optional — 스펙 표는 항상 보관을 전제. R31/R32 불일치와 연결됨 |
| R27 | 일치 | 38 상수 `service-wire-constants.generated.ts:34`; 요청 1회 `service-stateful-runtime.ts:1708-1714,3255-3260`; owner 검증 `validateActorFence`(`:4593-4602`, nodeRid·NodeGeneration·AuthorityOwnerGeneration; ObjectGeneration은 `requireActor`); 등록 후 reply 1회 `:3255-3310` | 테스트 `test/m6b/m6b-runtime.contract.ts:3978,4262` |
| R28 | 일치 | 24 record에 bindingGeneration+sequence `service-stateful-wire-codec.ts:1156-1181`; 36 송신 gate `service-stateful-runtime.ts:1821-1841,4888-4913`(`validateBoundSession`+NodeGeneration·AuthorityOwnerGeneration); 수신측 재검증 `:3541-3552`(직접 확인: bindingGeneration·sourceNodeGeneration·routingId 일치 요구) | |
| R29 | 불일치 | bind 전 authority Store 읽기 확인: `session-actor-coordinator.ts:112-113,221-249` → `runtime/host/index.ts:401-414` → `runtime/locations/resolvers.ts:387-410`(`readAuthority`); local instance overload: `session-context.ts:74-78,454-457`, `session-actor-coordinator.ts:54-61,425-430`(`ZLinkActor\|ActorRef` 허용) | Store 조회는 fence(generation) 채움 전용, route(nodeRid)는 덮어쓰지 않으며 ref에 generation 3종이 이미 있으면 생략됨; local instance overload는 공개 interface(`IZLinkSessionActor.ts:6`)엔 없고 내부 표면에만 존재 — 문구상 위반이나 영향은 제한적 |
| R30 | 일치 | follow 1회 dedup `runtime/actors/actor-handoff.ts:1085-1100`; follow 없음/만료→`actorLocationStale`→`Unavailable` `actor-handoff.ts:511-609,1479-1484`, `runtime/framework-errors-internal.ts:74`; session relay 경로에 별도 `resolveDirectActorRoute` 호출 없음 | 스펙 미정 값 코드가 선택: `MAX_MESSAGE_FOLLOW_HOPS=8`(`actor-handoff.ts:42`), follow duration 30,000 ms(`:41`) |
| R31 | 불일치 | ingress fence가 `ownerLeaseGeneration` 미비교: `service-stateful-runtime.ts:4593-4603`(직접 확인 — nodeRid·targetNodeGeneration·authorityOwnerGeneration만 비교, Spot fence는 `:4666-4668`에서 별도 비교하나 Actor fence는 안 함); 저장 route에 admission deadline 없음, `authorityFence`는 optional(`actor-session-binding-registry.ts:28,124`) | Store 장애 시 연장 없음은 일치(resolver 캐시 `resolvers.ts:463-473,693-701`) — 단 이는 location 캐시이지 binding route 자체는 아님. "local admission deadline" 상수는 relay 경로에서 미발견 |
| R32 | 불일치(부분) | 직접 확인: `service-stateful-registry.ts:373-386`(`installSessionBinding`)의 대소 비교 gate가 `sessionOwnerNodeRid===...&& sessionRid===...&& bindingGeneration>=...`만 검사하고 `sessionOwnerNodeGeneration`(lifecycle generation)은 비교하지 않음 | 같은 RID·sessionRid로 재시작한 owner가 더 작은 counter로 다시 bind하면 `ServiceStaleGenerationError`로 오거부될 수 있음 — 스펙 §4는 "다른 lifecycle이면 새로운 identity로 등록 가능"을 요구 |
| R33 | Actor owner 일치 / session owner 불일치 | owner측: 등록→reply→51 fire-and-forget `service-stateful-runtime.ts:3282-3297,3380-3390`; session owner측: 로컬 route를 owner reply **전에** 설치(`session-actor-coordinator.ts:130-158`), replacement branch는 `waitForAcknowledgement=false`(`:161-162`), owner 실패 시 diagnostics만(`:196-201`) | 최초 bind branch는 실패 시 rollback(`:169-195`)으로 스펙과 일치하나, rebind branch는 owner bind가 실패해도 로컬에 설치된 새 route가 남을 수 있어 "bind 실패 시만 기존 route 유지" 요구와 어긋남 |
| R34 | 대체로 일치 | 최대 1회 적용 `service-stateful-runtime.ts:3349-3372`(`appliedReplacementNotices`); identity 전부 일치 `:3319-3347`; closing 전이·inbound 거부 `session-context.ts:208,279-282`; 100 ms non-blocking timer+identity 재확인 직접 확인(`stream-session-runtime.ts:367-381` `scheduleClose`가 `isExactRetiredActorBinding` 재검증 후 close, `:74` delay=100); deadline 강제 종료 `:398-411`; callback 최대 1회 `callbackTerminal` guard(`:381-388`) | 스펙 미정 값: callback deadline 기본 30,000 ms(`stream-session-runtime.ts:73`, `runtime/streams/index.ts:279`의 `requestTimeoutMs ?? 30_000`). "outbound 비어도 단축 없음"은 단축 코드가 없다는 사실(부재)로 판정 |
| R35 | 일치 | 51 전송 실패 → bounded async retry `service-stateful-runtime.ts:3392-3448`(25 ms 시작·배증·최대 1 s·30 s 마감, `:106-108`); rollback 없음 주석 `:3427-3428`; callback 실패 diagnostics만 `stream-session-runtime.ts:392-398`; 이전 generation ingress 거부 유지 `:2081-2086` | 스펙 미정 값: retry 25 ms/1 s cap/30 s deadline — G3와 일치하는 값 |
| R36 | 불일치(부분) | tombstone 송신 `service-stateful-runtime.ts:1751-1784`; 수신은 bindingGeneration·sessionRid·ownerNodeRid 정확 일치 시만 제거 `:3450-3478`; 이전 lifecycle late push/51 거부 `:3324-3328,3547`; control/one-way는 application queue 밖 `:1900-1940`. **순서 위반**: 일반 disconnect 경로에서 `runtime/streams/bound-actor-relay-sender.ts:196-224`(`notifyDisconnectedCore`)가 `stream.unbindActor`(tombstone 트리거)를 `options.notifyDisconnected`(disconnect 콜백) 호출 **이전**에 실행 — R42와 동일 코드 경로·동일 원인 | R42의 불일치와 동일 근거(같은 `unbind-before-callback` 패턴). 테스트 `test/m6b/m6b-runtime.contract.ts:4004-4038`는 tombstone identity 정확성만 검증, 순서는 검증 안 함 |
| R37 | 일치 | 동일 session 재제출 → 즉시 Ok, 51 미전송 `service-stateful-runtime.ts:3273-3280`(`sameSessionIdentity`); 51은 `sameSessionBinding` 불일치 시만 `:3289-3297`; physical disconnect 시 snapshot all-settled 각 1회, 새 identity 보호 `runtime/streams/bound-actor-relay-sender.ts:230-272`(직접 확인) | 테스트 `test/m6b/m6b-runtime.contract.ts:4143-4161` |
| R38 | 일치 | 같은 objectGeneration·같은 nodeRid → `updateRef`만(rebind 아님) `session-actor-coordinator.ts:313-361`; objectGeneration 다르면 `requireSameIncarnation` throw `:590-603`; 새 generation은 `bindOrGet`이 `replaceBinding`(명시적 bind) `:262-281`; relocation route 적용은 `applyRelocation`(`actor-session-binding-registry.ts:708-799`, 직접 확인)이 seal 범위 내에서 route만 갱신, 새 identity 미생성 | 같은 objectGeneration이지만 nodeRid가 바뀌는 케이스가 `replaceBinding`(native bind 재실행)을 타는지, 그것이 §8/§9의 "rebind 아님" 경로와 정확히 일치하는지는 완전히 확정하지 못함(session-actor-coordinator.ts:362-369) |
| R39 | 일치 | atomic 등록→reply→51 one-way, ACK 없음 `service-stateful-runtime.ts:3282-3297,3421-3436`; bind 실패 시 `installSessionBinding` 미호출로 기존 유지 `:3301-3304`; 늦은 tombstone이 새 identity 미제거 `:3454-3478` | Actor owner 측 계약. session owner 측 route 유지 문제는 R33에 별도 기재 |
| R40 | 부분 불일치 | Actor 부재·nodeRid/NodeGeneration/AuthorityOwnerGeneration 불일치·ObjectGeneration 불일치 모두 `validateActorFence`→`ServiceStaleGenerationError`→`actorLocationStale`→`Unavailable`(`service-stateful-runtime.ts:3261,4593-4602`, `framework-errors-internal.ts:74`); 38 처리(`replySessionBind`, `:3255-3310`)에 Message Follow relay 분기 없음; `BindOrGet`의 Get은 `sameActorRef`(nodeRid·actorId·objectGeneration) exact 일치 시만 반환(`session-actor-coordinator.ts:262-268,584-588`, 직접 확인), hidden retry 없음 | (1) ObjectGeneration 불일치가 스펙이 요구하는 `InvalidOperation`이 아니라 `Unavailable`로 끝남 — `ActorGenerationStale→InvalidOperation` 매핑은 Message Follow 계층에만 연결되고 command 38 경로엔 미연결. (2) bind 시 Message Follow relay 미구현 → 곧바로 Unavailable. pre-commit seal→Unavailable은 `runtime/host/service-relocation-host-runtime.ts:1118-1146`로 일치 |
| R41 | 일치 | `contracts/Streams/BoundSessionContracts.ts:1-4`(`ZLinkBoundSession { send(); disconnect(); }` — global proxy 없음); `runtime/streams/bound-actor-relay-sender.ts:290-301`(`disconnectBoundSession()`이 route unbind만, Actor/Spot 미변경) | |
| R42 | 불일치(부분) — 6/7 항 일치, 순서 반전 | 직접 확인(`sed -n '195,275p' runtime/streams/bound-actor-relay-sender.ts`): snapshot `:231`; all-settled `:233-271`; dedupe — automatic path는 `route.bindingToken===actor.bindingToken` 불일치 시 조용히 반환(`:241,251`), public path는 `requireCurrentToken`(`:211`)에서 예외; deadline 실질값은 `DEFAULT_ACTOR_BIND_TIMEOUT_MS=30_000`(`.../streams/index.ts:140,379`); Store 조회 없음. **불일치점**: route unbind(`:248`/`:211`)가 disconnect callback 실행(`:256`/`:227`) **이전**에 발생 — 스펙 §7은 callback terminal 뒤 tombstone 순서를 요구. 코드 주석(`:244-247`)은 의도적("재접속이 알림 진행 중에도 새 route를 설치할 수 있도록")이라고 명시 | |
| R43 | 일치 | automatic: `runtime/host/actor-packet-relay.ts:116-144`(`notifyBoundActorDisconnected`); public: `:162-192`; 둘 다 `runtime/spots/spot-actor-membership.ts:230-243`(`onDisconnectActor?.(joinedActor)`)로 수렴, Entry-Spot fallback `spot-entry-activation.ts:448-449` | destroy/membership 변경 없음 |
| R44 | 일치 | `runtime/streams/` 하위에 `LocationStore`/`selectTarget` 참조 없음; handoff payload는 메타데이터뿐(`service-relocation-host-runtime.ts:5605-5620`, socket bytes 없음); `:1281-1307`(`handleSessionRelocationRoute`는 fence 검증과 위임만) | |
| R45 | 일치 | `remote-bound-session-relay.ts:543-558`(`validateServiceWireSessionRoute`가 SessionRid, binding generation+ActorId/ObjectGeneration, relocation identity dedupe, seal-binding==route-binding 4값 검증); `service-relocation-host-runtime.ts:1313-1330`(`validateSessionOwnerFence`) | |
| R46 | 일치 | command 42 request `actor-transfer-runtime.ts:1008-1030`; reply(`ServiceSessionRelocationSealed`, `service-stateful-wire-codec.ts:319-324`)에 relocation/coordinator/actor/session만, sequence/high-water 없음; held-queue는 actorId/sealId scope(`actor-session-binding-registry.ts:658-694`). E2E: `framework/languages/node/test/contract/bound-session-relocation-two-process.test.js:22-39`(seal 후 `command42:1, command44:0, writes:0`, push는 `submitted`이나 commit까지 보류) | |
| R47 | 일치 | one-way commit `remote-bound-session-relay.ts:502-533`가 `commitActorRoute(...,{releaseSeal})`로 route+seal 원자 갱신(`actor-session-binding-registry.ts:708-799`, `commitOwnerTransition()` 먼저, held는 arrivalOrder로 이후 drain); reply 없음(command 42만 reply 있음); command 45는 `service-wire-constants.generated.ts:37-45`에 없음(44→46); duplicate no-op `remote-bound-session-relay.ts:376-391,411-419` | 비고: matching 42 없는 orphan 44(`state===undefined`, `:392-401`)는 duplicate no-op이 아니라 `ZLinkRemoteBoundSessionFenceError`로 discard됨 — true fingerprint-matching duplicate만 no-op |
| R48 | 불일치(부분) | timeout 값/설정 일치 `service-relocation-host-runtime.ts:1091-1096`, `remote-bound-session-relay.ts:337-341`(`sessionRelocationSealTimeoutMs ?? 3_000`); race 처리 일치 `:376-391,422-423`. **누락**: `late_session_route_update` Warning 로그 문자열이 `src/` 전체에 없음(grep 결과 0건) — late/terminal duplicate는 조용히 무시됨(`:376-383`) | |
| R49 | 일치 | abort-before-accept `remote-bound-session-relay.ts:488-501`(seal 해제, outbound는 unchanged/source route로, reply 없음); accept 후 no-rollback은 `service-relocation-host-runtime.ts:891` 코드 주석("not rolled back here")이 명시, seal-timeout이 정리 담당 | |
| R50 | 일치 | 동일 no-rollback 주석(`:891`); `messageFollow:50` wire command 존재(`service-wire-constants.generated.ts:44`); shutdown은 connection을 이동 아닌 종료(`runtime/host/runtime-shutdown.ts:44-52`, `stream-session-runtime.ts:980-981`) | 명시적 전역 순서 보장 코드는 없음(스펙과 일치 — 원래 미보장) |
| R51 | 일치 | `stream-session-runtime.ts:805-843` — session당 단일 `ZLinkStreamSessionSerialExecutor`가 `'lifecycle'`과 `'application'` lane을 하나의 `ZLinkBoundedSerialScheduler`(`runtime/execution/serial-scheduler.ts:65-99`)로 직렬화; Actor mailbox는 별도 scheduler(`runtime/actors/actor-mailbox.ts:17-21`), lock 공유 없음 | |
| R52 | 일치(세부는 미확인) | `runtime/execution/index.ts:29-48`(`ZLinkExecutionArea='application'\|'infrastructure'`+`requireZLinkInfrastructureExecutionArea()`); disconnect/relocation-route 경로는 ingress/timer callback에서 실행, application callback 내부에 중첩되지 않음 | 모든 호출부를 전수 조사하지 않음 |
| R53 | 일치 | `stream-session-runtime.ts:980-981`(`drainCloseSessions` — `Promise.allSettled`); `runtime-shutdown.ts:44-52`; 프로세스 간 connection 이전 코드 없음 | |
| R54 | 일치(세부는 G2와 겹침) | stale+noFollow→Unavailable: `framework-errors-internal.ts:74`(`ActorLocationStale→Unavailable`); generation 다름→InvalidOperation: `:81`(`ActorGenerationStale→InvalidOperation`, 단 command 38 경로엔 미연결 — R40 비고 참고); pre-commit seal→Unavailable: 동일 매핑; packet key 중복→startup: `RegistrationValidators.ts:483-490`; factory 없음→explicit create error: `runtime/actors/index.ts:415-418`(`ZLinkConfigurationException`, `ErrorKind` 아님); binding 없이 push/close→InvalidOperation: `framework-errors-internal.ts:17,61`(`ActorSessionNotBound→InvalidOperation`); stale fence→typed stale error: `ActorLocationStale` 매핑 재사용(공개 kind 별도 없음) | factory/stale fence의 정확한 값은 G2에서 미지정으로 별도 확인됨 |
| R55 | 일치 | gate 분리: session scheduler와 Actor mailbox scheduler가 별개 인스턴스; 제어 record 우선순위: `serial-scheduler.ts:293-319`(`takeNext()`/`takeLifecycle()`이 `'lifecycle'`을 `'application'`보다 우선, burst limit 기본 8) | |
| R56 | 불일치(부분) | 단일 engine 공유는 일치(`ZLinkBoundedSerialScheduler`가 session/Spot/Actor mailbox 모두에서 재사용); **lane 정책 타입 미발견** — `serial-scheduler.ts:1`은 `ZLinkSerialWorkLane='application'\|'lifecycle'`만 정의, 도메인별 상태는 `spot-actor-join-dispatch.ts:114`, `spot-routed-frame-dispatch.ts:97`, `actor-session-binding-registry.ts:56`(`phase:'sealed'\|'applying'\|'applied'\|'terminal'`)에 흩어진 boolean/string; `LaneState`/`LanePolicy` grep 0건 | |
| R57 | 일치 | 재접속은 새 session object 생성(`getOrCreateSession`, `stream-session-runtime.ts:934-935`), 복원 경로 없음; relocation은 같은 session 유지, route만 교체(`actor-session-binding-registry.ts:187-247`) | |
| R58 | 일치 | `actor-session-binding-registry.ts:886-926`(`clearRouteRelocation` docstring이 retired-binding teardown과 relocation-seal lifecycle을 명시적으로 구분); tombstone(`service-stateful-runtime.ts:3454-3524`)과 seal state(`:708-799`)는 별개 state machine으로 공존 | |
| R59 | 불일치 | 직접 확인(`sed -n '140,165p;1095,1130p' runtime/streams/actor-session-binding-registry.ts`): `outboundCapacity=4096` 생성자 기본값(`:147`); `reserveOutbound`(`:1105-1126`)가 `queue.outboundCount < outboundCapacity`로 개수 상한 강제 — 스펙의 "seal에 numeric high-water/개수/byte 상한 없음"과 모순 | |
| R60a | 일치 | `RegistrationBuilders.ts:696-700`(`enableActorDispatch()` — MeshName 파라미터 없음) | |
| R60b | 일치 | `RegistrationValidators.ts:565-576`(Object role/Store 없으면 `ZLinkConfigurationException`) | |
| R60c | 일치 | `actor-session-binding-registry.ts:141`(actorId별 독립 route/token) | |
| R60d | 일치 | `service-stateful-runtime.ts:2074-2110`(`actorSend`가 Actor application queue에 직접 enqueue, Spot callback 미경유) | |
| R60e | 일치(경계선) | `runtime/streams/managed-stream.ts:173-228`(`bindActor` — 재시도 루프 없음); relocation-seal 재전송은 seal 채널에 한정, bind 자체의 hidden Store retry 아님 | |
| R60f | 일치 | `service-wire-constants.generated.ts:22,34,32`(actorSend:24, boundSessionBind:38, boundSessionSend:36) | |
| R60g | 일치(표준 패턴) | `service-stateful-runtime.ts`에서 correlation당 1회 `replyWire(...)` 호출 패턴(예: `:3273-3283`) | 전수 조사 아님 |
| R60h | 일치(구조적) | socket/session object를 프로세스 간 전달하는 코드 없음(부재로 확인); `handleSessionRelocationRoute`는 routing state만 갱신 | white-box 불변조건, 명시적 assertion/test는 미발견 |
| R60i | 불일치 | lane 상태 표에 대한 컴파일 타임/정적 강제 없음 — `ZLinkSerialWorkLane`은 도메인 상태와 무관, `draining`/`sealId`/`phase` 등이 클래스별 독립 boolean/string; "session lane + relocation seal" 같은 무의미 조합을 타입이 막지 않음 | R56과 동일 원인 |
| R60j | 일치 | `stream-session-runtime.ts:74`(`ZLINK_STREAM_ACTOR_BINDING_REPLACEMENT_CLOSE_DELAY_MS=100`), `:367-381`(callback terminal 뒤 non-blocking timer로 예약) | |

## G 항목 — 이 구현의 실제 동작

| G# | 이 구현이 하는 것 | 근거 (파일:줄) |
|---|---|---|
| G1 | 별도 설정 가능한 상수: `ZLINK_STREAM_ACTOR_BINDING_REPLACEMENT_CALLBACK_TIMEOUT_MS=30_000`(30초), `options.replacementCallbackTimeoutMs`로 override 가능 | `stream-session-runtime.ts:73,160,398-411` |
| G2 | "Actor factory 없음" → `ZLinkConfigurationException`(일반 예외 클래스, `ErrorKind` 멤버 아님). "fence stale" → 내부 kind `ActorLocationStale` → 공개 `Unavailable`로 매핑 — 다른 stale-route case와 같은 공개 kind 재사용, 별도 "typed stale" 공개 kind 없음 | `runtime/actors/index.ts:415-418`(factory); `runtime/framework-errors-internal.ts:9-51,74`(ActorLocationStale→Unavailable) |
| G3 | `boundSessionReplaced`(51) admission 실패 재시도: 초기 지연 25ms → 2배씩 증가 → 상한 1000ms, 전체 재시도 마감 30,000ms | `service-stateful-runtime.ts:106-108`(`SESSION_BINDING_REPLACEMENT_RETRY_DEADLINE_MS=30_000`, `_INITIAL_DELAY_MS=25`, `_MAX_DELAY_MS=1_000`), 사용처 `:3378-3388,3425-3434` |
| G4 | 코드 차원의 함의 없음 — server/client 스펙 문서 소유권 문제. 해당 없음 | — |
| G5 | 구체적 shared-permit 코드 존재: session record와 lifecycle/control record 모두 owner당 하나의 `ZLinkBoundedSerialScheduler`를 거치며 `'application'`/`'lifecycle'` 별도 admission 예산(메시지/byte capacity+burst limit)을 가짐 — "host permit"이 scheduler 레벨에서 이미 통합돼 있음(문서 소유권과 무관하게) | `runtime/execution/serial-scheduler.ts:1-30,293-319` |
| G6 | 구조적으로 원천 차단: `StreamNodeRegistration.session`은 컬렉션이 아닌 단일 optional 필드 — validator는 `session===undefined`(누락)만 확인; "한 node에 session 2개 등록"은 config 형태상 표현 자체가 불가능; "같은 session type을 다른 node에 중복 등록"을 별도로 확인하는 검사는 없음(R14 불일치와 연결) | `contracts/Configuration/RegistrationValidators.ts:561-576`, `RegistrationBuilders.ts:713-718` |
| G7 | Node는 20/48 순서(seal 해제 → held message 재제출)를 따르며 52의 순서를 따르지 않음: `applyRelocation`의 abort 분기에서 `commitOwnerTransition()`(seal 정리 포함)이 `startOutboundDrain(queue)`(held outbound 재제출)보다 먼저 실행 | `actor-session-binding-registry.ts:708-798`(commit 경로 757-787: `commitOwnerTransition()` 먼저, `startOutboundDrain`은 787 이후); abort는 `remote-bound-session-relay.ts:488-501` |
| G8 | 별도 "source coordinator" 타입 없음 — 단일 `ServiceWireRelocationCoordinatorFence` wire 타입을 양쪽 역할에 재사용, wire record의 `senderRole:'source'\|'target'` 필드로 구분(클래스명 구분 아님) | `service-relocation-host-runtime.ts:172,1948,3141`; `runtime/protocol/service_wire_pilot_codec.generated.ts:162`(`senderRole`) |

## 요약

- 불일치 12건: R14, R29, R31, R32, R33, R36, R40, R42, R48, R56, R59, R60i. R14/R29/R32/R33/R36/R40/R42/R48/R56은 규칙의 일부 하위 조항만 어긋나는 "부분 불일치"이며, R31·R59·R60i는 전체 조항이 어긋남
- 스펙 미정(코드가 값을 선택): G1(replacement callback deadline 30,000 ms), G2(factory 없음 에러 종류·stale fence 공개 kind), G3(51 재시도 25 ms 시작·2배 증가·1 s 상한·30 s 마감), R21(session sequence가 session별이 아닌 node 단일 카운터), R30(Message Follow hop 상한 8·duration 30,000 ms), R34(replacement callback deadline 30,000 ms)
- 판단 불가 0건. R18-R60 전 항목을 직접 코드 확인으로 판정했으며, R38("같은 objectGeneration·다른 nodeRid" relocation이 §8/§9의 non-rebind 계약과 정확히 일치하는지)만 완전히 확정하지 못해 해당 행 비고에 유보 표기

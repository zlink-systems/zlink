# session spec ↔ cpp 구현 대조

검토 기준: f1a2f416f6aa98274af32b9be61e5665dc3c43e7
검토 범위: `framework/languages/cpp/framework/{src,include}`, `framework/languages/cpp/tests/`,
`framework/languages/cpp/http-client/`, `framework/languages/cpp/connector/core/`,
`framework/runtime/protocol/generated/cpp/service_wire_constants.hpp`(cpp가 참조하는 공유 생성
헤더), `framework/doc/framework/common/spec/stream-connector/32-stream-connector.ko.md`(§6.3
대조용)

## R 규칙 대조

| R# | 판정 | 근거 (파일:줄) | 비고 |
|---|---|---|---|
| R1 | 일치 | `framework/src/runtime/streams/stream_host_service.cpp:1734` (`_core_socket->recv(..., dontwait)`, 명시적 poll+recv loop) | Core packet/raw callback 등록 코드 없음. |
| R2 | 불일치 | `framework/src/runtime/streams/stream_host_service.cpp:1697-1704,1734` | admission이 실패한 key가 있어도 다른 peer 진행을 위해 poller.wait/recv를 계속함(주석에 명시). 폐기·재전달은 없음(`core_stream_frame_assembler_t::append` 932-933)이나, "admission 실패 동안 새 packet 안 읽음" 원칙은 해당 key에 대해서만 지켜지지 않음. |
| R3 | 일치 | `framework/src/runtime/streams/stream_host_service.cpp:2695-2777`(`dispatch_core_frame`→`dispatch_packet_async`), `framework/src/runtime/streams/stream_runtime.cpp:1574-1660` | handler filter 호출 경로(`invoke_filters_async`) 미사용. |
| R4 | 일치 | `stream_host_service.cpp:2432` → `stream_runtime.cpp:1440-1446` → `stream_host_service.cpp:1603` | recv 시 저장한 routing id를 send 시 그대로 재사용. |
| R5 | 일치 | `stream_runtime.cpp:753-756`(response header `packet_name=""`), `stream_host_service.cpp:1640-1642,3561`(error header 동일), `stream_runtime.cpp:1073-1078`(`encode_header`가 response/error의 packet_name 비공백을 거부), `connector/core/src/runtime/calls/zlink_stream_calls.cpp:858-864`(client가 sequence만으로 매칭), `connector/core/include/.../zlink_stream_calls.hpp:140,184-199`(caller 지정 template type으로 decode) | 3개 세부 조항 모두 wire 레벨에서 강제됨. |
| R6 | 일치 | `stream_host_service.cpp:3287-3302`(closing 플래그 선행 후 io_context에 close post), `:3044-3049`(write 완료 중복 가드), `:2517-2524`(`finalized` 플래그로 이중 완료 방지) | |
| R7 | 불일치 | server root: `framework/src/runtime/host/app.cpp:469`(`serializer_registry_t serializers` 단일 멤버); connector instance: `connector/core/include/.../zlink_stream_connector.hpp:271`(`codec_registry_t _codecs` non-static 멤버) — 둘은 서로 미공유. HTTP client: `http-client/` 전체에서 host별 codec registry 타입을 찾지 못함(`framework::codecs::json`/`parse_json<T>()` 정적 함수 사용) | server·connector 축은 스펙대로 분리되어 있으나, HTTP client는 스펙이 요구하는 "host별 registry instance" 구조 자체가 cpp에 존재하지 않음. |
| R8 | 일치 | transport 오류→callback: `stream_host_service.cpp:3805-3810`→`:3881-3882`; handshake 실패→session 없음: `:3899-3913`; socket/accept 오류→session 없음: `:3209-3233`; handler 예외↛transport 오류: `:1358-1375`(별도 `dispatch_error_reporter_t` 경로) vs `:3805-3810`(`boost::system::system_error`만 `session_transport_error` 채움) | handshake·accept 오류 경로 안에서 runtime-monitoring 계측 호출은 직접 확인 못함(판단 보류, 판정에는 영향 없음). |
| R9 | 일치 | `framework/include/zlink/framework/contracts/streams/stream.hpp:89-97`(`stream_close_reason_t`: client_close/idle_timeout/heartbeat_timeout/server_drain/protocol_error/transport_error) | `framework/doc/framework/common/spec/stream-connector/32-stream-connector.ko.md` §6.3의 6개 값과 1:1 대응. |
| R10 | 일치 | `framework/include/zlink/framework/contracts/configuration/framework_options.hpp:1259-1276`(`register_session(...)` 명시 빌더 호출) | attribute/decorator/static 자동등록 코드 없음. |
| R11 | 일치 | 기본 64KiB: `framework_options.hpp:1170`; `0`→`-1`: `stream_host_service.cpp:1673-1674`; 음수 startup error: `framework_options.hpp:1305-1308`; 6-byte prefix 제외한 header+payload 합으로 측정: `stream_host_service.cpp:818-829`; inbound만 적용(`read_frame`만 호출, `write_frame`은 미호출); EMSGSIZE 기록 후 종료: `stream_host_service.cpp:2917-2929` | 모든 세부 조항 확인됨. |
| R12 | 일치 | `framework_options.hpp:1284-1292`(`set_session_name`이 `_session_configured` 재설정 시 예외) | |
| R13 | 일치 | cert+key 필수: `framework_options.hpp:1234-1236`; require_client_certificate 기본 false: `:1233,1359` | `require_client_certificate`가 `set_tls_server`의 파라미터라 "TLS 없이 client cert 요구"는 API 구조상 원천 불가능(구성상 동일 계약 충족). |
| R14 | 일치 | 이름 공백:`framework_options.hpp:1183`; 이름 중복:`:1184-1188`; endpoint 없음:`detail/framework_options_validation.hpp:197-202`; session type 중복:`framework_options.hpp:1289-1292`; node당 2+ session:`:1286-1288`; cert 공백:`:1235`; key 공백:`:1236` | 8번째("TLS 없이 client cert 요구")는 R13과 같은 이유로 API 구조상 불가능 — 별도 검증 코드 없이 동일 보장. |
| R15 | 일치 | `framework/include/.../streams/stream.hpp:252-274`(session callback은 `stream_t&`만 받음), `:198-227`(`stream_t` 공개 API에 Spot mutation 표면 없음) | |
| R16 | 일치 | 스펙 커맨드 집합(24·36·38·42·43·44·51) 근거: `framework/runtime/protocol/generated/cpp/service_wire_constants.hpp:28,38,40,43-45,51`(언어 공용 생성 헤더); cross-node 전달만: `stream_host_service.cpp:2361-2372`; Node RID·generation·fence·codec 비노출: `framework/include/zlink/framework/contracts/streams/stream.hpp` 등 공개 contracts 헤더에 해당 필드 0건 | 45는 enum에 아예 없음(44→46으로 점프), reserved 원칙과 일치. |
| R17 | 일치 | `stream_host_service.cpp:2342-2372`(`is_current_for_connection` 확인 후에만 tombstone 진행, stale binding은 `session_owner_route_tombstone_stale_ignored`로 skip), `framework/src/runtime/mesh/mesh_node_runtime.cpp:3849`(current `binding_generation`으로 tombstone 전송) | |
| R18 | 불일치 | `framework_options.hpp:1249-1256`(`enable_actor_dispatch()`는 MeshName 없이 write-only set에 삽입만); `framework/src/runtime/host/app.cpp:207-221`(Location Store 부재 startup 거부는 존재하나 mesh_node object_role 기반이지 `enable_actor_dispatch` 호출과 무관); `framework/src/runtime/stateful/public_host_runtime.cpp:37-44,3987` | MeshName 미수신은 스펙과 일치하나, "role/Store 없으면 이 enablement 자체를 startup에서 거부"라는 연결고리는 코드에 없음 — Store 부재 검증은 bind 시점에 `read_route_owner_fence`로 별도 수행됨. |
| R19 | 일치 | `framework/src/runtime/actors/actor_gateway_runtime.hpp:122-136`(Actor당 단일 route/token), `:152`(session→여러 actor_id) | |
| R20 | 일치 | `actor_gateway_runtime.cpp:1895-1906`(캐시된 `bound_session_route`의 `binding_generation` 사용, Store 미조회) | |
| R21 | 불일치 | `framework/src/runtime/protocol/service_wire_codec.hpp:174-191`(`actor_message_header_t`+`bound_session_source_t`: correlation, object_generation, authority_owner_generation, owner_lease_generation, **binding_generation**, session_sequence) | wire envelope에 "binding token" 필드가 없고 대신 binding **generation**을 담음; `binding_token`은 Actor→session 방향의 local-only 값(`actor_gateway_runtime.cpp:1113-1122`). |
| R22 | 불일치 | Actor별 FIFO 큐: `actor_gateway_runtime.cpp:377-419`(`hpp:227`); `:418-419`에서 `drain_session_relay`가 호출 thread에서 동기 실행, eager coroutine(`initial_suspend = suspend_never`, `task.hpp` ~415)이라 첫 suspend 전까지 session thread에서 inline 시작될 수 있음(코드 주석이 이를 인지) | Actor별 큐 분리와 global 직렬화 금지는 지켜지나, "session callback thread에서 Actor handler를 실행하지 않는다"는 문장은 eager coroutine 첫 turn에 한해 어긋남. |
| R23 | 일치 | `framework/include/zlink/framework/contracts/dispatch/task.hpp:243-257`(`complete()`가 `if (_result) return`으로 idempotent) | replyRelay(33)/replyRelayAck(46)은 내부 reliability 경로이며 app-terminal 재전송이 아님. |
| R24 | 일치 | `actor_gateway_runtime.cpp:1075,1212,1293`(binding_token staleness 확인), `:1941-1946`(`exact_session_relay_route`가 session_rid+binding_generation을 fence) | |
| R25 | 일치 | `actor_gateway_runtime.cpp:2216-2233`(destroy 시 record+sink 제거); `hpp:245`+`cpp:1643-1648`(`next_binding_token` 단조 증가, 재사용 없음) | |
| R26 | 일치 | `actor_gateway_runtime.hpp:70-89`(`actor_bound_session_route_t`: node_rid, session_rid, object_generation, node_generation, authority_owner_generation, owner_lease_generation, binding_generation, binding_token, session_sequence); `:122-136`; `framework/include/.../actors/actor.hpp:132`(mesh_name) | 5개 정보 범주 모두 확인. |
| R27 | 일치 | `public_host_runtime.cpp:37-44`(`classify_bound_session_bind_admission`), `:3975-4030`(권한 확인→admission→bind→reply 1회); `service_wire_constants.hpp:40` | |
| R28 | 일치 | cmd24: `public_host_runtime.cpp:5800-5806,6204-6206`(binding_generation+session_sequence 포함); cmd36: `:5406-5444`(`current_tenure`가 5개 값 모두 current일 때만 제출) | `:5448-5459`의 `provisional_current_tenure`(owner_lease_generation==0 특례)는 별도 게이트(`confirm_remote_tenure`)로 제한됨. |
| R29 | 일치 | `session_actor_manager_t::bind`/`bind_or_get`은 `actor_ref_t`만 받음(actor.hpp); `actor_gateway_runtime.cpp:1580-1660`(`bind_current_session`에 Store 조회 없음) | |
| R30 | 일치 | `framework/src/runtime/mesh/mesh_node_runtime.cpp:3336-3420`(stale route에서 Message Follow 시도), `app.cpp:2178-2183`(admission 실패→unavailable) | Store 재조회 후 다른 owner로 자동 재전송하는 경로 없음. |
| R31 | 일치 | `public_host_runtime.cpp:297-338`(`read_route_owner_fence`, `owner_lease_fencing_margin` 기본 5000ms — `public_host_runtime.hpp:347`); `:303-306`(Store 불가→nullopt→stale) | Store 장애 시 lease 연장 없이 stale 처리. |
| R32 | 일치 | `actor_gateway_runtime.cpp:2440-2494`(identity: node_rid/node_generation/binding_generation), `:2452-2456`(`binding_generation >` 비교) | node_generation 동등성 비교가 같은 위치에 명시적으로 co-located돼 있는지는 별도 확인 못함(주요 규칙은 확인됨). |
| R33 | 일치 | `public_host_runtime.cpp:4013`(등록)→`:4023`(reply)→`:4036-4042`(51 전송) | 51 전송은 delivery admission만 기다리며 이전 session 완료를 기다리지 않음. |
| R34 | 일치 | `stream_host_service.cpp:1798-1888`(`begin_actor_binding_replacement`): identity 전체 일치 확인 `:1804-1821`; `closing=true` `:1831`(callback 전); 100ms non-blocking timer `:1871`; timer의 identity 재확인 `:1879-1884`; inbound는 closing 상태에서 거부 `:3701-3726` | force-close deadline은 5초 하드코딩 값(G1 참조), 스펙의 "lifecycle deadline" 용어와 이름은 다르지만 기능은 일치. |
| R35 | 일치 | `public_host_runtime.cpp:3295-3341`(`queue_bound_session_replacement_retry`/`retry_bound_session_replacements`); `:4041-4042`(bind terminal은 이미 반환된 뒤 재시도 큐잉); `actor_gateway_runtime.cpp:2653-2677`(구 generation ingress 계속 거부) | 재시도 횟수·간격의 구체값은 G3 참조. |
| R36 | 일치 | `app.cpp:2320-2326`(state==tombstone일 때만 `retire_bound_session_route`); `actor_gateway_runtime.cpp:2653-2677`(exact node_rid/session_rid/binding_generation만 일치할 때 처리); `raw_mesh_node_owner.cpp:3017-3067`(malformed record는 protocol_error, mailbox 미진입) | |
| R37 | 일치 | `actor_gateway_runtime.cpp:1790-1801`(`same_physical_bound_session` idempotent 조기 반환, 51 미전송); `:2863-2879`(`unbind_session_stream`이 matching binding_token만 제거) | |
| R38 | 일치 | `actor_gateway_runtime.cpp:2681-2726`(`commit_session_relocation_route`는 같은 object_generation 요구, binding_generation 재발급 없음); `:2450`(generation 변경 시 "actor generation is stale") | |
| R39 | 일치 | `public_host_runtime.cpp:4013-4042`(등록→reply→51); `:3325`(send는 admitted bool만 반환, ACK 없음); `actor_gateway_runtime.cpp:2443-2451`(bind 실패 시에만 route 유지) | |
| R40 | 판단 불가 | `actor_gateway_runtime.cpp:1614-1631`(`bind_or_get`이 exact session-id+token+object_generation만 매치); `public_host_runtime.cpp:36-46,4021-4034`(stale_route/actor_not_ready/ready → conflict/busy/notFound) | bind 실패 시 Message Follow route로의 relay, "pre-commit seal 중→Unavailable" 명시 분기, "ObjectGeneration 다름→InvalidOperation" 명시 분기를 bind 경로에서 찾지 못함(`boundSessionBind`×`messageFollow` 동시 출현 검색 0건). |
| R41 | 일치 | `public_host_runtime.hpp:372-390`(`bound_session_operations_t`: bind/send/replaced/commit_relocation_route 등); `actor.hpp:655-703`(`bound_session_t`: send+disconnect만); `actor_gateway_runtime.cpp:2863-2879`(unbind가 Actor lifecycle·Spot에 미영향) | |
| R42 | 일치 | `stream_host_service.cpp:2320-2381`(`retire_actor_session_bindings`: snapshot `:2331-2332`, per-identity try/catch loop, dedupe `is_current_for_connection` `:2340-2354`, 5s 내 대기 `:2367-2368`) | remote 경로(`mesh_node_runtime_t::retire_application_actor_session`)의 Store 미접근은 별도 전수 확인 못함. |
| R43 | 일치 | `framework/src/runtime/spots/spot_runtime.hpp:1662-1675`(`notify_on_disconnect_actor`); `actor_gateway_runtime.cpp:1281-1314`(`notify_disconnected`는 bound=false만 설정) | 두 경로 모두 `commit_actor_left`/`leave_actor`(`spot_runtime.hpp:1308`) 미호출, `actors_by_id` 미삭제. |
| R44 | 일치 | `framework/src/runtime/stateful/stream_session_registry.cpp:835-914`(`commit_remote_route`), `:916-969`(`acknowledge_remote_abort`); `stream_session_registry.hpp:199-200` | registry에 Store 접근 코드 없음; target은 wire에서 decode(`public_host_runtime.cpp:4383-4405`). |
| R45 | 일치 | `stream_session_registry.cpp:547-586`(seal 검증), `public_host_runtime.cpp:4344-4376`(relocation identity 대조), `stream_session_registry.cpp:846-874`(connection_id+binding_generation, object_generation/previous_authority_owner_generation, barrier_token 동등성) | 4개 검증 항목이 한 함수가 아니라 두 호출 지점에 나뉘어 있으나 항목 자체는 모두 확인됨. |
| R46 | 일치 | `public_host_runtime.cpp:1571-1642`(`admit_session_relocation_seal`); `service_wire_codec.hpp:548-562`(`session_relocation_sealed_t`에 sequence·high-water 필드 없음); `stream_session_registry.cpp:385-386,424`(barrier_token으로 hold) | |
| R47 | 일치 | `public_host_runtime.cpp:2839-2847`(주석: "command 44 is a one-way submit -- there is no application reply"); `stream_session_registry.cpp:875-901`(route 갱신 후 `_barriers.erase`); `:861`(barrier_token 없으면 conflict) + `public_host_runtime.cpp:4412-4414`(중복은 silent continue = no-op); `:424-438`(순서대로 재개) | command 45는 enum에 존재하지 않음(`service_wire_constants.hpp` 44→46). |
| R48 | 불일치 | 기본값 3000ms: `framework/include/.../locations/options.hpp:30`; timeout 처리: `public_host_runtime.cpp:2772-2803`→`stream_session_registry.cpp:612-664`(`close_remote_route_seal`); 먼저 처리된 것만 유효: `consumed` 플래그(`:2780,2792` vs `:4418-4421`) | `late_session_route_update`(또는 임의) Warning 로그가 코드 전역에 0건 — late command 44는 `:4412-4414`의 동일한 silent continue로만 처리되고 로그를 남기지 않음. |
| R49 | 일치 | `stream_session_registry.cpp:916-969`(abort: held message pop→barrier_token erase `:955-956`→notify_all `:960`, reply 없음, 호출자 `public_host_runtime.cpp:4406-4410`); `stream_session_registry.hpp:199-200`+`.cpp:907-912`(commit 실패 후 rollback 불가) | "relay-ready-accepted"라는 용어 자체는 코드에 없으나 동작(경계별 결과)은 일치. |
| R50 | 일치 | rollback 불가: `stream_session_registry.hpp:199-200`,`.cpp:907-912`; Message Follow: `raw_mesh_node_owner.cpp:1394-1399,2914-2919`; owner process 종료 시 강제 종료: `stream_session_registry.cpp:1002-1030`(`force_close_all`) | "connection 간 전역 순서 미보장", "disconnect는 relocation 증거 아님"이라는 negative 주장 자체를 뒷받침하는 코드/주석은 못 찾음(대응하는 반례 코드도 없음). |
| R51 | 판단 불가 | `stream_session_registry.hpp:286`(session 쪽 단일 `_mutex`); `actor_gateway_runtime.hpp:200-238`(Actor 쪽 별도 `_state->mutex`) | 두 mutex 객체가 분리돼 있다는 사실은 확인했으나, "session turn과 Actor turn을 shared lock이나 callback stack으로 합치지 않는다"는 명시적 검증(assertion/화이트박스 테스트)은 검색으로 찾지 못함. |
| R52 | 판단 불가 | `spot_runtime.hpp:537,545,1632,1649,1667`(`run_serial_task`로 일부 lifecycle callback 처리) | "infrastructure task" 개념에 대응하는 이름(`infrastructure_task`/`infra_task`/`schedule_infrastructure` 등)을 stateful/mesh runtime에서 찾지 못함; `stream_session_registry.cpp:907-909`의 `commit_terminal` hook은 오히려 동기 실행됨. |
| R53 | 일치 | `framework/src/runtime/stateful/maintenance_runtime.cpp:2089-2090,2192-2251`(`_state=draining`, `try_seal_all`); `stream_session_registry.cpp:981-993,184-185,373-374`(신규 bind/admit 거부); `force_close_all`(`maintenance_runtime.cpp:2104,2193,2241`) | "accepted는 deadline까지 처리"의 명시적 deadline 변수는 별도로 특정 못함. |
| R54 | 불일치 | ObjectGeneration 다름→`invalid_operation`: `actor_gateway_runtime.cpp:637-639,1073,1210,2347`; 중복 handler 등록→protocol_error: `framework/src/runtime/handlers/handler_registry.cpp:526-537`; factory 없음→not_found: `spot_runtime.cpp:4356,6619,8865`; stale fence→`boundary_error_t::stale_generation`(`error.hpp:36-44`) | "binding 없이 push/close 요청"이 스펙 표의 `InvalidOperation`이 아니라 `not_configured`("actor session is not bound")로 끝남(`actor_gateway_runtime.cpp:676-680,1070-1073,1207-1210,2345-2347`). "stale location+no Follow→Unavailable", "pre-commit seal→Unavailable"은 해당 조건과 kind를 직접 잇는 코드를 찾지 못함(근접 후보: `stream_session_registry.cpp:582-594`의 conflict/backpressured). |
| R55 | 일치 | `raw_mesh_node_owner.cpp:2887-2909`(liveness probe/ack는 `raw_mesh_pump_result_t::infrastructure`로 분류되어 application queue 미진입); `stream_runtime.cpp:245-254`(session lane) vs `spot_runtime.cpp:846-849`(entry/spot_wide/per_actor lane) — 별도 `serial_execution_queue_t` 인스턴스 | |
| R56 | 일치 | `framework/src/runtime/execution/serial_execution_queue.hpp:26-121`(단일 `serial_execution_queue_t`; `serial_lane_policy_t = std::variant<spot_lane_policy_t, session_lane_policy_t, actor_delivery_lane_policy_t>`); `:34-44`(spot: return_wait/relocation_sealed); `:52-59`(session: open/connection_closed); `:63-64`(actor-delivery: 빈 struct) | 스펙 §11의 3-lane 상태표와 정확히 일치, 무의미한 조합은 타입상 표현 불가. |
| R57 | 일치 | relocation은 연결 유지, route만 갱신: `public_host_runtime.cpp:4312-4340`(cmd44는 route 갱신만 하고 새 connection을 만들지 않음) | "재접속은 새 session"이라는 반대쪽은 이전 connection 상태를 복원하는 코드가 전무하다는 사실(부재)로 뒷받침됨 — 명시적 재접속-거부 코드가 아니라 복원 경로 자체가 없는 구조. |
| R58 | 일치 | seal(42/43/44): `public_host_runtime.cpp:4284-4326`; retired binding 거부(51): `public_host_runtime.cpp:4034-4047`+`raw_mesh_node_owner.cpp:3043-3061`(`actorSessionNotBound`) | 서로 다른 command·decode·struct로 완전히 분리되어 구현됨. |
| R59 | 일치 | `public_host_runtime.hpp:953-964`(`session_seal_terminal_record_t`: seal/sealed/last_accepted_sequence/barrier/expires_at/consumed/ready/response_routing_id/local_completions — cap 필드 없음) | high_water/byte_cap/record_count류 식별자 검색 0건. |
| R60(검증항목 표본) | 일치 | terminal-once abort: `tests/Zlink.Framework.UnitTests/test_cpp_framework_m6c_runtime.cpp:2602-2610`; cmd44 무응답·재전송 없음: `service_wire_constants.hpp:45`(45 결번), `public_host_runtime.cpp:4312-4340`(응답 send 없음); SealTimeout 기본 3000ms: `options.hpp:30` + `tests/Zlink.Framework.ContractTests/test_cpp_framework_contract_headers.cpp:1814-1815`(수치 assert); abort 경계(pre-cutover discard): `maintenance_runtime.cpp:574-578,835,981,994,1283` | |
| R60(100ms 지연 close 측정) | 판단 불가 | 구현 자체는 `stream_host_service.cpp:1871`에서 100ms 타이머로 확인되나(§ R34 참고), 실측 elapsed time을 assert하는 테스트는 찾지 못함 | |
| R60(1,000ms cutover 대기값) | 일치(값), 판단 불가(측정) | `options.hpp:34`(`relocation_cutover_wait_timeout{1000}`), `maintenance_runtime.hpp:337`, `maintenance_runtime.cpp:626-633` | 기본값만 확인, elapsed 측정 테스트는 못 찾음. |

## G 항목 — 이 구현의 실제 동작

| G# | 이 구현이 하는 것 | 근거 (파일:줄) |
|---|---|---|
| G1 | 스펙의 "lifecycle deadline"에 대응하는 이름이 코드에 없고, 대신 두 개의 구체 값을 씀: (a) retirement 시작 시 무조건 armed되는 5초 force-close deadline(콜백 진행과 무관), (b) 콜백 terminal 뒤 100ms non-blocking timer. | `framework/src/runtime/streams/stream_host_service.cpp:1842`(5s), `:1868-1888`(100ms, `:1871`) |
| G2 | (a) Actor factory 없음 → `framework_error_kind_t::not_found`, 메시지 "actor factory is not registered". (b) stale fence → 내부 전용 `detail::boundary_error_t::stale_generation`이며 공개 kind로는 `unavailable`(disconnected/closed와 같은 kind)로 매핑됨 — 스펙이 요구하는 "typed stale error"라는 별도 공개 ErrorKind는 없음. | `framework/src/runtime/spots/spot_runtime.cpp:4356,6619,8865`; `framework/include/zlink/framework/contracts/errors/error.hpp:36-44,80-81,139-142`; `framework/src/runtime/stateful/raw_stateful_dispatch.cpp:1242,1244,1452,1536` |
| G3 | 51 전송 admission 실패 시 재시도: 최대 4회, 최초 재시도는 +10ms, 이후 `10ms * 2^(attempts-1)`(약 10/20/40ms) exponential backoff. Pending 큐 용량 1024이며 가득 차면 가장 오래된 항목을 drop. | `framework/src/runtime/stateful/public_host_runtime.hpp:1132-1133`(capacity=1024, max_attempts=4); `public_host_runtime.cpp:3295-3304`(+10ms), `:3309-3345`(backoff `:3334`) |
| G4 | 종료 사유는 `stream_close_reason_t{client_close, idle_timeout, heartbeat_timeout, server_drain, protocol_error, transport_error}` 6개 값이며, Stream Connector §6.3의 닫힌 집합과 이름까지 1:1 대응(PascalCase↔snake_case)한다 — server 쪽 스펙이 client 쪽 문서를 계약 출처로 가리키는 소유 구조 그대로 구현도 따름. | `framework/include/zlink/framework/contracts/streams/stream.hpp:89-97`; `framework/doc/framework/common/spec/stream-connector/32-stream-connector.ko.md` §6.3 |
| G5 | 해당 없음(순수 문서 소유권 문제 — 33/46/session 세 문서 중 어디가 host permit 규칙을 소유하는지는 구현 관찰로 판정 불가) | — |
| G6 | cpp는 두 startup 검증을 서로 다른 메커니즘으로 구현함: "같은 session type을 중복 등록"은 **전역** registry(`stream_session_names` set, 모든 node에 걸쳐 공유)로 검사하므로 서로 다른 node에 같은 session type을 등록해도 걸림. "한 node에 session을 둘 이상 등록"은 **node별** `_session_configured` flag로 검사. 두 규칙은 서로 겹치지 않는(redundant하지 않은) 별개 조건이다 — 전자가 후자에 포함되는 관계가 아니다. | `framework_options.hpp:1284-1292`(node별 `_session_configured` 가드), `:1289-1292`(전역 `stream_session_names` insert 가드) |
| G7 | abort 처리에서 seal 해제가 held message 재제출보다 **먼저** 일어남: `_barriers.erase(*barrier_token)`(seal 해제) → `barrier_token.reset()` → `_changed.notify_all()` → `lock.unlock()`. 대기 중인 `admit_inbound`의 `_changed.wait_until`은 이 unlock 이후에만 lock을 재획득해 재제출하므로, seal 해제와 wakeup이 재제출보다 앞선다 — 새 문서(20·48)가 기록한 순서와 일치. | `framework/src/runtime/stateful/stream_session_registry.cpp:916-969`(특히 `:955,956,960,961`); wait/resume 지점 `:440-457` |
| G8 | 코드/주석 어디에도 "source coordinator"·"relocation coordinator" 표현이 없음. wire 필드명은 `coordinator`(타입 `relocation_coordinator_fence_t`), 로컬 변수명은 `relocation_coordinator`이며 source node 자신의 identity(source_node_rid, source_node_generation, source_authority)로 채워짐. | `framework/src/runtime/protocol/service_wire_codec.hpp:531-545,564-579`; `framework/src/runtime/mesh/mesh_node_runtime.cpp:2938-2942` |
| TEST | `tests/Zlink.Framework.ContractTests/test_cpp_framework_layout_contract.cpp:393-419`의 `session_actor_dispatch_documents_disconnect_destroy_boundary`는 여전히 옛 경로 `doc/framework/common/spec/server/04-session/02-session-actor-binding.ko.md`를 열어 문장 3개("binding을 해제하지만 Actor를 destroy하거나 Spot membership을 바꾸지 않는다.", "두 통지는 connection 종료 사실만 알리며", "Actor를 destroy하거나 Spot membership을 변경하지 않는다.")를 needle로 검색한다. 3개 needle 모두 새 문서 `framework/doc/framework/common/spec/server/session/session-actor-binding.ko.md:198,323,323`에 글자 단위로 동일하게 존재함(직접 확인). 다만 테스트 자체는 옛 파일 경로를 계속 열고 있어, 20번 문서가 삭제되면 즉시 깨진다 — 경로 갱신 필요. | `tests/Zlink.Framework.ContractTests/test_cpp_framework_layout_contract.cpp:397-398,409-412`; `framework/doc/framework/common/spec/server/session/session-actor-binding.ko.md:198,323` |

## 요약

- 불일치 8건: R2, R7, R18, R21, R22, R48, R54, (R2/R7/R18/R21/R22/R48/R54는 판정 열에서 불일치로 표시; R60 계열은 판단 불가 별도 집계)
- 판단 불가 5건: R40, R51, R52, R60(100ms 지연 close 실측), R60(1,000ms cutover 실측)
- 스펙 미정 0건
- 그 외 R# 47건은 일치

Coding sub-agent for zlink Issue #259. Work ONLY in the worktree you were given (`-C <worktree>`),
commit ONLY to its branch. Do NOT push, do NOT open a PR, do NOT merge. The supervisor does that.

== Absolute prohibitions ==
- Do NOT edit any spec document. `framework/doc/**`, `core/doc/**`, `bindings/doc/**` and
  `doc/**` are off limits. The spec change for this work is ALREADY DONE and committed on main.
  If you believe the spec is wrong, stop and report it under BLOCKERS — do not edit it.
- Do NOT touch `scripts/local-package/**`, `core/tests/perf/hotpath_reference.json`, or any
  other language's directory. One job, one language.
- Do NOT run the full gate, benchmarks, or perf tickets. Run only the tests named below.
- Do NOT widen a timeout, add a retry, relax an assertion, or add a sleep to make a test pass.

== The product decision you are implementing ==
Overload is controlled by backpressure alone. Nothing rejects work because a queue is full.

- Core controls with its byte HWM. Framework controls with the number of jobs queued.
- One Framework host owns ONE job-permit limit, `MaxQueuedApplicationJobs`. However many
  MeshNodes, ClientServer channels, sockets or connections the host has, they share that one
  limit, and only one place in a host counts permits.
- At 80% of that limit the host applies `PAUSED` to every supported socket; at or below 60%
  it applies `RUNNING`. Supported sockets are the RouteMesh ROUTER-ROUTER socket and the
  ClientServer DEALER-ROUTER socket.
- If usage still reaches 100%, the host stops receiving. What it does not receive stays in
  the local Core queue, Core hits its byte limit, and the sender is held there.
- NOTHING IS DROPPED OR REJECTED. Everything received goes to a handler.
- Sending is a single call to the Core socket's send or request. The Framework builds no
  waiting queue of its own, does not stage backpressure, and does not retry. Waiting belongs
  to Core and the binding. If the wait runs out of time the call ends with `DeadlineExceeded`
  — send, publish, one-way and request alike.

== What that means for the code: three separate deletions ==

**(1) The completion-slot cap.** Sending a request reserved a completion slot and rejected
when the reservation failed. The cap is gone. So is the reservation itself: it existed only
to protect against a rejection the cap created, and with no cap there is nothing to protect
against. What stays is the REGISTRATION that §10 of the submit spec requires for a different
reason — the pending entry, reply route and dispatcher slot are registered before the
transport submit so a reply that arrives immediately is never processed before its
registration. That is an ordering rule, not a capacity rule. A runtime may still pre-allocate
the queue node to keep the terminal path allocation-free; that is a cost choice it owns.

**(2) Every queue limit other than the host job permit, and every "queue is full, so fail"
path that went with it.**

**There is exactly one limit left in the Framework: `MaxQueuedApplicationJobs`, the number of
jobs a host has waiting for a handler.** Bytes are Core's business — Core's byte HWM bounds
what a queue holds, and when this host stops reading, those bytes hold the sender back. The
Framework counts items, at one place per host, and nowhere else.

So these all go, along with the failures they produced:

| Limit | Where |
|---|---|
| completion slot 4,096 | operation registry and completion dispatcher |
| application lane 1,024 items / 64 MiB | per-execution-object FIFO |
| lifecycle lane 128 items / 4 MiB | per-execution-object FIFO |
| mailbox count and bytes | owner mailbox |
| worker scheduler queue bound | worker pool |
| any slot for holding a reply | wherever it is kept |

Delete the fields, the constructor parameters, the options that carry them, and every branch
that failed when they were reached. Leave no branch that can never be taken.

**These are limits too, but they stay — they are not queue capacity:**

| | Why it stays |
|---|---|
| Per-node Actor/Spot limit, per-stable-type limit | "How many Spots may live on this node", not a queue. Waiting does not create one — it ends with `Unavailable` |
| `MaxMessageSize` | The size of one message, not the length of a queue |
| Pending activation 128 | Activations in progress |
| Owner time budget 10 ms, lifecycle 8 consecutive turns | Fairness, not capacity — they stop one owner from holding the turn |

Separating the application and lifecycle lanes also stays. That separation orders and
prioritises work; it never bounded anything.

**Do not remove a public API, builder option, or configuration surface that is not one of the
deleted limits.** If a change seems to need one, stop and report it under BLOCKERS.

**(3) The `CapacityExceeded` error kind itself.** With no cap anywhere, nothing can produce
it. It is removed from the `ErrorKind` enum and the values after it shift down:

```
0 NotFound        1 AlreadyExists   2 TypeMismatch    3 NotConfigured
4 Rejected        5 Unavailable     6 DeadlineExceeded
7 ShuttingDown    8 ProtocolError   9 InvalidOperation
10 DataLost       11 InternalFailure
```

Twelve kinds, 0..11. Every existing producer moves:

| Old producer | New result |
|---|---|
| A queue, table or slot was full | Not an error. Wait; `DeadlineExceeded` if the time runs out |
| No node can host the Spot (placement) | `Unavailable` |
| HTTP client response body exceeded its configured size | `Rejected` |

== Explicitly OUT OF SCOPE — do not touch, and report it if you are tempted ==
- The per-node Actor limit, per-node Spot limit, per-stable-type Spot limit, and the
  `Pending activation` default of 128. These stay. Only the NAME of the error they produce
  changes to `Unavailable`.
- The topology-reason enum. It has its own `capacity_exceeded` value which is unrelated to
  `ErrorKind` and stays exactly as it is. Check which enum you are looking at before editing.
- The wire protocol and runtime metrics.
- `MaxQueuedApplicationJobs`, the 80/60 percentages, and the profile computation.

== What the spec now says (already committed — read it, do not change it) ==
- `.../01-execution/01-submit-and-completion.ko.md` §11 — putting a completion callback on
  the dispatcher cannot fail; nothing rejects a request for lack of a slot. §10 still
  requires registration before the transport submit. §3 — a full worker queue waits.
- `.../01-execution/04-application-job-queue-and-backpressure.ko.md` §1 §2 §3 §6 §8 — the two
  capacity authorities, one limit per host with one counter, which ingress consumes a permit,
  the no-drop rule, and "the Framework calls the Core socket's send or request once".
- `.../00-foundation/07-framework-error-model.ko.md` §2 §5 — the 12 kinds, and "a full queue
  is not an error".
- `doc/plan/issue-259-worklog/gap-survey.ko.md` — the per-language survey of the caps.

== Repository rules you must follow ==
- `AGENTS.md` §3 — fix the cause in the module that owns it. Never add a rule: after your
  change the design must have FEWER rules. No new state, timer, index, helper layer, option,
  or "only in this case" branch. Leave no branch that can never be taken — if a boolean can
  no longer be false, delete the boolean.
- `AGENTS.md` §3 — cross-language check. The other three languages are being changed in
  parallel to the same decision; if your language needs something the others do not, say why.
- `doc/principal/source-comment-principles.ko.md` — comments say WHY, never restate the code.
- One job, one cause. If you cannot finish inside your time budget, write down the facts you
  proved and what remains, and stop.

== Build environment (already prepared — just export these) ==
```bash
export JAVA_HOME=/usr/lib/jvm/jdk-25.0.4.1+1
export PATH="$JAVA_HOME/bin:$PATH"
export ZLINK_LOCAL_PACKAGE_ROOT=/home/hep7/project/zlink/.artifacts/wsl
cd framework/languages/java && ./gradlew --offline :zlink-framework-core:test
```
JDK 25 is required and Gradle toolchain auto-download is not configured, so `JAVA_HOME` must
point at the Temurin 25 above. Local Maven artifacts are under
`$ZLINK_LOCAL_PACKAGE_ROOT/maven`. Use `--offline` first; drop it only if a dependency is
genuinely missing. Run the `zlink-framework-core` unit tests plus your new test. No e2e,
samples, or benchmarks.

== Report format (write it to the summary path given below, in Korean) ==
  1) 결과 — 한 줄
  2) 변경 파일 — 경로마다 무엇을 왜
  3) 소유 계층 / spec 조항 / 교차언어 대조 / 변경 분류(A 계약 적응·B 기존 결함·C 우회·D spec gap)
     — 네 줄. 이 네 줄이 없으면 감독자가 커밋하지 않는다.
  4) 규칙 수 — 수정 전 N개 → 수정 후 M개, 무엇이 사라졌는지
  5) 실행한 test와 결과, 남은 실패
  6) BLOCKERS — 스펙이 틀렸다고 판단한 것, 다른 언어에 넘길 것

== YOUR LANGUAGE: Java (and the Kotlin surface) ==
Summary path: `.artifacts/codex/259-java/summary.md`
Java is the only language whose ClientServer path shares the same registry as RouteMesh, so it
shows most clearly what the cap covered. 35 files mention the kind.

Root: `framework/languages/java/zlink-framework-core/src/main/java/systems/zlink/framework`

**(1) The completion-slot cap — two limits, delete both.**
   `runtime/internal/service/ZLinkServiceOperationRegistry.java:341` —
     `entries.size() >= maxPendingOperations`. Default `:23`
     `DEFAULT_MAX_PENDING_OPERATIONS = 4_096`; field `:27`; constructors `:37-98` thread it
     through with validation at `:94-97`. Delete the limit, the field, the constructor
     overloads that exist only to carry it, and the constant. Keep the closed check `:338` and
     the duplicate/zero id checks `:350-354` — those are correctness.
   `runtime/internal/service/ZLinkServiceCompletionDispatcher.java:11` `CAPACITY = 4_096`,
     `:36` `reserved >= CAPACITY` inside `tryReserve`, caller
     `ZLinkServiceOperationRegistry.java:356`. `tryReserve` can no longer fail: delete the cap,
     the false return, the `reserved` counter, and the `item.reserved` flag if it only guarded
     the cap; make the method a `void` named for what it now does. Keep
     `releaseWithoutDispatch`, `requireReserved` and the shutdown drain working — check what
     each reads before deleting a field.
   `capacityExceeded()` at `:298-302` then has no caller. Delete it.

**(2) Every "queue is full, so fail" path.**
   `errors/ZLinkWorkerQueueFullException.java` — a full worker queue must wait for room, not
     throw. Find its throw sites and make them wait; if the class then has no producer, delete
     it and follow every catch.
   The per-execution-object FIFO — find the site that rejects when the lane is full and make it
     wait. While waiting the record keeps its host permit.

**(3) The enum.**
   `errors/ZLinkFrameworkErrorKind` — delete `CAPACITY_EXCEEDED` and renumber the values after
   it down by one. Follow every switch and mapping, including
   `runtime/binding/ZLinkJavaRawMeshNode.java:2336` (maps it to `new int[] {106, 18}`) and
   `:4953` (maps it to `"backpressure"`, with the comment at `:4961` "Only an explicit capacity
   failure is backpressure"). After the kind is gone, decide whether each mapping is dead or
   still serves another resource, and say which in your report.
   `monitoring/ZLinkTopologyReason.java` has its own `CAPACITY_EXCEEDED` — that is the
   topology-reason enum. LEAVE IT.
   `ZLinkJavaRawMeshNode.java:142` `USER_SPOT_TERMINAL_CAPACITY = 4096`, used at `:7851` —
   decide whether that is a queue (→ wait) or User Spot placement (→ `Unavailable`) and say
   which.
   Placement sites move to `Unavailable`. Read each one; do not batch-rename.
   The Spring starter (`zlink-framework-spring-boot-starter/.../ZLinkRouteMeshRuntimeService.java`)
   and the Kotlin surface (`zlink-framework-kotlin`) project the same enum — follow them.

Tests that assert the old behaviour must be rewritten to the new contract, not deleted
silently. List each one you changed: `ZLinkServiceOperationRegistryTest`,
`ZLinkCompletionTerminalConformanceTest`, `ZLinkChannelSubmissionFailureTest`,
`ZLinkBackendRequestResultTest`, `KotlinCapacityMonitoringContractTest`, and the Spring test.

Regression test: start far more concurrent requests than 4,096 through BOTH the RouteMesh node
path and the ClientServer channel path, and assert every one completes with a reply.

Build and test (this worktree only): JDK 25 is required and toolchain auto-download is not
configured — use the Temurin 25 under `/usr/lib/jvm`. Run the `zlink-framework-core` unit tests
plus your new test. No e2e, samples, or benchmarks.

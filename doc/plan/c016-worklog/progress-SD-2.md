# core-rf-SD-2 진행 기록

- 2026-09-08 시작: branch `wip/sd-1`, HEAD `84d25131a6424031149ab7321e0678b65409bd75`, SD-1 미커밋 diff 6개와 사용자 소유 `SUPERVISOR-NOTE.md` 확인. 공통 규칙·SD-1 brief/report·감독관 D-B274/275/277 확인. 커밋 없이 B1 호출 귀속부터 진행한다.
- 2026-09-08 21:00 KST: 기존 SD-1 Release lib(`4e27d7d0…`)를 B1 후보(b) 보관본으로 저장했다. 코드상 `use_speculative_read_for()`가 TCP transport capability만으로 speculative drain을 항상 허용해 full read 직후 EAGAIN을 만드는 지점을 확인했다. 후보(a)는 TCP readiness 기반 async rearm, non-TCP만 기존 명시 opt-in+capability일 때 bounded drain하는 단일 policy로 수정했다. 다른 트랙 `ccu2` 측정과 `mac4` dev build가 실행 중이어서 Callgrind/strace/build는 자원 규칙에 따라 대기한다.
- 2026-09-08 21:10 KST: Release+LTO B2-only(`573375d1…`)와 최종 B1+B2(`00fe958e…`) lib 빌드·보관 완료, base는 SD-1 보관본(`1ed8fd9c…`)을 재사용한다. `SD-2.patch`, `SD-2-ablation-B2only.patch` 생성 및 reverse apply check 통과. 최종 dev build와 focused 5-test PASS. 관련 27-suite는 1·2회 27/27 PASS, 3회차 알려진 기존 간헐 `test_stream_socket_recv_multiclient_ready_regression` 1건 timeout 후 단독 3/3 PASS로 격리했다. 다른 `ccu2` review codex가 남아 있어 측정은 계속 대기한다.
- 2026-09-08 21:22 KST: `ccu2`의 Release build/CCU 4000 측정 작업이 계속 실행 중이고 load1=4.00이므로 idle 기준(load1<1.0)을 충족하지 못했다. 겹친 Callgrind와 dev-build 내부 hotpath 결과는 유효 결과에서 폐기했으며, `PERF_LOCK` 아래 Callgrind/strace 후보 비교와 세 빌드 interleave 묶음을 시작하지 않고 대기한다.
- 2026-09-08 21:30 KST: 최초 대기 후 40분 넘게 유효 idle 창을 확보하지 못했다. CCU 측정 종료 직후 MAC 트랙의 dev build와 반복 test가 시작됐고 review-CCU-5 `codex exec`도 실행 중이다. 지시대로 측정 항목을 생략하지 않고 60초 간격 대기를 계속한다.
- 2026-09-08 21:40 KST: MAC 트랙 TSan 실행 사이에 ninja=0을 확인하고, Callgrind에서 LTO inline 때문에 사라진 `speculative_read()` symbol을 보존할 후보(b) dev lib(`478bf58c…`)를 추가로 만들었다. 즉시 최종 후보(a) source와 dev lib(`0a78b18b…`)로 복구했고 diff check를 통과했다. review-CCU-5 `codex exec`가 남아 있어 성능 창은 아직 열지 않았다.
- 2026-09-08 21:50 KST: 최초 유효 창(load1=0.71, available≈10.7 GiB)에서 후보 비교를 실행했다. 단순 async rearm은 후보(b)와 똑같이 recvfrom≈2.015/msg·EAGAIN=1.000/msg라 반증됐다. 원인은 Boost.Asio async 시작 자체의 선행 nonblocking recv다. 새 상태 없이 full TCP STREAM read 뒤 transport `async_wait(wait_read)` 후 read하는 규칙으로 보완했고 focused 3-test PASS, Release+LTO 최종 lib `a773b865…`를 만들었다. build load가 가라앉은 뒤 strace를 재측정한다.
- 2026-09-08 22:00 KST: 성공 read 모두에 readiness wait를 적용하자 recvfrom 14,502/14,262=1.017/msg, EAGAIN 0을 달성했지만 readiness 뒤 별도 async callback을 둔 첫 구현은 latency가 악화됐다. readiness callback에서 owner TCP socket을 즉시 nonblocking read하고 spurious readiness만 기존 async로 fallback하도록 callback/state 하나를 제거했다. focused test PASS, 새 Release+LTO lib는 `90cef2e0…`; 동일 후보 교차 측정을 다시 수행한다.
- 2026-09-08 22:10 KST: 상태 없는 TCP-native readiness 구현의 후보 교차 3-run 중앙값은 p50 4.3446→4.3449 ms(+0.009%), p99 5.8640→6.6872 ms(+14.0%); 총 recvfrom은 약 2.015→1.017/msg, EAGAIN은 1.000→0이다. syscall 목표와 p50 동률을 우선해 최종 후보로 유지했다. 22:05:17 load1=0.35/available≈10.7 GiB에서 ablation interleave를 시작했고 base-r1·B2-r1 완료, final-r1 진행 중이며 다른 worktree process와 mismatch는 없다.
- 2026-09-08 22:20 KST: ablation interleave의 run 1·2 cycle(base→B2-only→final)을 모두 완료했고 마지막 base-r3를 시작했다. 한 lock/창을 유지 중이며 다른 worktree process 0, 현재까지 mismatch 0이다. 완료 즉시 세 대조 stack의 인접 묶음 변동률을 계산해 15% 기준을 판정한다.
- 2026-09-08 22:30 KST: ablation 9묶음 완료(22:05:17~22:26:03), 인접 대조군 최대 변동 7.466%로 유효. 64 KiB zlink 중앙값은 32.287/40.323/39.615 kops, RSS 279,192/423,340/425,160 KiB(base/B2/final). WS base-r1 12셀은 완료됐으나 gate FAIL(ws 0.3296, wss 0.7590)을 shell이 조기 종료로 처리했다. 유효 결과는 보존하고 runner가 FAIL을 기록한 뒤 계속하도록 수정했으며 load1<1 재진입을 기다린다.
- 2026-09-08 22:40 KST: WS base/final 각 3-run 완료. 중앙값은 base ws=0.5466 FAIL/wss=1.1573 PASS, final ws=0.5272 FAIL/wss=1.0184 PASS다. B1/B2는 RAW STREAM에만 적용돼 이 ZMP gate를 바꾸지 않았고 base도 같은 ws 병목을 재현한다. ZMP-over-WS 64 KiB DR에서 decoder/encoder와 WebSocket frame 경계의 2단계 read/copy 여부를 코드·진단 실행으로 좁혀 D-B277에 따라 미달을 남기지 않는다.
- 2026-09-08 22:50 KST: 기존 gather 진단은 WS/WSS 64 KiB DR을 30.7/9.9 kops로 회복시켰으나 TCP까지 gather해 TCP DR을 11.1 kops로 낮췄다. 원인을 ZMP header/body의 WebSocket frame 분리로 확정하고, env 변경 없이 `protocol header + gather capability + message boundary`일 때 한 transport frame으로 보내는 규칙을 추가했다(RAW STREAM/TCP ZMP 불변). focused 4-test PASS, 최종 Release lib `b55f95d1…`; 22:47 load1=0.68에서 final WS 3-run 재측정 중이다.
- 2026-09-08 23:00 KST: 경계-aware gather 적용 후 WS gate 최종 3-run은 전부 PASS했고 중앙값 ws=1.8454, wss=1.4156이다. 최종 dev build, 관련 stream/asio/decoder/ws 27-suite 3회(각 27/27), 전체 ctest 211/211도 통과했다. 이제 TSan 27-suite와 idle hotpath 5셀을 수행한다.
- 2026-09-08 23:10 KST: 최종 TSan build와 관련 27-suite 27/27를 통과했고 TSan 보고는 0건이다. Release+LTO hotpath target을 최종 source로 다시 link한 뒤 build 부하가 내려가기를 기다린다.
- 2026-09-08 23:20 KST: idle hotpath 최초 실행은 4/5 PASS, 기존 dealer-router reference만 유리한 5.15% 명령 감소로 FAIL했다. 이번 조건 경로와 무관함을 확인하고 idle 3회 중앙값으로 해당 reference만 갱신했으며 최종 5/5 PASS했다. 누적 patch와 B2-only patch를 생성·검사하고 최종 보고서를 작성했다.

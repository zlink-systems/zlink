# R2 인벤토리 — `core/src/runtime/engine/asio/` + `core/src/runtime/protocol/`

읽기 전용 조사. HEAD `2529709db6`. 빌드·측정 없음.

## 표

| # | 분류 | file:line | 관찰 | 제안 | 반경 | 계약 | 성능 |
|---|---|---|---|---|---|---|---|
| 1 | 3 (얕은 모듈) + 2 (중복) | `asio_engine.cpp:1547-1826` `restart_input_internal()` | 279행 단일 함수. `use_stream_rx_slab()` 분기로 STREAM slab 큐(`pending_stream_rx_chunks`)와 일반 `pending_buffers` deque를 처리하는 두 while 루프가 거의 동일한 로직(decode→process→backpressure 되감기→풀 반환)을 복사-수정 형태로 반복. 그 위에 "현재 버퍼 처리", "flush 뒤 race 재확인", "STREAM speculative_read 여부"까지 한 함수에 들어 있음. | 공용 청크 추상화(오프셋+길이를 갖는 뷰) 하나로 두 루프를 합치거나, 최소한 "청크 하나를 디코더에 흘려보내고 backpressure/부분소비를 처리"하는 헬퍼로 추출해 두 곳에서 호출. 함수를 250행 이하로 쪼갬. | 1파일(`asio_engine.cpp`), ~280행 재작성 — 이번 캠페인 중 가장 큰 반경 | 없음(관측 동작 불변 목표), 다만 리팩터 자체가 위험 — 계약 테스트로 회귀 확인 필수 | 없음/위험(리팩터 실수 시 STREAM 회귀) |
| 2 | 1 (죽은 코드: 중복 검사) | `asio_engine.cpp:1099-1109` (`on_write_complete`) | 에러 분기(1101행 근처)에서 `finish_gather_output()`를 무조건 호출하는데 그 함수 자체 첫 줄(830행)이 `if (!_pipeline.async_gather) return;`로 이미 가드함. 정상 분기(1108-1109)는 `if (_pipeline.async_gather) finish_gather_output();`로 **같은 판정을 호출부에서 다시** 함 — S-4 보고서가 이미 지목(항목 1). | 1109행의 `if (_pipeline.async_gather)` 가드 제거, 무조건 호출로 통일(함수 내부 가드가 이미 정답). | 1파일, 1줄 | 없음 | 없음 |
| 3 | 1 (죽은 코드: 항상 같은 값 분기) | `asio_engine.cpp:754-825` `prepare_gather_output()` | 이 함수는 `_connection_fastpath_policy.gather_write_enabled()`가 참일 때만 호출되고(683, 1350행), 그 판정은 `protocol_builds_gather_header_`가 참(=ZMP 엔진, S-4)일 때만 참이 될 수 있음. 즉 이 함수 안에서 `_options.type == ZLINK_CORE_SOCKET_STREAM`은 **항상 거짓**. 그 결과 `stream_mode`(756행), `tiny_stream_gather`(780-781행), `threshold`의 STREAM 분기(779행), 그리고 811행 근처 `use_stream_handler_alloc`(STREAM 전용 handler-alloc 분기, 805행)가 전부 죽은 경로. S-4 자체 보고서 §7#4가 이미 지목. | STREAM 전용 분기 제거, `asio_gather_threshold`만 남김. `asio_stream_gather_threshold`/`asio_stream_tiny_gather_threshold`는 `ZLINK_ASIO_STREAM_GATHER_THRESHOLD`/`ZLINK_ASIO_STREAM_TINY_GATHER_THRESHOLD` env로 문서화돼 있어(08-stream.ko.md) 제거 시 스펙 문장 변경 필요 — **확인 필요**: env를 그대로 파싱만 하고 버리게 둘지, 문서에서 "no-op" 표시할지, env 자체를 없앨지 감독관 판단. | 2파일(`asio_engine.cpp`, `asio_stream_fastpath_policy.hpp`) + 스펙 문서 1곳 | 있음→D (env 문서 문장) | 없음(죽은 경로 삭제, 실측 불필요) |
| 4 | 1 (죽은 코드: 항상 같은 값 분기) | `asio_engine.cpp:803-825` (`prepare_gather_output` 안 async_writev 호출부) | 항목 3과 같은 이유로 `use_stream_handler_alloc`이 이 지점에서 항상 거짓 → `if (use_stream_handler_alloc) {...}` 블록(806-816행)은 도달 불가, else 블록(817-824)만 항상 실행. | if 블록 삭제, else 블록만 남김(또는 항목 3과 함께 정리). | 항목 3과 동일 파일·구간(같은 apply job으로 묶을 것) | 없음 | 없음 |
| 5 | 3 (얕은 모듈: 파일 하나에 여러 개념) | `asio_engine_pipeline.hpp:22-83` `asio_engine_pipeline_t` | 한 struct에 (a) 모든 엔진 공용 read/write·pending-buffer 상태, (b) gather 전용 필드 5개(`async_gather`, `gather_header[64]`, `gather_header_size`, `gather_body`, `gather_body_size`, 연결당 88B) — ZMP만 씀(S-4 §7#2), (c) STREAM 전용 성장 타깃 필드 7개(`stream_decoder_read_target_*`, `stream_encoder_*`)와 STREAM rx-slab 큐 2개가 섞여 있음. 모든 연결이 세 그룹 전부를 생성자에서 초기화. | (b)를 ZMP 엔진 쪽 멤버로, (c)를 raw/STREAM 엔진 쪽 멤버로 옮기고 pipeline 구조체는 공용 상태만 남김. | 4-5파일(`asio_engine_pipeline.hpp`, `asio_engine.hpp/.cpp`의 `_pipeline.gather_*`·`_pipeline.stream_*` 참조 전부, `asio_zmp_engine.*`) — 기계적이지만 넓게 퍼짐 | 없음(내부 레이아웃) | 이득(연결당 바이트 감소, 미실측) — 이번 4묶음에는 넣지 않음(반경 큼) |
| 6 | 1 (죽은 코드: 호출자 없음) | `asio_error_handler.hpp:1-103` (`zlink::asio_error::classify/is_terminal/should_ignore`, `error_info_t`, `severity`, `category`) | `grep -rn "asio_error::"` 전체 트리에서 정의 파일 자체 외 참조 0. `ssl_context_helper.cpp`·`ssl_transport.cpp`가 헤더를 `#include`하지만 어느 심볼도 호출하지 않음(확인: 두 파일에 `classify`/`severity::`/`category::` 문자열 0개). | 파일 삭제 + 두 include 제거. 의도된 스캐폴딩일 가능성 낮음(호출부가 전혀 없고 최근 커밋 이력에 연결 흔적 없음) — 삭제 전 담당자 확인 권장. | 3파일(`asio_error_handler.hpp` 삭제, 2개 include 제거), 공개 헤더 아님 | 없음 | 없음(실행된 적 없는 코드) |
| 7 | 1 (죽은 코드: 선택되지 않는 템플릿 기본값) | `decoder_allocators.hpp:18-43` `c_single_allocator` | `decoder_base_t<T, A = c_single_allocator>`의 기본 인자. 저장소 전체에서 `decoder_base_t<...>` 인스턴스화는 `zmp_decoder.hpp/.cpp`의 `decoder_base_t<zmp_decoder_t, shared_message_memory_allocator>` 단 하나뿐(raw_decoder는 decoder_base_t를 안 씀, 자체 클래스) — 기본값이 선택된 적이 없음. | `c_single_allocator` 삭제, `decoder_base_t`의 `A` 기본 인자 제거(또는 유일 사용처에 맞춰 비템플릿화는 별도 판단). | 2파일(`decoder_allocators.hpp`, `decoder.hpp`) | 없음(비공개 구현) | 없음 |
| 8 | 4 (잘못된 소유) | `asio_engine.hpp:262-265` `_stream_read/write/handshake_handler_allocator` | 주석대로 STREAM 전용(게이트: `_options.type==STREAM && asio_stream_enable_handler_alloc`)이지만 멤버는 공용 베이스 `asio_engine_t`에 있어 **모든 ZMP 연결도 3×1KiB(3KB) inline storage를 할당**(안 쓰지만 생성자에서 만듦). raw 엔진(`asio_raw_engine_t`, ZLINK_FINAL, STREAM의 유일한 엔진)으로 옮길 수 있음. | 세 멤버를 `asio_raw_engine_t`로 이동, 베이스는 protected 접근자만 유지하거나 raw 엔진이 오버라이드하는 방식으로 변경. | 4파일(`asio_engine.hpp/.cpp`, `asio_raw_engine.hpp/.cpp`) | 없음 | 이득(ZMP 연결당 3KB) — 확인 필요: 05-connection-memory 예산에 이 항목이 이미 반영돼 있는지, 반경 대비 이득(3KB) 이 리팩터를 정당화하는지 감독관 판단 |
| 9 | 1 (기존 결함, S-3 이미 설계·검증) | `handler_allocator.hpp:33-66` `handler_allocator` | 1024B 인라인 슬롯 1개 + `_in_use` bool 하나. `start_async_read()`가 read 완료 핸들러 **안에서** 다음 read를 재무장하므로(asio가 op 메모리를 핸들러 반환 후에 해제) 재무장 시점엔 항상 `_in_use==true` → 매번 `::operator new`로 폴백. 이 클래스의 존재 이유(헤더 주석: "10-15% 개선")가 read 경로에서 100% 무산됨. S-3(`core-rf-S-3-summary.md`)가 원인을 정확히 진단하고 2-슬롯 배열 안을 uncommitted worktree(`~/project/zlink-work/s3`)에서 검증(malloc/free 쌍 제거, ZMP·raw 공통 경로)했으나 커밋되지 않음, 성능 측정(CCU 1000)·ASan 1회가 남아 있다고 명시. | S-3의 이미 리뷰된 패치를 이번 캠페인 apply job으로 반영(2-슬롯 배열 + 선형 스캔) + 남은 성능/ASan 측정. | 1파일(`handler_allocator.hpp`), ~15줄 | 없음 | 이득(S-3 실측: 메시지당 malloc+free 1쌍 제거, decoder 측 malloc의 ~82%) — 이 job에서 재측정 필요 |
| 10 | 2 (중복, 경미) | `asio_zmp_engine.cpp:49-99` `asio_zmp_engine_t` 생성자 3개 | 15개 필드 멤버 초기화 목록이 세 생성자(non-SSL, transport 인자, SSL)에 걸쳐 거의 그대로 반복(베이스 `asio_engine_t(...)` 인자와 SSL 컨텍스트만 다름). | 위임 생성자(delegating constructor)로 통합. | 1파일, ~30줄 감소 | 없음 | 없음 — 우선순위 낮음, 이번 4묶음 밖 |

`decoder_allocators.cpp`의 `max_messages_`/spare-slot 기계장치(`_max_counters`, `take_exact_spare`, `recycle_or_destroy_buffer`)는 여전히 살아있는 경로다: raw_decoder는 `max_messages_=1`을 항상 명시 전달하고, zmp_decoder는 버퍼 크기 기반 카운터 수를 쓴다(1-인자 생성자 경유) — 두 경로 모두 실제로 도달하며 죽은 코드가 아니다.

## 적용 job 묶음 제안 (파일 겹침 없음, 각 ≤1.5h)

1. **`engine-dead-branches`** — 항목 2, 3, 4 (모두 `asio_engine.cpp`의 gather-write 구간, 680-830행). 순수 삭제 위주, 계약 테스트(`stream|engine|asio|raw|zmp` ctest)로 회귀만 확인. 항목 3의 env 문서 변경 여부만 먼저 감독관에게 확인.
2. **`engine-restart-input-split`** — 항목 1 단독. `asio_engine.cpp`의 다른 구간(1547-1826행)이지만 같은 파일이므로 묶음 1과 **동시 진행 금지**(순차 진행, rebase 순서 필요). 반경이 크므로 별도 job으로 분리하고 시간을 온전히 확보.
3. **`protocol-decoder-dead-code`** — 항목 6, 7. `decoder_allocators.hpp/.cpp`(변경 없음, `.hpp`만), `decoder.hpp`, `asio_error_handler.hpp`(삭제), `ssl_context_helper.cpp`/`ssl_transport.cpp`(include 제거) — engine 파일과 겹치지 않아 묶음 1·2와 병렬 가능. 항목 6은 삭제 전 "정말 죽었는지" 재확인(라이브러리 export/테스트 헬퍼 경유 호출 없는지 `nm`/링크 심볼로 재검증) 권장.
4. **`handler-allocator-2slot`** — 항목 9. `handler_allocator.hpp` 단독, 다른 묶음과 겹치지 않음. S-3 설계를 그대로 적용 + 누락된 CCU 1000 성능 측정·ASan 1회를 이 job에서 완료.

항목 5, 8, 10은 반경이 크거나(5, 8: 여러 파일에 걸친 구조 변경) 우선순위가 낮아(10) 이번 4묶음에서 제외 — 후속 job 후보로 남김.

보고 경로: `doc/plan/c016-worklog/core-rf-R2-inventory.md`

# Core 리팩토링 스펙 정렬 inventory

조사 기준: `git log 285f37792d..HEAD -- core/src core/tests core/CMakeLists.txt`와 사용자가 지정한 변경 근거 커밋. 대상 문서의 공개 API 동작·errno·이벤트 순서 계약은 제외하고, 내부 구조·파일·메커니즘 서술만 대조했다. G-11a worktree(`~/project/zlink-work/g11a`)도 확인했으나 PAIR activation의 fence 예외를 서술한 대상 문장은 발견하지 못했다.

| 문서 경로:행 | 현재 문장(원문 인용) | 왜 stale인가(커밋·파일) | 제안 문장 | 계약 문장인가(예/아니오 — '예'면 수정 제안 없이 표시만) |
|---|---|---|---|---|
| `core/doc/spec/core/socket/08-stream.ko.md:315` | “gather된 두 buffer를 단일 binary frame(`async_write` 한 번)으로 보낸다.” | S-4 `597f134d68`: STREAM raw protocol은 gather header를 만들지 않으며, gather 가능 여부는 연결별 `connection_fastpath_policy_t`가 결정한다(`core/src/runtime/engine/asio/asio_stream_fastpath_policy.hpp`). STREAM의 WS/WSS transport capability만으로 STREAM gather-write가 성립한다고 서술할 수 없다. | “STREAM raw write는 payload buffer를 WebSocket write 경로에 전달한다. gather write 여부는 transport capability가 아니라 protocol header 생성 가능 여부와 연결별 fast-path policy의 결합으로 결정된다.” | 아니오 |
| `core/doc/spec/core/socket/08-stream.en.md:330-331` | “A WS write sends one supplied buffer or two gathered buffers as a single binary frame (one `async_write`).” | S-4 `597f134d68`, `core/src/runtime/engine/asio/asio_stream_fastpath_policy.hpp`: STREAM raw engine에는 gather header가 없고 연결별 policy가 gather를 비활성화한다. | “A STREAM raw write passes the payload buffer to the WebSocket write path. Gather use is selected by the per-connection fast-path policy, including whether the protocol can build a gather header.” | 아니오 |
| `core/doc/spec/core/socket/08-stream.ko.md:333-334` | “Gather write는 WS/WSS에서 지원한다. `supports_gather_write()`는 `true`이고, `async_writev()`가 두 buffer를 하나의 `async_write`로 묶는다.” | S-4 `597f134d68`, `core/src/runtime/engine/asio/asio_engine.cpp`: transport의 `supports_gather_write()`는 capability 입력일 뿐이며 STREAM raw protocol은 gather header를 만들지 않는다. | “WS/WSS transport는 gather capability를 제공할 수 있지만, STREAM raw 연결에서는 protocol 조건을 포함한 연결별 policy가 gather 사용을 결정한다.” | 아니오 |
| `core/doc/spec/core/socket/08-stream.en.md:351-352` | “Gather write is supported for WS/WSS. `supports_gather_write()` returns `true`, and `async_writev()` combines two buffers into one `async_write`.” | S-4 `597f134d68`, `core/src/runtime/engine/asio/asio_stream_fastpath_policy.hpp` 및 `asio_engine.cpp`: STREAM gather capability를 transport의 `true`만으로 설명하는 것은 현재 mechanism과 불일치한다. | “WS/WSS transports may advertise gather capability, but STREAM uses gather only when the per-connection policy also permits it, including protocol header support.” | 아니오 |
| `core/doc/spec/core/socket/08-stream.ko.md:402-404` | “`ZLINK_ASIO_STREAM_DISABLE_GATHER`: 기본 비활성이라 STREAM gather-write는 유지됨” / “`ZLINK_ASIO_STREAM_GATHER_THRESHOLD`: 기본 `1024`” / “`ZLINK_ASIO_STREAM_TINY_GATHER_THRESHOLD`: 기본 `0`” | S-4 `597f134d68`, `core/src/runtime/engine/asio/asio_stream_fastpath_policy.hpp:154-162`: D-f로 이 세 환경변수는 더 이상 유효한 STREAM 정책 입력이 아니다. | 해당 세 환경변수 항목을 삭제하고, “STREAM gather 사용은 연결별 fast-path policy가 protocol·transport capability를 함께 평가해 결정한다”로 대체한다. | 아니오 |
| `core/doc/spec/core/socket/08-stream.en.md:428-431` | “`ZLINK_ASIO_STREAM_DISABLE_GATHER`: disabled by default, so STREAM gather writes remain enabled” / “`ZLINK_ASIO_STREAM_GATHER_THRESHOLD`: default `1024`” / “`ZLINK_ASIO_STREAM_TINY_GATHER_THRESHOLD`: default `0`” | S-4 `597f134d68`, `core/src/runtime/engine/asio/asio_stream_fastpath_policy.hpp:154-162`: D-f 이후 무효화된 STREAM gather 환경변수와 옛 threshold를 나열한다. | Remove the three variables and state: “STREAM gather use is selected by the per-connection fast-path policy from protocol and transport capabilities.” | 아니오 |
| `bindings/c/bench/with_stream/README.md:106` | “`--build-dir PATH            default: core/build`” | 러너 `run_benchmarks.sh:1-6, 897-898`의 기본 build directory는 `bindings/c/build`이고, `bindings/tools/local_core_runtime.sh:7-29`에서 Core source 기본값은 `release`이며 release를 다운로드한다. | “`--build-dir PATH`: default `bindings/c/build` (the runner uses the verified release Core by default; set `ZLINK_CORE_SOURCE=local` to use the workspace Core).” | 아니오 |
| `bindings/c/bench/with_stream/README.ko.md:107` | “`--build-dir PATH            기본값: core/build`” | `run_benchmarks.sh:1-6, 897-898`의 실제 기본 build directory는 `bindings/c/build`이며, `local_core_runtime.sh:7-29`의 Core source 기본값은 `release` 다운로드다. | “`--build-dir PATH`: 기본값 `bindings/c/build` (러너는 기본적으로 검증된 release Core를 사용하며, workspace Core를 쓰려면 `ZLINK_CORE_SOURCE=local`을 지정한다).” | 아니오 |
| `CONTRIBUTING.md:32-33` | “Core test executables link the static archive, so enabling tests on the LTO tree re-optimises the whole library once per test executable after every Core change (hence tests OFF in `release`).” | `ddf9ff7e95`의 `core/CMakeLists.txt`와 `core/tests/CMakeLists.txt`: interface/contract/e2e는 shared `libzlink`, unit은 한 번 빌드한 non-LTO `test-core` archive를 사용한다. 모든 테스트가 static production archive에 링크한다는 서술은 완료된 구조 변경 전 상태다. | “Interface/contract/e2e test executables link the shared `libzlink`; unit executables link the single non-LTO `test-core` archive. The production release tree remains tests-OFF.” | 아니오 |
| `CONTRIBUTING.ko.md:32-33` | “Core 테스트 실행파일은 정적 아카이브에 링크하므로 LTO 트리에서 테스트를 켜면 … 테스트마다 전체 최적화를 다시 한다(그래서 `release`는 테스트 OFF).” | `ddf9ff7e95`의 `core/CMakeLists.txt`, `core/tests/CMakeLists.txt`: 공개 경계 테스트는 shared library, 내부 assertion은 단일 non-LTO `test-core` archive로 분리됐다. | “Integration·contract·e2e 실행파일은 shared `libzlink`에 링크하고, unit 실행파일은 한 번 빌드한 non-LTO `test-core` archive에 링크한다. 제품 release tree는 계속 테스트 OFF다.” | 아니오 |
| `CONTRIBUTING.md:76-78` | “Unit tests compile the sources of the component under test directly (no library link). A test that observes library behaviour is an integration test. (Today both kinds still link `libzlink-static`; the transition is in §9.)” | `ddf9ff7e95`에서 unit은 `test-core` archive를 사용하고, “두 부류 모두 `libzlink-static`” 및 transition 대기 문구가 완료됐다. | “Unit tests assert implementation-owned state through the single non-LTO `test-core` archive. Tests that observe library behaviour are integration tests, which link the shared `libzlink`.” | 아니오 |
| `CONTRIBUTING.ko.md:74-76` | “단위 테스트는 검사 대상 컴포넌트의 소스를 직접 컴파일한다(라이브러리 링크 없음). … (현재는 두 부류 모두 `libzlink-static`에 링크한다. 전환 계획은 §9.)” | `ddf9ff7e95`의 `core/tests/CMakeLists.txt`: unit은 소스를 실행파일마다 직접 컴파일하지 않고 공용 `test-core` archive를 사용하며, 공개 테스트는 shared `libzlink`에 링크한다. | “단위 테스트는 단일 non-LTO `test-core` archive를 통해 내부 상태를 검사한다. 라이브러리 동작을 보는 테스트는 shared `libzlink`에 링크하는 통합 테스트다.” | 아니오 |
| `CONTRIBUTING.md:163-167` | “Test link structure: integration tests will link the release `libzlink.so` dynamically … unit tests will compile their sources directly. … Until then, develop on the `scripts/build-core.sh dev` tree (§1).” | `ddf9ff7e95`로 명시된 전환이 완료됐다(`core/tests/README.md:19-22`, `core/CMakeLists.txt`의 `test-core`). §9의 “will”, 직접 compile, 내부 훅 재작성 대기 및 임시 dev-tree 지침은 stale이다. | “Test link structure: integration, contract, and e2e tests link the shared `libzlink`; unit tests use the single non-LTO `test-core` archive. The public-API boundary and test split are now in effect.” | 아니오 |
| `CONTRIBUTING.ko.md:152-155` | “테스트 링크 구조: 통합 테스트를 릴리스 `libzlink.so`에 동적 링크 … 단위 테스트는 소스 직접 컴파일로 전환. … 완료 전까지는 §1의 `scripts/build-core.sh dev` 트리로 개발 빌드를 한다.” | `ddf9ff7e95`로 전환 완료. `core/tests/README.md:19-22`와 `core/CMakeLists.txt`는 shared `libzlink` 대 non-LTO `test-core` 구조를 이미 명시한다. | “테스트 링크 구조: integration·contract·e2e는 shared `libzlink`에 링크하고, unit은 단일 non-LTO `test-core` archive를 사용한다. 공개 API 경계와 테스트 분리는 현재 적용 상태다.” | 아니오 |
| `scripts/build-core.sh:14-16` | “Until the test link structure is reworked (CONTRIBUTING.ko.md §9), that is the fast path.” | `ddf9ff7e95`로 test link structure rework가 완료됐다. 이 머리 주석은 더 이상 존재하는 부채를 전제로 한다. | “The dev tree is for correctness/iteration; the release tree is for the shipped library and perf measurement; release-gate is for final hotpath/LTO validation.” | 아니오 |

## 요약

- 대상 stale 문장 수: **15개**. 모두 내부 구조·파일·메커니즘 서술이며 공개 계약 문장은 **0개**다.
- 문서별 개수: `core/doc/spec/core/socket/08-stream.ko.md` 3, `core/doc/spec/core/socket/08-stream.en.md` 3, `bindings/c/bench/with_stream/README.md` 1, `bindings/c/bench/with_stream/README.ko.md` 1, `CONTRIBUTING.md` 3, `CONTRIBUTING.ko.md` 3, `scripts/build-core.sh` 1.
- `core/tests/README.md`, `bindings/c/perf/README.md`, `doc/perf/*.md`, `core/doc/guide/**`, 그리고 지정된 기타 Core spec 문서에서는 이번 캠페인 변경을 옛 내부 구조로 고정하는 문장을 추가로 찾지 못했다.
- 별도 확인: `core/doc/guide/04-transports.*`의 일반적인 gather-write 설명은 STREAM 내부 정책을 고정하지 않아 stale로 세지 않았다. G-11a의 PAIR activation fence 예외 및 삭제된 내부 파일명 서술도 발견되지 않았다.

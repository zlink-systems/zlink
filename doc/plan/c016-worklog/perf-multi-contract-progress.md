# perf multi completion contract progress

- 2026-09-04: detached worktree와 clean 상태를 확인했다. 구현 변경 범위는 `bindings/c/perf/**`로 제한하고, Core 계약·기존 one-way/REQREP 정책·runner/test 구조 조사를 시작했다.
- 2026-09-04: Socket 공통의 pull-completion/part-send pending 계약, DEALER 및 실제 트리에서 PUB 계약을 소유하는 `02-pub.ko.md`를 확인했다. `DONTWAIT FINAL`은 ID 0 즉시 admission 또는 nonzero ID+정확히 한 SEND completion이고, poll readiness 뒤 completion queue를 `NO_DATA`까지 drain해야 한다. PUB publish는 completion 대상이 아니다.
- 2026-09-04: 현재 DEALER/ROUTER one-way submit helper가 completion ID를 버리고 active 구간 latency를 함께 측정하며, DEALER_DEALER stop 및 PUBSUB stop publish가 transient 오류를 무기한 재시도하는 지점을 확인했다. Python runner의 server STOP wait는 기본 5초 뒤 terminate/kill로 이미 bounded하다.
- 2026-09-04: `bash scripts/build-core.sh release --lib-only`를 `JOBS=4`, `ulimit -v 16777216` 조건으로 완료했다. multi echo client는 `POLLIN|POLLCOMPLETION` 단일 poller, socket당 pending completion 1개 상한, phase 종료 시 completion/reply drain, 별도 1초 latency phase(socket당 reply in-flight 1) 구조로 변경했고 관련 client translation unit 빌드가 통과했다.
- 2026-09-04: DEALER/ROUTER relay reply도 동일한 DONTWAIT FINAL 계약 대상임을 확인해 nonzero SEND completion을 drain하고 한 개만 미완료로 유지하도록 정정했다. stop 전송 공통 정책은 SNDTIMEO 2초와 최대 5회로 추가했으며, runner는 실패한 DEALER_DEALER client 뒤 장시간 server RESULT 대기를 생략하도록 변경 중이다.
- 2026-09-04: DEALER_DEALER는 active 포화 구간 뒤 모든 client stop barrier를 통과하고, 별도 1초 global in-flight 1 latency 구간에서 wire ACK를 받도록 분리했다. DEALER server의 ACK도 completion을 drain하며, multipart FINAL 경계 전까지 수신한 뒤 pending ACK가 있으면 POLLIN을 중단한다.
- 2026-09-04: PUBSUB는 benchmark 기본 NODROP, 모든 SUB의 active stop barrier, `LATENCY_READY`/`LATENCY_ACK` 기반 global in-flight 1 latency 구간으로 분리했다. final STOP 직후 runner STOP과의 정상 종료 race, 1초 deadline 직전 ACK wait의 밀리초 내림 race, blocking stdin watcher join을 정정했다.
- 2026-09-04: relay의 immutable multipart retry snapshot, application pending 1개 상한, pending/completion 중 POLLIN gating, message close/free 및 terminal ENOENT 처리를 보완했다. 공통 part helper의 내부 empty FINAL도 MORE 전에 초기화하고 성공·실패 모두 close하도록 정정했다.
- 2026-09-04: `ci_multi_smoke.sh`와 README CI 예를 추가했다. CCU/DUR/SIZES/PATTERNS/TIMEOUT의 환경·인자 override와 pattern×size별 exact throughput RESULT 누락 진단을 구현했다.
- 2026-09-04: 전체 C perf target `cmake --build bindings/c/build -j4`, C++ metric test, Python perf tests 12개와 single policy tests 47개, shell/Python 구문 검사가 통과했다. 4KiB 개별 PUBSUB 재검증도 완료했다. 전체 4KiB CI 재실행 중 시스템 load가 150 이상으로 상승해 한 차례 PUBSUB deadline race를 발견·수정했고, 다음 재실행은 DD server latency barrier가 scheduling 지연으로 실패했다. 최종 기능 smoke는 load 완화 뒤 다시 실행한다.
- 2026-09-04: 최종 계약 감사에서 completion context 수명, exact completion ID 상관관계, multipart ROUTER borrowed RID 수명, relay 정상 종료 시 마지막 completion drain을 보강했다. DD client context는 socket 수명에 묶고 common/DD client·DD server·relay 모두 cap 1의 실제 ID를 저장·검증·clear한다. ROUTER RID는 첫 recv 직후 owned 값으로 복사하며, relay 종료 drain은 5초 상한이다. PUBSUB은 completion 비대상 계약에 따라 NODROP=1로 고정했다.
- 2026-09-04: 최종 C perf 전체 증분 빌드(`-j4`), C++ completion metric test, Python unittest 59개, shell/Python 구문 및 `git diff --check`가 모두 통과했다. 변경 경로는 `bindings/c/perf/**`뿐임을 확인했다.
- 2026-09-04: 최종 4KiB CI smoke는 8 clients·2초·TCP에서 대상 3패턴의 throughput 셀 3/3과 전체 RESULT 15/15를 만들고 완료했다. 64KiB도 동일 조건에서 16초, exit 0, throughput 셀 3/3과 RESULT 15/15로 완료했다. 따라서 요청에 예상된 현재-main 64KiB 정체/비0 빠른 실패는 이 clean Core 빌드에서 재현되지 않았다.
- 2026-09-04: 1024B 회귀 검증은 multi 전체 7패턴 RESULT 35/35, single 전체 7패턴 RESULT 35/35로 완료했다. 변경된 matched ROUTER baseline도 SENDSEND·REQREP 두 패턴 RESULT 10/10으로 완료했다. 성능 절대값은 비교·판정하지 않았다.
- 2026-09-04: CI failure path는 존재하지 않는 `MULTI_NOT_A_PATTERN` 셀로 검증했다. runner 실패를 exit 1로 전파하고 `pattern=MULTI_NOT_A_PATTERN,transport=tcp,size=4096,metric=throughput` 누락을 출력했다.

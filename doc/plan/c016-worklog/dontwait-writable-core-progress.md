- 2026-09-04 시작: `main` branch와 기존 33개 tracked 수정(요청 설명의 32개 Core/raw mirror 변경 + 별도 사용자 문서 변경), untracked 사용자 파일을 확인. 기존 A안 변경은 보존하고 확정 계약 B의 WRITABLE 토큰 모델로 전환한다.
- 2026-09-04 범위 보호: `doc/**`, `core/doc/**`, `framework/**`, `bindings/**`는 raw header mirror 8개 동기화 외 수정하지 않고, commit/push/reset/checkout/stash 및 `scripts/local-package/**` 실행을 하지 않는다.
- 2026-09-04 정찰 시작: 기존 A안 progress/brief와 현재 diff를 인계받고 runtime gap, public API 테스트 gap, framework/bindings 영향 목록을 독립 범위로 병렬 조사한다.
- 2026-09-04 공개 API 최소 변경 완료: `ZLINK_COMPLETION_WRITABLE = 3`을 기존 enum 뒤에 추가하고 Core 및 C/C++/Go/Rust raw `socket/api.h` mirror 5종을 동기화했다. completion 구조체 ABI는 유지했고 public header unit에 값 고정 assert를 추가했다. mirror cmp와 범위 diff-check가 통과했다.
- 2026-09-04 B 런타임 경계 확인: 토큰 등록은 intrusive FIFO publish 후 fence와 RID별 readiness 재확인이 필요하며, WRITABLE completion 잔존 동안 `POLLOUT` level을 별도 상태로 유지해야 한다. ROUTER aggregate/STREAM 상시 true `xhas_out`은 재확인에 사용할 수 없어 family-specific readiness가 필요하다.
- 2026-09-04 B completion queue 구현: 기존 socket-owned reservation pool의 고정 크기 node를 WRITABLE wait FIFO로 재사용하고, 등록을 ID 발급과 같은 mutex 경계에서 선형화했다. 대상별 FIFO publish, unread WRITABLE의 POLLOUT level count, recv/recycle 및 close terminal cleanup 단위 테스트를 추가했고 `unittest_socket_completion_queue`가 통과했다.
- 2026-09-04 첫 통합 빌드: DONTWAIT 실패 뒤 payload-free token 등록, register→fence→RID별 readiness 재확인, attach/write/weight/route-adoption wake, WRITABLE completion/POLLOUT level 경로를 연결한 상태로 `bash scripts/build-core.sh dev`가 성공했다. 공개 API·wake-invariant 통합 테스트 보강을 이어간다.
2026-09-04: REQUEST regression investigation confirmed all three failures were B-fixture errors, not runtime split regressions. Adjusted fixtures now make the first limited request genuinely transport-pending and start timeout only after admission; full test_phase3_request_reply_contract passed (20.89s, ulimit -v 16777216).
2026-09-04: Removed synchronous completion-owner startup from DONTWAIT wait-token issuance; poll/completion_recv remain the progress points, so token creation cannot wait for async-owner quiescence/start.
2026-09-04: Cross-audit fixed three wake boundaries: consume the current write-activation markers before WRITABLE readiness recheck, reject STREAM readiness after exact transport identity is cleared, and wake the retained promoted ROUTER standby after publishing its original RID outside the route lock.
2026-09-04: Updated legacy helper ownership expectation for a missing ROUTER target from NOT_CONNECTED to B BACKPRESSURED/EAGAIN with a nonzero token. Targeted helper ownership, route handover, phase3 completion, STREAM wake, and wake-invariant tests passed under the 16 GiB limit.
2026-09-04: Closed the pipe credit rearm ABA window: HWM/FLOW recovery markers are now consumed under the pipe output lock only when the recovered state is still active, so a refill/pause racing the socket callback keeps the newer marker armed. Dev build plus completion and wake-invariant tests passed.
2026-09-04: Made POLLCOMPLETION strictly queue-level by acknowledging the coalesced wake hint without treating it as readiness. Added a public poller assertion that the bit drops after the final WRITABLE dequeue; the existing REQUEST level test remains unchanged.
2026-09-04: Serialized connect-before-bind DEALER/ROUTER application attachment through the connector mailbox owner, ordered the two-lane completion pipe before the application command, made repeated PAIR attachment idempotent, and made PAIR wake sampling use the callback's lifetime-held pipe. This removes scheduler/route and PAIR pointer races without adding a SEND hot-path lock. Incremental build and phase3 completion contract passed.
2026-09-04: Extended socket-close cleanup so both still-waiting and already-ready WRITABLE tokens are terminalized before public delivery is dropped. The completion-queue unit now fixes the close race on both states while retaining the existing socket-lifetime reclamation model.
2026-09-04: Re-ran the full dev build after the close/TLS/CMake edits. Completion queue/header units, phase3 SEND contract, full REQUEST contract, and TLS targeted tests passed; phase3, TLS, and STREAM public contract executables then passed five consecutive runs each under the 16 GiB limit.

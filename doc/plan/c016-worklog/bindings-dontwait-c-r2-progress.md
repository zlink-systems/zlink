2026-09-04 C binding r2 시작: detached @70a9998998 확인. 기존 사용자 항목은 core/build, core/build-dev symlink뿐이며 보존. bindings/c/**와 지정 로그만 수정하고 core build는 실행하지 않음. API/문서, 테스트/gate, 확정 계약/선행 포팅을 병렬 정찰 중.
2026-09-04 정찰 결과: C public raw header 8개는 이미 Core와 byte-identical이고 WRITABLE=3, DONTWAIT wait-token, REQUEST-only PENDING_MAX 주석이 반영됨. 별도 async API는 없으며 구 SEND-completion 경로는 perf event-loop helper 5곳과 perf README에 존재. public HWM→WRITABLE→동일 payload 재전송 테스트와 helper 이관을 분리해 구현 중.
2026-09-04 failure boundary 보강: 존재하지 않는 ROUTER RID의 DONTWAIT send가 NOT_CONNECTED/EHOSTUNREACH, ID 0, completion 없음임을 public C API로 고정.
2026-09-04 public 계약 테스트 구현: HWM 충전→BACKPRESSURED/EAGAIN+nonzero token→즉시 completion 없음→peer drain→POLLOUT|POLLCOMPLETION→같은 token/context/RID의 WRITABLE→동일 payload 재생성/재전송 성공(ID 0)→SEND completion·중복 전달 없음. sleep 없이 작성했고 독립 빌드에서 5회 연속 통과, WRITABLE=3 surface 검사도 추가.
2026-09-04 표준 gate 1차 통과: `ulimit -v 16777216; ZLINK_CORE_SOURCE=local ZLINK_BUILD_JOBS=3 ./bindings/c/tests/run_tests.sh` 결과 contract 9/9, sample smoke 6/6 green. core/build는 링크 대상으로만 사용.
2026-09-04 최종 감사 보강: 라우팅된 wait token과 무관한 socket-wide POLLOUT 뒤 completion NO_DATA이면 POLLOUT을 억제하고 POLLCOMPLETION으로 정확한 WRITABLE을 기다리도록 client/stream event loop를 보강. DEALER server는 wait token 동안 POLLIN을 제거해 입력 backlog의 busy loop를 차단.
2026-09-04 no-route 경계 확대: 기본 ROUTER와 4-byte RID STREAM에 대해 NOT_CONNECTED/EHOSTUNREACH, ID 0, part 소비, completion 없음 검증을 추가. 별도 진단에서 Core release의 명시적 ROUTER MANDATORY=0은 확정 spec과 달리 silent-drop을 반환했으며, bindings/c 범위에서 수정할 수 없어 신규 toggle 검사는 제외하고 잔여 관찰로 기록.
2026-09-04 perf 이관 완료: ordinary SEND의 SEND completion accounting을 제거하고 client/relay/stream/dealer-dealer event loop를 exact payload/RID 보관→token/context/RID WRITABLE match→NO_DATA까지 pull→동일 bytes 재submit으로 변경. REQUEST 경로는 미변경. 영향 perf target 9개 빌드 green, perf_multi_metrics_test 5/5 green.
2026-09-04 최종 표준 gate green: `ulimit -v 16777216; ZLINK_CORE_SOURCE=local ZLINK_BUILD_JOBS=3 ./bindings/c/tests/run_tests.sh` 결과 contract 9/9, sample-smoke 6/6. 신규 dontwait backpressure 계약 실행 파일 별도 5/5 green, 변경 C 테스트 3개 strict `-Wall -Wextra -Werror` syntax green.
2026-09-04 최종 정적 gate green: `git diff --check`, core/include↔bindings/c/include 8-file `diff -qr`, public header tracked diff 없음, tracked 변경은 bindings/c/** 한정, runtime ZLINK_COMPLETION_SEND 참조 없음, 신규 sleep/thread/timer 없음. 독립 재감사에서 actionable correctness 이슈 없음.
EXIT:0

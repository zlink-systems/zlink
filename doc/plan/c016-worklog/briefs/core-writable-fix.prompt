역할: zlink Core 수정 담당(gpt-6-astra). 작업 트리: /home/hep7hep7/project/zlink-wt-core-writable (detached; **앞 job의 미커밋 테스트 `core/tests/integration/test_request_writable_credit.cpp` + CMake 등록이 있음 — 그 위에서 작업**, stash/checkout/reset 금지; 빌드는 기존 `core/build-writable`, `-j3`; core/build·core/build-dev symlink 사용 금지). 수정 범위 core/src/**, core/tests/**. **spec·framework·bindings·doc/plan 수정 금지.** git commit/push 금지. 진행 로그 /home/hep7hep7/project/zlink-work/c016/core-writable-fix-progress.md 한 줄씩 append(3분마다 한 줄 이상), 종료 시 `EXIT:<code>`. 요약 /home/hep7hep7/project/zlink-work/c016/core-writable-fix-summary.md (원인 `file:line`, 변경, 회귀 테스트 결과 분포, 게이트, hot path 영향, spec gap 여부, A용 한 줄).

결정(D-B119, spec 반영 커밋 2b359d6606·27b736c96f): `core/doc/spec/core/socket/README.ko.md` REQUEST DONTWAIT 절 — "거절 원인이 pair의 correlation work·count 한도 부족이면 그 토큰은 **해당 pair의 correlation reservation이 반환될 때(terminal reply·timeout·disconnect)만** WRITABLE을 발행하고, physical write credit만 회복된 상태에서는 발행하지 않는다(거절 원인이 되는 자원의 회복만 wake 조건 — 규칙 하나)"; `systems/06-auto-hwm.ko.md` §work budget 끝 문장 동일. 앞 job 요약 `/home/hep7hep7/project/zlink-work/c016/core-writable-summary.md`의 원인: `pipe.cpp:1699-1707`(request-full 원인을 일반 EAGAIN으로 변환), `lb.cpp:325-332`, `socket_request_reply_submit_api.cpp:240-247`(원인 구분 없이 SEND 공용 WRITABLE wait 등록), `socket_send_complete.cpp:89-94, 210-216`(등록 직후 `xhas_out()` 참이면 즉시 signal), 회복 경로 `pipe.cpp:1617 release_request_correlation()`·`:1634-1649` owner mailbox activation.

할 일:
1. 최소 변경으로 구현: 거절 원인(correlation-full)을 submit 경로까지 보존하고, 그 경우 등록하는 대기 토큰의 wake predicate/signal 경로를 "해당 pair의 correlation reservation 반환"으로 한다(기존 `release_request_correlation` → owner mailbox activation을 그대로 signal 원천으로 재사용; 새 timer/spin/poll 금지; SEND·physical-credit 토큰 경로는 변경하지 않음). 토큰 종료 조건(WRITABLE 1회, 명시 제거 TERMINAL+ENOENT, close TERMINAL)은 기존과 동일. DEALER candidate 집합에서 pair가 여러 개일 때 하나의 pair가 correlation-full이어도 다른 pair로 admission이 가능해야 하면 그 경우는 거절이 아니라 다른 pair 선택이어야 한다(`lb.cpp`) — 현재 동작을 확인하고 spec(§4 DEALER 분배)과 맞으면 유지.
2. `test_request_writable_credit`의 상한 assertion을 결정에 맞게 확정(64 KiB: reply 전 WRITABLE 0회 또는 reservation 반환 이벤트당 1회, reply 뒤 재제출 성공; 64 B/4 KiB 기존 동작 유지) → 5회 green. 기존 `test_request_writable_contract`, `test_backpressure_*`, `test_router_multiple_dealers`, single-lane suite 포함 `integration` -j2 green, 전체 `ctest -j2`(hotpath_gate 제외) green, `git diff --check`.
3. hot path(`socket_send_complete.cpp`, `socket_request_reply_submit_api.cpp`, `pipe.cpp` submit 경로)를 건드리므로 요약에 변경 위치를 명시(감독자가 Release+LTO hotpath_gate 실행).
메모리 11 GB, 다른 측정 동시(Go): `-j3`. 상한 75분.

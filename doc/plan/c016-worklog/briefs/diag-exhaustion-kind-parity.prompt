# Job: cross-language parity of the durable-operation exhaustion kind and replay scope — dotnet/java Actor Join, node STREAM actor bind, cpp replay scope (STAGE 1: diagnosis only, no code)

You are a codex sub-agent under a Claude supervisor. Read `AGENTS.md` (root) §3 (계층 소유권·금지 패턴·교차언어 대조) and `framework/AGENTS.md`. Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/framework/**`, `doc/site/**`, other languages; never lower an assertion or widen a deadline; do not commit. Local packages were rebuilt at 09:49 with Core `7ffb8e55d9` (D-086) and java `c9d294c44f`. Do not rebuild Core/local packages. STAGE 1 = NO CODE CHANGES; read-only. Other jobs edit the dotnet mesh node and node runtime files concurrently — read from `git show HEAD:<path>` where a file is dirty.

## Facts
- Spec: `framework/doc/framework/common/spec/server/03-spot-actor/04-actor-model.ko.md` sender replay bullets (after "Terminal record가 original deadline 뒤 5분…"): durable lifecycle operations replay with the same OperationId on any outcome without a terminal envelope (missing route, the single handover timeout, lost reply); whole remaining deadline per attempt; exhaustion kind follows the cause — never admitted → `Unavailable` (error model :75-85), admitted reply never arrived → `DeadlineExceeded`. Application requests never replay.
- cpp (`c835f5b325`) and node (`21c12eb02e`) now implement the exhaustion kind. cpp stage-2 reported: dotnet and java Actor Join, and node STREAM actor bind, still collapse pre-admission exhaustion into DeadlineExceeded; node stage-1 reported cpp's replay scope is narrower than the spec (retries only on route absence, not on the handover single timeout / lost reply).

## Deliverable — `doc/plan/c016-worklog/diag-exhaustion-kind-parity.md`
Per implementation (dotnet Actor Join/create path e.g. `ZLinkActorRemoteJoiner.cs`, `ZLinkSubmitFailureMapper`; java actor join/create + bound-session bind; node STREAM actor bind; cpp `raw_mesh_node_owner.cpp` retry predicate): (1) file:line of the retry predicate and of the exhaustion mapping; (2) which binding typed result marks "admitted" there; (3) current behaviour vs the spec on a 3-row matrix (route absent whole deadline / admitted-then-no-reply / handover single timeout); (4) owner layer and whether any site duplicates the binding's classification; (5) class A/B/C/D and the minimal fix + regression per language. STOP after the diagnosis.

# Job: final java sample gate, round 3 — aggregate `run_samples.sh` (java + kotlin) on the committed main tree, plus individual re-runs only for failures

You are a codex sub-agent under a Claude supervisor. Execution/classification job (no source edits).
Read `AGENTS.md` (root) §2/§4 and `doc/plan/c016-worklog/gate-final-java-samples-2-summary.md` (round 2:
the contract-test stop was resolved by reverting the TicTacToe mode change `dd23fce2ff`; ZoneWorld
self-invocation fixed; Bingo 1× TEARDOWN_FAILED unreproduced 5/6 — see `bucketB-java-bingo-teardown-2-summary.md`).
Do not modify source/tests, nothing under `core/**`, `bindings/**`, any `doc/spec/**`,
`framework/doc/framework/**`, `doc/site/**`. Do not rebuild Core or local packages. Another job runs
node gates concurrently — only touch the java tree.

## Environment
`TMPDIR=/dev/shm/zlink-tmp-java` (mkdir -p), `flock -w7200 /tmp/zlink-jvm-gate.lock` around every
gradle/sample invocation, do NOT export a directory-valued `ZLINK_LIBRARY_PATH`.

## What to run
1. `bash framework/languages/java/samples/run_samples.sh` once from the repo root (java + kotlin, 14
   samples): exit, duration, per-sample completion markers in order, stop point if any.
2. For every sample that failed in the aggregate: re-run it individually once via
   `bash framework/languages/java/samples/<java|kotlin>/<Sample>/run_sample.sh` (from the repo root)
   and record determinism. Preserve the failing run's role logs (copy `build/sample-logs/` of that
   sample and the `/dev/shm/zlink-tmp-java/tmp.*` dir to `zlink-work/c016/logs/gate-final-java-3-<sample>-evidence/`
   BEFORE any retry — a retry overwrites `build/sample-logs/`).
3. `./gradlew --no-daemon contractTest` once (known C: `JavaDocumentationRegressionTest` only; the
   `SampleReleaseGateContractTest` must pass now).

## Classify every failure
(A) DONTWAIT/backpressure, (B) terminal/error classification, (C) known pre-existing (exact prior
evidence `file:line`), (D) environment/runner, (E) binding-port dependency; first failing
assertion/log excerpt with `file:line`. For a Bingo `TEARDOWN_FAILED` or client timeout, include the
play-a cause stack from the preserved role log.

## Deliverable
`doc/plan/c016-worklog/gate-final-java-samples-3-summary.md`: 14-row aggregate table, retries,
contractTest counts, failures bucketed, exact commands, logs under
`zlink-work/c016/logs/gate-final-java-3-*.log`, BLOCKERS. Do not commit.

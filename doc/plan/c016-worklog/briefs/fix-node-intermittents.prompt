# Job: node framework — root-fix two intermittent gate failures (M6A test 39, spot-manager timer test 86)

You are a codex sub-agent under a Claude supervisor. Model gpt-6-astra, reasoning xhigh. Cap 3 h. Read `AGENTS.md` §2/§3/§4.1/§5 and `framework/AGENTS.md`. Campaign rules (D-098, D-109, D-111, D-095): one owner per rule, fewer rules, NO mitigations (no timeouts, retries, sleeps, wider tolerances, skips, second pollers), regression test per fix, cross-language parity, public API unchanged. Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/**`, `doc/site/**`, other languages. Do not commit. Work in `framework/languages/node`. Take `/tmp/zlink-node-gate.lock` (flock) for any gate; `/tmp/zlink-samples-gate.lock` too if samples run. Another job edits only `framework/languages/node/samples/ZoneWorld/**` and `e2e/**` runner scripts — do not touch those.

## Failure 1 — M6A test 39 `bilateral endpoint-only manual connections learn peer RIDs and converge`
`test/m6a/m6a-runtime.contract.ts` ~:1958 (compiled `build/m6a-runtime/.../m6a-runtime.contract.js:1435`). Intermittent: after both peers are Ready and the initiator is decided, a request is sent and `pollUntil(() => target.pumpOne() === 'application')` times out ("Timed out waiting for deterministic runtime progress"). Evidence: `/tmp/zlink-node-r8-parity.mVQCTy/m6a.log:236`; earlier note D-116 (passed 100x once, so it is order/state dependent). Both peers are Object Servers (NotRequired path is not involved). Determine the root cause in the runtime (`runtime/foundation/raw-service-mesh-runtime.ts`, backend, admission) or in the test's deterministic pump model — e.g. a completion/record that is delivered to a lane the pump does not drain, a reciprocal-connect collapse (D-094 RID comparison, loser standby) that drops the request when the loser's lane is chosen, or a WRITABLE token path. Reproduce first: run the M6A suite 200x (`npm run verify:m6a-runtime` under the lock, or the compiled test with `--test-name-pattern`) and capture the failing state (which lane has the record, socket states, peer RIDs). Fix the cause; the test must then pass 300/300.

## Failure 2 — `spot managed timer overrun policies follow dotnet skip catch-up and fixed-delay semantics`
`test/contract/spot-manager.test.js:3990` (helper ~:4184 replaces global `setTimeout` and collects every scheduled timer). Intermittent under load: pending list `[10, 5]` observed instead of `[10]` — a 5 ms timer from some other callback (another test's leftover, a runtime retry, or a Spot timer registry tick) leaks into the collected set. Evidence: `/tmp/zlink-node-r8-parity.mVQCTy/gate.log:9339`. Find the issuer of the 5 ms timer. If it is a runtime timer that should not exist (a hidden retry/poll), remove it at its owner. If it is a test-isolation defect (global setTimeout interception seeing timers of other tests/handles), fix the fixture so it only observes timers scheduled by the Spot timer registry under test (inject the scheduler into the registry instead of patching the global), without weakening the assertion.

## Gates
Per fix: the targeted test(s) 300x under the lock, then full `flock /tmp/zlink-samples-gate.lock flock /tmp/zlink-node-gate.lock npm test` (0 failures; it includes samples and M6A). If a failure is unrelated and you can prove it, report it with evidence instead of retrying.

## Deliverable
`doc/plan/c016-worklog/fix-node-intermittents-summary.md`: per failure — root cause with file:line and the failing state captured, fix, regression test, diff split, gate results, BLOCKERS. Do not print it to stdout.

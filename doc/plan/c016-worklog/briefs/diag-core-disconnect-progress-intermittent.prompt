# Job: Core — root-cause the intermittent `test_socket_disconnect_progress_without_app_poll` failures (DISCONNECTED event not observed within 3 s; 3/40 at low load)

You are a codex sub-agent under a Claude supervisor. Cap 3 h. Work ONLY in `/home/hep7/project/zlink-core-gate` (detached at origin/main `ac5b6a6b92` or newer — run `git -C /home/hep7/project/zlink-core-gate fetch origin && git -C /home/hep7/project/zlink-core-gate checkout --detach origin/main` first; `core/build-gate` is a Release+LTO build already configured with tests). Never touch `/home/hep7/project/zlink`. Never modify `doc/spec/**`. Do not commit; write the diff to `/tmp/zlink-core-gate/disconnect-progress-fix.patch`. Campaign rules: no mitigations (no longer timeouts, retries, sleeps), one owner per rule, regression via the public C API, `hotpath_gate` if a hot path changes. Another Core job runs in `/home/hep7/project/zlink-core-tests` — do not run two LTO builds at once (`pgrep -c lto1` == 0 before building; the test targets are non-LTO now, so rebuilding tests is cheap).

## Failure
`core/tests/integration/test_socket_disconnect_progress_without_app_poll.cpp` (added by commit `973ebe30d5` "streamline STREAM pull delivery and fix disconnect progress"). In 40 standalone runs at load < 7, 3 failed: `test_inproc_unregistered_server_disconnect_progresses` (`:302`, twice) and `test_stream_disconnect_reconnect_on_single_io_thread` (`:594`, once) — `wait_monitor_event(monitor, ZLINK_EVENT_DISCONNECTED, old_connection_id, ...)` returns false within `event_timeout_ms = 3000`, followed by "Forced closure of 2 sockets". Logs: `/home/hep7/project/zlink/zlink-work/gates/core-main/dp-13.log`, `dp-19.log`, `dp-35.log`. It also failed once in the full integration lane under load. Determine whether (a) Core does not always emit/deliver DISCONNECTED for that connection id after `zlink_disconnect`/close of an unregistered inproc server or on the single-I/O-thread STREAM reconnect (a disconnect-progress defect the commit intended to fix — compare the commit's own description and the pipe/session/own.cpp changes; consider interaction with D-118 `pipe.cpp:1874` hold-release credit waiter and D-098 endpoint release), (b) the event is emitted but the monitor's one-slot coalescing (spec `06-monitoring` §6.3 / D-B119) replaces it before the test drains, or (c) the test's `wait_monitor_event` helper misses an event that arrived before it started waiting (helper contract). Reproduce under `perf`/`ZLINK_DEBUG_*` or a small instrumented copy; capture the monitor event history for the failing run. Fix at the owner (Core, monitor semantics, or test helper) without weakening assertions; regression 200/200 for the two cases; integration lane 0 failures; hotpath if applicable.

Deliverable: `doc/plan/c016-worklog/diag-core-disconnect-progress-summary.md` in the MAIN tree's doc/plan dir. Do not print it.

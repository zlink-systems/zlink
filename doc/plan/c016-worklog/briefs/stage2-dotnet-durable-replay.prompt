# Job (STAGE 2, approved A, model astra): dotnet — Actor Join / Actor create / User Spot durable operations follow the sender replay policy and exhaustion kind

You are a codex sub-agent under a Claude supervisor. Read `AGENTS.md` (root) §3 (계층 소유권·금지 패턴·교차언어 대조), `framework/AGENTS.md`. Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/framework/**`, `doc/site/**`, other languages; never lower an assertion or widen/split a deadline; no second poller, no retry tables, no errno re-classification; do not commit. STOP only for a Core/binding defect (report with a public-API repro description). Env (never `--artifacts-path`, never `ulimit -v`): `export TMPDIR=/dev/shm/zlink-tmp-dotnet ZLINK_LIBRARY_PATH=/home/hep7/project/zlink/core/build-dev/lib UseSharedCompilation=false MSBUILDDISABLENODEREUSE=1 DOTNET_CLI_TELEMETRY_OPTOUT=1; pkg_hash=$(sha256sum .artifacts/wsl/nuget/Systems.Zlink.0.17.0.nupkg | awk '{print $1}'); export NUGET_PACKAGES=/dev/shm/zlink-tmp-dotnet/nuget-${pkg_hash:0:16}; cd framework/languages/dotnet; flock -w7200 /tmp/zlink-dotnet-gate.lock dotnet test ...` (every build/test under the flock). Packages rebuilt 10:55 with Core 8b82d51b75 (stranded REQUEST -> immediate REQUEST_NOT_CONNECTED), 0c39ed2e52, 1c69086a4a; core/build-dev also has 7cbf12de41 (monitor command lease). Do not rebuild. Another dotnet job edits `Runtime/Channels/ZLinkClientServerClientRuntime.cs` concurrently — do not touch it; you own the Actor Join/create and User Spot durable sender paths (`ZLinkActorRemoteJoiner.cs`, `ZLinkSubmitFailureMapper`, the canonical create/join submit loops in `ZLinkManagedMeshNode.cs` / `ZLinkFrameworkRuntimeActors.cs`, User Spot terminal replay) and their tests.

## Approved diagnosis
`doc/plan/c016-worklog/diag-exhaustion-kind-parity.md` §.NET (retry predicate/exhaustion mapping file:line, binding admission marker, 3-row matrix, 최소 수정·회귀). Spec: `04-actor-model.ko.md` sender replay bullets (same OperationId replay on any outcome without a terminal envelope; whole remaining deadline per attempt — the `ServiceTerminalRetryTimeout` fixed cut is forbidden; exhaustion kind by cause: never admitted → Unavailable, admitted reply never arrived → DeadlineExceeded; application requests never replay) and Core §4 (bb730c654f: a request stranded by HANDOVER completes immediately with REQUEST_NOT_CONNECTED — treat the binding's typed NotConnected as the replay marker, never a topology guess). Reference implementations: java `ZLinkJavaDurableRequest` (23564aa920), node `requestDurableOperation` (71202e4947, 21c12eb02e, 8e509bb143), cpp `raw_mesh_node_owner` retry state (c835f5b325).

## Known red that this must address (gate v8 on the rebuilt packages, `scratchpad/gate-v8-rest.log`)
`StatefulServiceRuntimeTests.RemoteUserSpotTerminalReplaysAfterDeadlineAndExpiresWithoutReexecution` (expected create count 1, actual 0: the first losing-route attempt consumed the 2 s deadline — with Core now completing it as NotConnected immediately, the sender must replay within the remaining deadline). The four other Stateful reds (`BilateralCallerFirst…`, three `RemoteActorStaleAuthority*`) and Canonical `HandoverLeavesReplyRouteToCore` are attributed to a Core reciprocal classification issue under investigation — re-run them and report, but do not try to fix them here.

## What to do
1. One durable-sender owner for the .NET durable operations (encoded identity frozen across attempts, deadline, admission history from the binding typed result) — mirror the java/node shape; delete the fixed `ServiceTerminalRetryTimeout` cut and any topology-based re-classification of a typed timeout.
2. Regression: 3-row matrix per operation (route absent whole deadline → Unavailable; admitted then reply withheld → DeadlineExceeded; NotConnected then route ready → replay succeeds within the original deadline, create count 1).
3. Verify: `StatefulServiceRuntimeTests`, `ServiceRuntimeFoundationTests`, `ZLinkMeshPeerAdmissionTests`, Canonical family (`--blame-hang --blame-hang-timeout 5m`), then the full unit gate once (`FullyQualifiedName!~CanonicalActorJoinIngressReplyTests`), samples TicTacToe + GameQuest 1×.

## Deliverable
`doc/plan/c016-worklog/stage2-dotnet-durable-replay-summary.md`: diff, the four lines, matrix results, gate counts (with the remaining reds attributed), BLOCKERS.

# Job: dotnet framework — root-fix the intermittent `MeshNodeShutdownSealTests.IdempotentAdmit_CompletesWithoutReadmittingOrResettingTheEpoch`

You are a codex sub-agent under a Claude supervisor. Model gpt-6-astra, reasoning xhigh. Cap 3 h. Read `AGENTS.md` §2/§3/§4.1/§5, `framework/AGENTS.md`. Campaign rules: NO mitigations (no longer deadline, retries, sleeps, skips), one owner per rule, regression test per fix, tests are interface-based and deterministic (inject clocks/schedulers; no real-time windows). Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/**`, `doc/site/**`, other languages, `framework/languages/dotnet/perf/**` (another job), or `Runtime/Channels/ZLinkClientServerRuntimeService.cs` (just committed). Do not commit. dotnet env: `source framework/languages/dotnet/perf/scripts/dotnet-env.sh`; `/tmp/zlink-dotnet-gate.lock` for builds/tests.

## Failure
`framework/languages/dotnet/tests/Zlink.Framework.UnitTests/Runtime/MeshNodeShutdownSealTests.cs:94` `IdempotentAdmit_CompletesWithoutReadmittingOrResettingTheEpoch`: after the first Hello is admitted (`:121` admitted count 1 passes) the test waits at `:122` for the Admit reply and hits the fixture's 2 s deadline at `:414`. Failed once in a full unit run under machine load (another job running); passed alone in 200 ms. Evidence: `/dev/shm/zlink-cs-server-ready-gate-20260906/unit-main.log:37` and the TRX there. The test builds a binding context, `ZLinkManagedMeshNode` and a Dealer directly (`:96,97,107`) — RouteMesh admission path, not ClientServer monitoring.

## Task
1. Reproduce: run the test class 200x under the lock, then 100x with artificial load (`yes > /dev/null` x20). Capture the failing state: was the Admit reply sent by the node (Framework message-flow/diagnostics `ZLINK_DEBUG_FRAMEWORK_*`), received by the Dealer socket (binding completion), or lost between (poller/owner not progressing because the test thread holds it)? Distinguish: (a) runtime defect — Admit reply for an idempotent re-Hello not sent, sent on the wrong lane, or completion not delivered; (b) test defect — the fixture waits with a real-time deadline while the reply delivery depends on a poller the test thread should drive, or a wall-clock race in the Dealer receive (D-095).
2. Root-fix at the owner. If (a): fix the runtime and add a deterministic regression. If (b): make the fixture drive/observe the binding owner deterministically (receive with the public blocking API or the owner's wait, no timed polling) — keep every assertion.
3. Gate: the class 300/300 (with load), then the split unit gate (`--filter 'FullyQualifiedName!~CanonicalActorJoinIngressReplyTests'` and the complement, `--blame-hang --blame-hang-timeout 10m`) → 0 failures.

Deliverable: `doc/plan/c016-worklog/fix-dotnet-meshnode-admit-intermittent-summary.md` (captured state, cause file:line, fix, gate). Do not print it.

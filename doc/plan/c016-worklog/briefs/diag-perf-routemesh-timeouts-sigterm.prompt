# Job: dotnet perf phase 1 follow-up diagnosis — RouteMesh 4096 warmup DeadlineExceeded x16 and perf servers not exiting on SIGTERM

You are a codex sub-agent under a Claude supervisor. Model gpt-6-astra, reasoning xhigh. Cap 2.5 h. Read `AGENTS.md` §2/§3/§4.1/§5, `framework/AGENTS.md`, `framework/doc/framework/common/perf/README.ko.md` §4/§16/§20. Diagnosis job: you may edit only `framework/languages/dotnet/perf/**` (harness) when the cause is in the harness; Framework runtime (`framework/languages/dotnet/src/**`), Core, bindings, specs, other languages are read-only — report causes there with file:line and a public repro. Do not commit. dotnet env: `source framework/languages/dotnet/perf/scripts/dotnet-env.sh`; take `/tmp/zlink-dotnet-gate.lock` for builds and `/tmp/zlink-samples-gate.lock` while perf cells run (they use fixed ports). Another job edits `src/Zlink.Framework/Runtime/Channels/ZLinkClientServerRuntimeService.cs` — do not touch src/.

## Symptom A — RouteMesh channel-echo-only 4096 bytes: 16 `DeadlineExceeded` during warmup (sent 11904, completed 11888), measured window never started
Evidence: `doc/plan/c016-worklog/perf-dotnet-runner-phase1-summary.md` §셀별 결과 and §남은 실패; raw `/dev/shm/zlink-perf-dotnet/perf-results/phase1-smoke/channel-echo-only/4096/request-ordinary-routemesh-*/server-channel-0.json` (first error + kind). Same cell with message-flow tracing on: 0 errors (2 s and 30 s). 1024 bytes: 0 errors. The machine ran 3-4 other jobs at the time (load 20-50). First reproduce on a quiet machine (check `/proc/loadavg` < 5 before each run): run the 4096 cell 5 times (30 s each). Then determine the cause: (1) harness — request timeout value/configuration for 4096 (perf README §5.2 defaults: request timeout 1000 ms), pipelined depth, the warmup trigger racing the reset barrier, source-side application counter lock; (2) Framework — 4096-byte payload crossing a Core HWM/credit boundary (D-118 fixed today in Core source but `core/build-dev` is NOT yet rebuilt: note the library hash you ran against, `sha256sum core/build-dev/lib/libzlink.so.0.17.0`), reply route, ClientServer/RouteMesh admission; (3) load. If it reproduces only under load, prove it by running with and without an artificial load (`yes > /dev/null` x20) and report it as load-sensitivity with numbers — do not change timeouts.

## Symptom B — three perf servers (session-echo-only and channel-echo-only) did not exit within 5 s after SIGTERM and were SIGKILLed (`cleanup.json` exitCode -9)
Determine whether the harness `ServerApplication` SIGTERM path calls the Framework host shutdown (`ZLinkFrameworkHost` Shutdown / `IHostApplicationLifetime`) and awaits it, whether the application HTTP listener/metrics threads keep the process alive, or whether Framework shutdown itself does not complete within its bounded drain deadline (host relocation §14: bounded teardown `ForceStopped/DeadlineExceeded`, D-097/D-098 seal). Reproduce: start a perf server alone, send SIGTERM, measure time to exit and the final host state via the application `/perf/stats` or logs. If the harness is at fault, fix it in perf/ and add a check to `HarnessContractTests`/`test_harness.py`. If the Framework does not finish shutdown, capture the host state/last drain step with a public repro and report it as a Framework defect (do not fix src/).

## Deliverable
`doc/plan/c016-worklog/diag-perf-routemesh-timeouts-sigterm-summary.md`: for A and B — reproduction table (runs, load, library hash), cause with file:line, harness fix if any (files), what remains a Framework defect with public repro, BLOCKERS. Do not print it to stdout.

# Job: Core — root-cause the intermittent `test_flow_state_paired` / `test_network_peer_weight_replays_after_reconnection` ("Peer weight did not reach the public monitor" + teardown hang → 10 s timeout)

You are a codex sub-agent under a Claude supervisor. Cap 3 h. Work ONLY in `/home/hep7/project/zlink-core-tests` on branch `core-tests-rebase` (commit `eda6ef24e5`, rebased Core test restructure; `core/build-gate` is a Release+LTO Ninja build already configured). Never touch `/home/hep7/project/zlink`. Never modify `doc/spec/**`. Do not commit; leave a diff and write it to `/tmp/zlink-core-tests/flow-state-fix.patch`. Campaign rules: no mitigations (no timeouts/retries/sleeps), one owner per rule, regression test via the public C API, hot-path changes need `hotpath_gate`. Check `/proc/loadavg` < 10 before builds; no other Core build may run (`pgrep -c lto1` == 0).

## Failure (from `doc/plan/c016-worklog/core-tests-rebase-summary.md` BLOCKERS 1)
In the full integration order `test_network_peer_weight_replays_after_reconnection` (in `core/tests/integration/test_flow_state_paired.cpp`) failed twice, and once on the first run of a selected-case repeat, with `Peer weight did not reach the public monitor`; after the assertion the socket teardown does not finish and CTest kills it at 10 s. Alone: 0.38 s PASS; under gdb PASS. Two questions: (1) why the peer weight (re)publication after reconnection sometimes does not reach the public monitor — Core weight replay on reconnect (`07-router` weight contract, D-094/D-098 endpoint release, D-118 hold release credit, or the STREAM/disconnect-progress commit `973ebe30d5`), or a test race (monitor opened after the event, event coalesced by the monitor's one-slot rule — check the spec `06-monitoring` on which events are coalesced); (2) why teardown hangs after a failed assertion (Unity longjmp leaves sockets open → `zlink_ctx_term` waits on linger/monitor?) — the hang is a separate defect if a normal close should complete.
Reproduce: run the full integration lane order (`bash core/tests/run_test_lanes.sh --build-dir core/build-gate --lanes integration`) until it fails, capturing with the existing monitor/diagnostic options; also loop the single test 200x with `yes > /dev/null` x20 load. Capture the state: was a WEIGHT event emitted by the ROUTER (public monitor snapshot/history), what the reconnect sequence was, and where teardown blocks (gdb attach / `ZLINK_DEBUG_*`).
Fix at the owner if Core; if test, fix the fixture without weakening the assertion (the weight must reach the public monitor after reconnection — that is the contract) and explain what was racing. Regression 300/300; integration lane 0 failures; `hotpath_gate` if you touched a hot path.

Deliverable: `doc/plan/c016-worklog/diag-core-flow-state-paired-summary.md` in the MAIN tree's doc/plan dir. Do not print it.

역할: zlink Java 바인딩 hot-path 리뷰 기반 pass 2(gpt-6-astra). 작업 트리: /home/hep7hep7/project/zlink-wt-java-perf2 (detached @ main; core/build·core/build-dev symlink — core/** 아래 cmake/build/clean 금지). 수정 범위 bindings/java/** 만(공개 API 시그니처·ownership·error contract·측정 의미 유지; spec·doc·core·다른 binding 금지; 러너 scheduler/drain/fairness 변경 금지). git commit/push/reset/checkout/stash 금지. JDK `/home/hep7hep7/.jdks/jdk-22.0.2+9`(JAVA_HOME), gradle workers 2, 실행 전 `export ZLINK_CORE_SOURCE=local; source bindings/tools/local_core_runtime.sh`, 종료 시 `./gradlew --stop`. 진행 로그 /home/hep7hep7/project/zlink-work/c016/java-perf-pass2-progress.md 한 줄씩 append(3분마다 한 줄 이상), 종료 시 `EXIT:<code>`. 요약 /home/hep7hep7/project/zlink-work/c016/java-perf-pass2-summary.md (후보 표, 변경, before/after 표, gate, spec gap 여부, BLOCKERS). after report는 /home/hep7hep7/project/zlink-work/c016/reports/ 로 복사. **측정 규칙: 공식 after는 `uptime` 1분 load ≤ 3에서만, 진행 로그에 load 기록.**

배경: pass 1+1b(`82b0fd9f38`, 요약 `doc/plan/c016-worklog/java-perf-pass1-summary.md`, `java-perf-pass1b-summary.md`: REQREP socket별 settlement 대기 → poller 1회 처리 뒤 일괄, native scratch, Context당 completion pump 1개) 뒤 조용한 머신 paired 3-run(15:23) Java/C: DD 70.8%(64B 75.9, 256B 72.9, 1024B 77.0, 4096B 66.1, 64K 62.2; 목표 90%), DR_REQREP 54.7%(37.0/38.5/44.8/44.4/108.8; 목표 70%), RR_REQREP 58.5%(39.8/31.3/58.2/68.3/95.0; 목표 70%), PUBSUB 102.9% 통과. 잔여 비용 후보(pass 1 계측): REQREP 요청당 managed 1,7xx B·FFM 39회(C API 17회), DD 444 B·FFM 20회(C 13회), DD 4096/64K에서 send-side backpressure 뒤 wake 비용, RR 256B 31.3%의 편차. 사용자 원칙: 규칙 단순·중복 없음·제어 한 곳·POSDDD — 공개 시그니처를 유지하면서 내부 구조를 바꾸는 후보는 모두 검토 대상. 가이드 `doc/perf/BINDINGS_OPTIMIZATION_GUIDE.ko.md` §4 기각 목록 금지. 참고: C++ pass 2(`cpp-perf-pass2-summary.md`: resume slot 번들, inline first completion entry, reply adopt in place), .NET pass 1(struct submitter, native scratch, pooled receive headers).

할 일:
1. read-only 리뷰로 후보 표(예상 효과·계약 보존 방법·§4 해당 여부), 각 후보를 미니벤치/할당 계측으로 확인(추측 금지). 특히 (a) REQREP 요청당 FFM 39회→C 17회 차이의 원인(part별 downcall, size/has_more 조회, completion recv 왕복), (b) 요청당 managed 할당(Arena/MemorySegment/CompletableFuture/lambda), (c) DD 큰 메시지 send-side wake, (d) public poller 미사용 경로와 사용 경로가 같은 코드를 타는지.
2. 5% 이상 기대되는 후보 적용, no-go는 근거와 함께.
3. after 측정(규칙 준수): `JAVA_HOME=... bash bindings/java/perf/multi/run_benchmarks.sh --pattern DEALER_DEALER,DEALER_ROUTER_REQREP,ROUTER_ROUTER_REQREP,PUBSUB --transports tcp --msg-sizes 64,256,1024,4096,65536 --clients 100 --duration 5 --runs 1`, before(3-run)/C 대비 표.
4. Gate: `bindings/java/tests/run_tests.sh` 전체 + samples 7/7, 관련 contract 5회, `git diff --check`, 공개 API 불변(classfile 대조), spec gap 여부.
메모리 11 GB, 다른 job 동시: gradle workers 2. 상한 90분.

역할: zlink Rust 바인딩 리뷰 기반 pass 2(gpt-6-astra). 작업 트리: /home/hep7hep7/project/zlink-wt-rust-perf2 (detached @ main; core/build symlink — core/** 아래 cmake/build/clean 금지). 수정 범위 bindings/rust/** 만(공개 API·ownership·error contract·측정 의미 유지; spec·doc·core·다른 binding·러너 금지). git commit/push/reset/checkout/stash 금지. cargo `CARGO_TARGET_DIR=/home/hep7hep7/project/zlink-work/c016/rust-target-perf2` `-j3`; 공식 테스트 `ZLINK_CORE_SOURCE=local CARGO_BUILD_JOBS=3 bash bindings/rust/tests/run_tests.sh`. 진행 로그 /home/hep7hep7/project/zlink-work/c016/rust-perf-pass2-progress.md 한 줄씩 append(3분마다 한 줄 이상), 종료 시 `EXIT:<code>`. 요약 /home/hep7hep7/project/zlink-work/c016/rust-perf-pass2-summary.md. after report는 /home/hep7hep7/project/zlink-work/c016/reports/ 로 복사. **측정 규칙: 벤치·프로파일은 `uptime` 1분 load ≤ 3에서만, 진행 로그에 load 기록.**

배경: pass 1(`4163072701`, 요약 `doc/plan/c016-worklog/rust-perf-pass1-summary.md`) 뒤 quiet 3-run(21:48) Rust/C: DD 64.7%(64B 48.3, 256B 40.0, 1024B 48.6, 4096B 65.4, 64K 120.9; 목표 95), DR 66.1%(67.2/68.6/63.9/69.7/61.2; 85), RR 69.1%(66.9/73.6/67.9/72.9/64.1; 85), PUBSUB 93.8%(95, 최소 85 충족). pass 1 요약이 남긴 후보: 공개 wrapper `Message` 이동 비용, close와 경쟁하는 shared submit RwLock, REQREP pending state, SEND 즉시 경로의 잔여 FFI 호출. 계획서 §7.4-11(리뷰 pass: 공개 contract 보존 후보 → 적용 → after 1회), 가이드 §2/§4. 참고: C++ pass 2(`cpp-perf-pass2-summary.md`: resume slot 번들, inline first completion entry, reply adopt in place), Go pass 1(`go-perf-pass1-summary.md`). 사용자 원칙: 규칙 단순·중복 없음·제어 한 곳·POSDDD.

할 일:
1. read-only 리뷰로 후보 표(예상 효과·계약 보존·§4 해당 여부), callgrind/미니벤치로 확인(추측 금지): (a) SEND 즉시 경로 FFI 호출 수 vs C(13), (b) shared RwLock 경합·제거 가능성(close 확인을 원자 상태로), (c) REQREP pending 상태 번들, (d) 수신 wrapper 재사용.
2. 5% 이상 후보 적용, no-go 근거.
3. after 측정(규칙 준수): `CARGO_TARGET_DIR=... bash bindings/rust/perf/run_benchmarks_multi.sh --pattern DEALER_DEALER,DEALER_ROUTER_REQREP,ROUTER_ROUTER_REQREP,PUBSUB --transports tcp --msg-sizes 64,256,1024,4096,65536 --clients 100 --duration 5 --runs 1`, C 기준 `bindings/c/perf/results/multi/report/*_p1rust-r3q2.txt`(21:36~21:48) 3-run 평균.
4. Gate: `run_tests.sh` 14/14(samples), 관련 5회, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, `git diff --check`, 공개 API 불변, spec gap 여부.
메모리 11 GB, 다른 job 동시: `-j3`. 상한 90분.

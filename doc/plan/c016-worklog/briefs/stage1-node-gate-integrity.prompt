# Job: node — (1) standard `npm test` gate false-green (force-exit truncates 24 tests), (2) SupportChat timing values vs shared sample timing

You are a codex sub-agent under a Claude supervisor. Read `AGENTS.md` (root) §2/§3/§4.1 and `framework/AGENTS.md`. Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/framework/**`, `doc/site/**`, other languages' trees; never lower an assertion; do not commit. Root-cause rule (user mandate): no budget/timeout increases, no retries "to be safe", no fixture changes that hide runtime behaviour; if the honest fix makes a test fail or slow, report it with evidence instead of mitigating. Do not rebuild Core or local packages. B session owns Core/bindings — if you find a Core/binding defect, STOP that item and report it with a public-API repro description. Env: `framework/languages/node`, `TMPDIR=/dev/shm/zlink-tmp-node`, `unset ZLINK_LIBRARY_PATH`, `flock -w7200 /tmp/zlink-node-gate.lock` around every npm invocation. Another node job (first-reply-loss investigation) runs concurrently on runtime files — you only touch `scripts/run_node_runtime_gate.js`, `samples/SupportChat.Ts/**`, `test/contract/**`.

## Item 1 — gate integrity (from `doc/plan/c016-worklog/review-campaign-cpp-node.md` §"Node test 수 1552 → 1536 조사")
`scripts/run_node_runtime_gate.js:47-52` runs each test file with `--test-force-exit`; two files end right after announcing their last tests (`contract-surface.test.js` `1..28`, `sample-regression.test.js` `1..39`) and 24 registered tests (list in the review) are cut before completion, yet the gate reports success. Expected complete count is 1560. Fix: remove force-exit in favour of explicit handle cleanup / a parent watchdog that fails the file, and make the gate FAIL when a file's TAP plan count differs from the announced tests or when the aggregate completed count differs from the expected total (derive the expected total from the files, do not hardcode 1560 unless the repo already pins counts). Then run the full `npm test` and report the true counts and every failure (classify; the 24 previously-cut tests may reveal real failures — report them, do not fix runtime code here).

## Item 2 — SupportChat timing values (review verdict C on `2e3b1b47e4`)
The ordering root cause is fixed (`bucketB-node-supportchat-aggregate-2-summary.md`). Now verify the Node sample's idle/close/wait values against the shared sample timing source (`framework/languages/shared_sample/**` SampleTimings / the sample contract `framework/doc/framework/common/sample/supportchat/README.ko.md`) and the cpp/dotnet/java SupportChat values; if Node's `sample-names.ts:13-14` / `main.ts:27` differ from the shared values, restore the shared values (no other change) and re-run SupportChat 2× + the four-sample aggregate order 1×. If they already match, say so with the file:line evidence and change nothing.

## Deliverable
`doc/plan/c016-worklog/stage1-node-gate-integrity-summary.md`: gate diff, true counts, failure table, timing comparison table, BLOCKERS.

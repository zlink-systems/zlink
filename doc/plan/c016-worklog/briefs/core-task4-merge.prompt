역할: zlink Core 수정 병합·재검증 담당(gpt-6-astra). 작업 트리: /home/hep7hep7/project/zlink-wt-core-task4m (detached @ main 최신; core/build·core/build-dev는 메인 트리 symlink이므로 **사용 금지** — 별도 `core/build-task4m`에서 scripts/build-core.sh dev 와 같은 cmake 옵션으로 configure, 빌드 `-j3`). 수정 범위: core/src/**, core/tests/**. **spec(`core/doc/spec/**`, `bindings/doc/spec/**`, `framework/doc/framework/**`, `doc/site/**`)·framework/**·doc/plan/** 수정 금지.** git commit/push/reset/checkout/stash 금지. 진행 로그 /home/hep7hep7/project/zlink-work/c016/core-task4m-progress.md 한 줄씩 append(3분마다 한 줄 이상), 종료 시 `EXIT:<code>`. 요약 /home/hep7hep7/project/zlink-work/c016/core-task4m-summary.md.

배경: 두 job이 같은 inproc 경로를 고쳤다. (A) main에 이미 커밋된 `1c69086a4a`(monitor connection identity: inproc 양 pipe half가 같은 connection id, tcp/ipc attempt endpoint pair 1회 할당, inproc peer detach 뒤 `reconnect_inproc` self-command로 connect intent 재등록; 요약 /home/hep7hep7/project/zlink-work/c016/core-monid-summary.md). (B) 아직 main에 없는 작업 4 패치 /home/hep7hep7/project/zlink-work/c016/task4.patch(이전 main 기준; 요약 /home/hep7hep7/project/zlink-work/c016/core-task4-summary.md: ①DISCONNECTED의 inproc connection id 정규화 ②inproc peer close 시 connect intent 소실 수정 ③1-lane DEALER-ROUTER inproc `send_bind`/terminal command가 idle peer mailbox에 남는 문제 — peer command executor를 attach/explicit disconnect에 일시 설치·quiesce ④bound inproc endpoint의 server `zlink_disconnect`가 attached pipe를 종료하지 않던 문제 — no-delay 종료·양방향 ack) + 새 테스트 /home/hep7hep7/project/zlink-work/c016/test_socket_disconnect_progress_without_app_poll.cpp(12 case; CMake 등록은 patch 안).

할 일:
1. patch (B)를 현재 main 위에 적용한다(`git apply --3way` 시도, 충돌은 손으로). ①②는 (A)가 이미 다른 방식으로 해결했을 수 있다 — 두 구현을 읽고 **중복·상충을 제거**해 한 가지 메커니즘만 남긴다(예: reconnect intent는 (A)의 `reconnect_inproc` 명령 경로를 기준으로 하고 (B)의 pipe map 보존 로직 중 필요한 부분만). ③④는 (B)를 채택하되 (A)의 identity 규칙을 깨지 않게.
2. 게이트: `core/build-task4m`에서 신규 두 테스트(`test_socket_disconnect_progress_without_app_poll`, `test_monitor_connection_identity`) 각 5회 green, `test_ctx_term_fixed_rid_handover`·`test_router_reciprocal_handover_lanes`·`test_zmp_request_reply_receive_transaction`·`test_flow_state_paired`·`test_completion_pipe_budget_is_fair_and_stale_requeue_is_fenced` green, 전체 `ctest -j2`(hotpath_gate 제외) green, `git diff --check`. hotpath_gate는 감독자가 Release+LTO로 별도 실행.
3. 요약: 병합에서 버린/합친 부분과 이유(`file:line`), 최종 게이트 수치, BLOCKERS(작업 4 요약의 spec gap 2건은 그대로 인용).
메모리 11 GB, 다른 job 동시 실행: 빌드 `-j3`. 상한 75분.

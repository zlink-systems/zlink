# Job: cpp framework — root-fix two intermittent gate failures (Fanout scope release 4/3, ZoneWorld G4 crash-boundary Unavailable wait)

You are a codex sub-agent under a Claude supervisor. Cap 3 h. Read `AGENTS.md` §2/§3/§4.1/§5, `framework/AGENTS.md`, `framework/languages/cpp/AGENTS.md` if present. Campaign rules: NO mitigations (no timeouts/retries/sleeps/skips/wider assertions), one owner per rule, regression test per fix, tests are interface-based and deterministic. Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/**`, `doc/site/**`, other languages. Do not commit. Locks: `/tmp/zlink-cpp-gate.lock` for builds/ctest, plus `/tmp/zlink-samples-gate.lock` for samples. Check `/proc/loadavg` < 10 and `pgrep -c lto1` == 0 before builds/gates (a Core LTO build may run elsewhere). Preset `linux-ninja-debug`, `--parallel 2`.

## Failure 1 — `ZLinkFrameworkStoreLocationResolvers.AppFanoutPublishUsesLocationAutoConnect`
`tests/Zlink.Framework.UnitTests/test_cpp_framework_store_location_resolvers.cpp:2983`: scoped dependency created 4 / released 3 in the full `ctest -L framework-unit` run; passes alone and when its CTest target runs alone. Evidence: `/tmp/zlink-cpp-cs-server-only-ready-20260906/unit-gate.log`, `unit-LastTest.log`. Fixture is Fanout-only (`:2937-2970`). Reproduce (full label run under the lock, repeat until it fails, capture which scope was not released and who created the 4th one); determine whether a runtime scope is leaked on a specific shutdown/publish ordering (runtime defect — fix at the owner) or the fixture shares provider/scope state across tests (fixture defect — isolate without weakening the 4/4 assertion).

## Failure 2 — cpp ZoneWorld G4 `zoneworld-g4=failed reason=boundary-or-fresh-actor-proof`
Preserved run: `/tmp/zlink-cpp-cs-server-only-ready-20260906/sample-tmp/tmp.WeQwRfNMYV/` (`logs/client-g4.log:1-2` stream connector wait timeout at `samples/ZoneWorld/Client/main.cpp:658-664` waiting for the `Unavailable` reply after arming the crash boundary; `logs/zone-node-1.log:31` `join pending` before owner exit; `logs/zone-node-2.log:194-195` `JoinSpot phase=replied outcome=failed reason=activation_rejected`, `zoneworld-join-failed ... kind=7`; fresh-actor proofs 24 printed afterwards). Compare with the dotnet/node/java ZoneWorld G4 scenario semantics (they pass) and the spec for the crash boundary (Actor join during owner loss → `Unavailable` to the client; failure policy `05-location-relocation/06-failure-failover-policy`). Determine whether the client never got a reply (runtime: join failure not mapped to a client-visible `Unavailable`/terminal on the STREAM session), got a different error kind (kind=7 — map it), or the sample's wait targets the wrong signal. Fix at the owner (runtime or sample), keep the sample's assertion semantics equal to the other languages.

## Gates
Per fix: targeted test/sample 20x under the locks, then `ctest -L framework-unit --parallel 1` 0 failures and `bash samples/run_samples.sh` 7/7.

Deliverable: `doc/plan/c016-worklog/fix-cpp-intermittents-summary.md` (captured state, cause file:line, fix, gate). Do not print it.

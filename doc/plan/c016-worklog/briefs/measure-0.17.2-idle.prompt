너는 job measure-0.17.2-idle(0.17.2 idle 재측정)이다. /home/hep7hep7/project/zlink/doc/plan/c016-worklog/briefs/_common-rules.md 와 core-rf-GATE.prompt 절차 6~8을 읽고 따른다(JOBS=4, PERF_LOCK, 포그라운드, 소스·스펙·커밋·stash 금지). 상한 2시간. 메인 저장소 main(tag core/v0.17.2, `git status --short -- core bindings` 비어 있어야 함; 다른 codex job 없음 확인).

절차: (1) `pgrep -x ninja` 비고 1분 load<1.0이 2분 유지될 때만 측정 시작(최대 20분 대기). (2) `JOBS=4 scripts/build-core.sh release --lib-only`(이미 최신이면 빠름) — `libzlink.so.0.17.2` 확인. (3) with_stream `--stack zlink,asio,zmq --size all --ccu 1000 --runs 3 --reuse-build`, `ZLINK_CORE_SOURCE=local`. (4) perf/c single 5 패턴·multi 7 패턴, tcp, **전 size**, runs 3, `ZLINK_CORE_SOURCE=local`(러너 README대로; 자동 재빌드가 JOBS=16이면 사전에 release lib를 최신으로 두어 재빌드가 안 나게). 각 측정 직전 load 기록. (5) 비교: 계획 §7.1 Phase 0/Phase 2S/G-11b idle 행, §7.4 Phase 0·Phase 2G 기준 값(계획 문서에서 읽음)과 패턴·size별 비율 표.
보고서: /home/hep7hep7/project/zlink/doc/plan/c016-worklog/measure-0.17.2-idle-summary.md (표 위주, 결과 디렉터리·load 포함). 진행 파일 progress-measure-0.17.2-idle.md(3분마다). 계획 문서는 수정하지 않는다(감독관이 반영).

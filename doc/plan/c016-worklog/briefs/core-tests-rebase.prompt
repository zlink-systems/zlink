# Job: Core tests restructure — rebase the worktree result onto current main (conflicts with the STREAM single-part commit 973ebe30d5) and re-verify

You are a codex sub-agent under a Claude supervisor. Cap 2.5 h. Work ONLY in `/home/hep7/project/zlink-core-tests` (detached worktree holding the uncommitted result of the previous job; report `doc/plan/c016-worklog/core-tests-public-api-link-summary.md`, patch `/tmp/zlink-core-tests/fix.patch`). Never touch `/home/hep7/project/zlink`. Never modify `doc/spec/**`. Do not push. Check `/proc/loadavg` < 10 before the Release build (another Core build may not run concurrently — `pgrep -c lto1` must be 0).

## Task
1. In the worktree: `git add -A core && git commit -m "wip: core tests public-api link"` on a local branch (`git switch -c core-tests-rebase`), then `git fetch origin && git rebase origin/main`. Conflicting files are the ones the user's Core commit `973ebe30d5` ("STREAM single-part FINAL, MORE → NOT_SUPPORTED; RAW receive single part; disconnect progress") also touched: `core/tests/integration/monitoring/test_monitor_socket_contract.cpp`, `test_ctx_destroy.cpp`, `test_helper_interleave.cpp`, `test_stream_socket.cpp`, and `test_stream_fastpath.cpp` (deleted upstream — drop our hunks; move any still-valid internal assertions of it to the unit test the previous job created, or note them as covered by upstream `test_stream_socket.cpp`). Resolve by keeping BOTH intents: upstream's new/changed test cases and contract (single-part STREAM, new ctx-destroy/ownership/disconnect-progress tests) AND the public-API split (integration = public headers + shared libzlink; internal assertions in `unittest/`). Any upstream test that includes private headers must be split the same way (record it in the report). Also merge `core/tests/CMakeLists.txt`, `core/CMakeLists.txt` (upstream may have added/removed test targets).
2. Re-verify exactly as the previous job did: clean Release+LTO Ninja build of `core/build-gate` (record wall time library vs rest), `bash core/tests/run_test_lanes.sh --build-dir core/build-gate --include-e2e --include-regression --unittest-jobs 8` → 0 failures, `ctest -R '^test_pub_monitor_close_full_mask$'`, `ctest -R '^hotpath_gate$' -V` PASS within ±5%, Debug configure+build, and the public-link audit (`nm -D` undefined-only vs public headers; `ldd` shows `libzlink.so.0`; no LTO flags on test targets).
3. Produce `/tmp/zlink-core-tests/fix-rebased.patch` = `git diff origin/main` of the rebased branch (code + tests only), and `git format-patch` is not needed.

Deliverable: `doc/plan/c016-worklog/core-tests-rebase-summary.md` in the MAIN tree's doc/plan dir: conflict resolution table per file (what upstream changed, what we kept/moved), gate results, timing, BLOCKERS. Do not print it.

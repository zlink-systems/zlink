# Job: java framework — ClientServer Server-only readiness parity (local Ready Server missing from target count)
You are a codex sub-agent under a Claude supervisor. Model gpt-6-astra, reasoning xhigh. Cap 2.5 h. Read `AGENTS.md` §2/§3/§4.1/§5 and `framework/AGENTS.md`. Campaign rules: no mitigations, one owner per rule, fewer rules, public API unchanged, regression test per fix, tests are interface-based (public API only; no internal-structure assertions in integration tests). Never modify `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/**`, `doc/site/**`, other languages. Do not commit. Take the language gate lock (flock) and `/tmp/zlink-samples-gate.lock` for samples. Check `/proc/loadavg` < 10 before a full gate.

## Contract
ClientServer topology readiness: `framework/doc/framework/common/spec/server/06-observability/01-runtime-monitoring.ko.md:194` — `ready` = host serving and the topology can process application messages; ReadyTargetCount includes local and remote Ready Servers with positive weight (dotnet interface :379 wording; the language interface doc for your language is the projection). A **Server-only** ClientServer host (manual peer source, weight > 0, host Serving) must report topology Ready / isReady=true / readyTargetCount=1. A Client-only host with no ready Server is not ready. Outbound Client-role checks (send/request on a Server-only host → NotConfigured) stay with their existing owner. Reference fix: dotnet commit "ClientServer topology readiness counts the local Ready Server" (`ZLinkClientServerRuntimeService.cs`, `ClientServerChannelRuntimeTests.cs:632-693`) and `doc/plan/c016-worklog/fix-dotnet-cs-server-only-ready-summary.md` (parity table).

## Java gap (from the dotnet job's source analysis; verify first)
`ZLinkTopologyRuntimeViews.java:42,61,68` admits readiness for a Server role (`hostServing && (server || !client || readyTargetCount > 0)`) but `ZLinkChannelSocketRegistry.java:573,579,599` only iterates `clientServerConnections`, so a Server-only host reports readyTargetCount 0. Also check the "server role alone is ready" rule: readiness must require the local Server to be registered/listening and Ready (weight > 0), not just the role. One owner: the snapshot computes Ready targets (local + remote) once; the public projection reads it.
Regression tests mirroring dotnet: Server-only weight 100 → Ready/true/1; weight 0 → not ready/0; Client-only without server → not ready/0; Client+Server unchanged. Kotlin projects java — verify with the kotlin contract tests. Gate: `flock /tmp/zlink-java-gate.lock ./gradlew test` 0 failures, samples 7 java + 7 kotlin.
Deliverable: `doc/plan/c016-worklog/fix-java-cs-server-only-ready-summary.md` (cause file:line, fix, rule count, tests, gate). Do not print it.

# Review: cpp + node framework changes of the 0.17.0 DONTWAIT campaign — contract adaptation vs workaround vs spec gap

You are a codex sub-agent under a Claude supervisor doing a READ-ONLY conformance review. Do not edit any file except your deliverable; do not run gates that modify trees; never touch `core/**`, `bindings/**`, any `doc/spec/**`, `framework/doc/framework/**`, `doc/site/**`. Context: the framework worked end to end on Core 0.15/0.16 (baseline `seven-samples-green-v1` = `4bad5ac979`, 2026-08-29, all 7 samples green in 4 languages). Core 0.17.0 changed the contracts: SEND/REQUEST DONTWAIT return a wait token on BACKPRESSURED and the binding async terminal resubmits on WRITABLE (`50d77800f2`, D-B85 `7d8205a028`); `ZLINK_POLLCOMPLETION` is level-triggered and the public poller is the sole completion-drain owner; ROUTER submit to an unknown RID = `ZLINK_SUBMIT_NOT_FOUND`+`ENOENT`; ROUTER RID duplicate policy HANDOVER with reciprocal collapse by RID comparison (core socket README §4). The user's expectation: the ONLY framework changes needed are adaptations to those contract changes; every other change is suspect — either a legitimate pre-existing framework bug that the new Core exposed, or a workaround that hides a root cause (in Core, a binding, or the framework) and risks a spec gap. Your job: for EVERY commit listed below, read the diff (`git show <hash>`) and its worklog summary in `doc/plan/c016-worklog/*.md` (grep the hash), then classify it as exactly one of: (A) required 0.17.0 contract adaptation — cite the Core/binding spec clause (`core/doc/spec/**`, `bindings/doc/spec/**`) that requires it; (B) legitimate framework defect fix — cite the framework spec clause (`framework/doc/framework/common/spec/server/**`) the OLD code violated and explain why it was latent before 0.17.0; (C) workaround/mitigation — the change makes a symptom disappear without the cause being established (budget/timeout increases, retries, reordering "to be safe", fixture-only changes that hide runtime behaviour); name the most likely real cause and the evidence needed to confirm it; (D) spec gap — the change makes behaviour diverge from a spec clause (cite it) or invents a rule no spec has; (E) harness/build/sample-only, no runtime effect. PARITY CHECK is mandatory for every (B)/(C)/(D): find how the cpp implementation (and node) handles the same situation — cpp and node pass all gates — and state whether cpp/node needed an equivalent change (if cpp did NOT need it, explain precisely why dotnet/java did: a genuine structural difference, or a symptom of a different root cause). Verdict per commit: KEEP / RE-FIX AT ROOT (where) / REVERT. Be concrete: file:line, spec clause ids, and what the cpp code does. Deliverable: a Korean markdown report with one table row per commit (hash, one-line, class, spec clause, cpp/node parity finding, verdict) followed by a "재수정 권고" section ordered by risk, and a "스펙 gap 후보" section. 2 h cap.

## Commits to review, oldest first
cpp:
- `b32d4cae64` framework/cpp: transition runtime to the 0.16.0 pull-completion model (context)
- `4d263e66b9` (cpp part) DONTWAIT sends through the binding async path, WRITABLE on POLLOUT
- `180323e6fa` carry the typed serializer's content type through the payload encoder; `0c15d3261f` moved-from supply slot; `504d39fc6e` nested authenticate handlers; `2f1de0b56d` dead-target Actor join → Unavailable + ZoneWorld B8 proxy; `af7afd28e7` crash-boundary (cpp origin)
- `a22f8c880b` framework/cpp: classify a routed submit's NOT_FOUND+ENOENT as a retryable route absence — is ENOENT the spec-defined errno for "RID not in routing map" (`core/doc/spec/core/socket/07-router*`)? Is "retryable" the framework spec's classification (transient route absence vs permanent)? Check `test_cpp_framework_m6b_runtime.cpp` assertion change in the same era (`deadline_exceeded` vs `unavailable`, see the refactor commit's removed comment in `01e5c4613d`) — was an assertion changed to match the implementation (D)?
- `44b9b27efc` framework/cpp: retire the stale same-endpoint connect intent when a replacement RID reconnects — spec `04-network-listener-identity §replacement`; compare with dotnet `b28eb24270` DisconnectTransport rule and `ebff5b3e1b`: are cpp and dotnet now implementing the SAME rule?
- `dd028150c9`, `cee95ff462` cpp tests (poller drive; transport instead of tokenless mailbox record) — test-only: do they weaken what is observed?
- `dbfcf7d6fe`, `4573c09a2a`, `20b94c3457`, `eb756181a6` build/package/connector-test (E expected; confirm)
- `01e5c4613d` refactor pass (confirm behaviour-identical)
node:
- `360181172f` transition (context); `4d263e66b9` (node part); `e86cf2cc8f` diagnostics level Set
- `285cfa522a` framework/node: reserve deadline for operation-terminal replay after a lost first reply (`service-stateful-runtime.ts` ~:4008-4017, "half of the remaining deadline for the first attempt"). This is a BUDGET SPLIT: classify carefully — spec clause for terminal replay (`02-spot-messaging.ko.md:392-403`, `02-session-actor-binding.ko.md:626+`)? Why was the first reply lost in the two-process User Spot test on 0.17.0 and not before — is the lost reply itself the root cause (Core/binding: a reply dropped on a retired pair, D-B85 REQUEST wait token) that this change papers over? How does cpp handle the same replay (parity)?
- `2e3b1b47e4` framework/node/samples: SupportChat idle 10000 / close grace 2000 / wait 20000 — the user explicitly rejects this as a mitigation; the failure has now recurred in the node aggregate (`gate-final-samples-node-aggregate-2.log:270-300`, three stream waits time out). Establish from the sample contract (`framework/languages/shared_sample/**` supportchat, `framework/doc/framework/common/sample/**`) and the cpp/dotnet/java SupportChat clients what the budgets and the wait-arming order are in the languages that pass, and state the real defect (scenario ordering vs framework push-after-resume). A separate job (`bucketB-node-supportchat-aggregate-2`) is fixing it concurrently — do not edit; your finding will be cross-checked against its result.
- `c67deb44e0`, `2ceb137abe`, `4135d4edf6`, `8dd97bda2d` (E expected; confirm the smoke attribute-name change `4135d4edf6` matches the observability spec names)
- `ec2ee1ea50` DeliveryDispatch parity (node/cpp/dotnet parts): sample semantics change — see the java brief's question; answer for cpp/node.

Also answer: the node contract tests count went 1552 → 1536 between the earlier gate and the final one — were tests removed/renamed (list them) or is it the ZoneWorld dist set difference?

## Deliverable
`doc/plan/c016-worklog/review-campaign-cpp-node.md`

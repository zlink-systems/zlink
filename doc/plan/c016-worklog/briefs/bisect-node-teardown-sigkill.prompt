# Job (bisect, model astra): find the commit since the all-green baseline that made node sample roles stop exiting at teardown

You are a codex sub-agent under a Claude supervisor. Premise (user, plan `doc/plan/dontwait-0.17.0-framework-completion.ko.md` line 4):
at baseline `seven-samples-green-v1` = **4bad5ac979 (2026-08-29)** all 7 samples in all 4 languages were green and exited cleanly;
everything since (the 0.16.0 pull-completion campaign and the 0.17.0 DONTWAIT campaign, 3 days of work) is the regression window.
No source/test changes; do not commit. Another job (`diag-node-teardown-sigkill`) captures stacks on the main tree — do not modify the
main tree; use worktrees. Env for sample runs: `TMPDIR=/dev/shm/zlink-tmp-node`, `unset ZLINK_LIBRARY_PATH`, `flock -w7200
/tmp/zlink-node-gate.lock`. ORACLE: today's runner (`framework/languages/node/samples/run-sample.mjs` + `run_samples.sh` from main
HEAD, commit 9a0371fea7 — it reports a SIGKILLed role as failure; the old runner hid it) copied into each worktree; `bash
samples/run_samples.sh TicTacToe.Ts` exit 0 with no SIGKILLed role = GOOD; SIGKILLed role = BAD. Confirm each boundary 2×.

## Window and layers (since 4bad5ac979)
- framework/node (`framework/languages/node/packages`): 22 commits — `git log --oneline --no-merges 4bad5ac979..HEAD -- framework/languages/node/packages`
- bindings/node: 19 commits — `... -- bindings/node`
- core/src: 56 commits — `... -- core/src`
The node binding's native module and the Core library it loads: read `bindings/node` build/packaging to learn how the .node addon
locates `libzlink.so` (bundled in the tgz under `.artifacts/wsl/npm`, or dynamic). This decides how to vary Core independently.

## Method
1. Split by layer first. Build a node worktree `/home/hep7/project/zlink-node-bisect` at 4bad5ac979 for `framework/languages/node`
   only (the framework at baseline) but consume the CURRENT packages (12:54 `.artifacts/wsl/npm` tarballs; adjust `package.json`
   file: paths if they differ at baseline) + today's runner. If the baseline framework is GOOD on current packages → the regression
   is in framework/node → `git bisect` over the 22 framework commits (build + oracle per step). If BAD → Core or node binding:
   build Core at 4bad5ac979 in `/home/hep7/project/zlink-core-b` (`nice -n 10 env JOBS=4 scripts/build-core.sh dev`; never touch
   main's `core/build-dev`) and run the baseline framework against it (`LD_LIBRARY_PATH` if dynamic; if the tgz bundles the library,
   rebuild only the node tgz at the candidate via `scripts/local-package/build-wsl.sh node` in the worktree with a Core prefix
   materialized from that worktree build — see `scripts/local-package/README.ko.md`; keep artifacts under the worktree, never
   overwrite main's `.artifacts/wsl`). GOOD → bisect Core (56) / node binding (19) by rebuilding at candidates. Baseline expected
   GOOD by premise; if the baseline itself is BAD with the current runner, say so explicitly (that would refute the premise) and
   still bisect for the earliest BAD.
2. Each bisect step: TicTacToe.Ts 2× (agree), Bingo.Ts at the found boundary.
3. Deliverable `doc/plan/c016-worklog/bisect-node-teardown-sigkill.md`: layer split result, bisect table (commit, layer, result),
   FIRST BAD commit with its diff summary, mechanism hypothesis for the stuck role. Remove the node worktree at the end; leave
   `zlink-core-b` at the last built commit (say which).

# ST-1 진행

- 2026-09-08 04:56 KST — 시작. 공통 규칙·결함 보고서(§8 포함) 확인. `main` 1696ed55e5 기준 detached worktree `/home/hep7hep7/project/zlink-work/st1` 생성. 기존 main untracked 파일 보존, ninja 0 확인. 원칙·스펙·구현 경로 조사 중.
- 2026-09-08 05:00 KST — synchronization model §3.1~§3.4·§5·§8 및 STREAM pump/pipe/mailbox/receive-owner 경로 대조. H2 경로는 `process_activate_read`→`read_activated`→receive epoch/poller 재알림이 존재. H1의 `activate_write` 실제 owner 경계를 추적하면서 dev 재현 준비.
- 2026-09-08 05:04 KST — `pgrep -c -x ninja=0` 확인 후 `JOBS=4 scripts/build-core.sh dev` 성공(RelWithDebInfo, LTO OFF, tests ON). 기존 `unittest_monitor_ready_drain` 컴파일 경고 외 실패 없음. public API scratch 재현기 작성 단계.
- 2026-09-08 05:09 KST — public C API scratch 재현 성공: TCP STREAM packet mode, clients=100, M=100, body=64, RCV/SNDHWM=4096, thread A poller+DONTWAIT recv, thread B blocking routed echo. 30초 뒤 expected 10,000 / recv=sent=echo=9,436, client timeout 8, send failure 0, dispatcher queue 0. 짧은 실행 종료에서는 별도 `fq.cpp:39 _pipes.empty()` assertion도 재현. 정체 dump/hypothesis 판별 단계.
- 2026-09-08 05:40 KST — (ST-1b 인계, Claude) codex 재현기 인수. 라이브러리와 동일한 컴파일 플래그(`-DNDEBUG` 포함)로 재빌드하고 watchdog(3 s 무진행 시 덤프)를 추가해 **정체 시점** 덤프 확보. 결과: engine 정지 0·engine buffer 0·pending input 없음 → **H1 반증**. 대신 `_fq._active==0`인데 pipe 6~11개가 `_in_active==true` + `_in_pipe->check_read()==true`(각 145 B 대기) → fq 활성 파티션에서 빠졌는데 pipe는 "깨어 있음"으로 표시돼 `process_activate_read`가 조기 return(=wake 유실). 부수 관측: `_fq._pipes`에 같은 pipe 포인터가 2회 등장(중복). 원인 지점 추적 중.
- 2026-09-08 06:05 KST — **원인 확정(H1·H2 모두 아님, 제3의 원인)**: fq 계측(`ZLINK_FQ_DEBUG`)으로 `_fq._pipes`의 위치↔`_array_index` 불변식이 깨지는 것을 확인하고, 재진입 감지기로 **서로 다른 thread가 fq에 동시 진입**함을 증명(recvpipe↔activated 399회, read_miss↔activated 48회, attach/pipe_terminated 포함). recv thread는 `receive_once_guarded`의 **lock-free public receive lease**만 쥔 채 `stream_t::xrecv`/pump→`_fq.recvpipe`를 돌리고, send thread는 `process_commands`에서 `public_api_sync`+`command_owner_sync`+`receive.sync`를 쥔 채 `activate_read`→`xread_activated`→`_fq.activated`를 적용한다 — 두 배타 도메인이 다르다(스펙 §3.1 "command 적용과 공개 연산이 같은 상태를 동시에 만지지 않는다" 위반, §5 3행). 결과로 `array_t` 색인이 깨져 `_active`가 0이 되고, pipe는 `_in_active==true`라 `process_activate_read`가 조기 return → 영구 wake 유실. 수정 설계: command 적용 구간이 public receive lease를 함께 획득(hot path 무비용).
- 2026-09-08 06:40 KST — 수정 적용: `socket_receive_runtime_t::try_acquire_receive_owner_for_commands()` 추가(비공개 헤더) + `process_commands`의 command 적용 구간을 `socket_command_receive_turn_t`(receive lease → `receive.sync` 순서)로 교체. 재현기 3/3 통과(10,000/10,000). 회귀 테스트 `core/tests/integration/test_stream_concurrent_pull_send.cpp` 작성·등록. 수정 되돌린 트리에서 3/3 FAIL(+`fq.cpp:39 _pipes.empty()` assertion 동반 재현 → §5 assertion도 같은 원인), 수정 트리에서 0.09 s PASS. 반복 검증 시작.
- 2026-09-08 06:55 KST — 새 테스트 `--repeat until-fail:20` 통과(총 2.32 s). 수정 전 트리 4/4 FAIL·수정 후 4/4 PASS로 대조 확인. `stream|pipe|wake|hwm|flow|credit|poll` 57개 `until-fail:5` 진행 중. 참고: 기존 TSan suppression `/tmp/mp2-tsan.supp`가 `race:receive_once_guarded`·`race:check_read`를 이미 억제하고 있었다 — 이번 결함이 바로 그 race다.
- 2026-09-08 07:25 KST — 수정 1차판(lease→sync 순서)이 whole-record receive scope와 lock 순서가 겹쳐 교착 위험이 있어, `sync` 획득 후 lease 1회 시도·실패 시 `sync`를 놓고 yield 재시도하는 형태로 정련. 1차판으로 돌린 `stream|pipe|wake|hwm|flow|credit|poll` until-fail:5는 55/57까지 전부 Passed(정련판 재빌드 위해 중단). 보고서 초안 `core-rf-ST-1-report.md` 작성.
- 2026-09-08 07:45 KST — 정련판 재빌드 후 새 테스트 `until-fail:20` 재통과(2.39 s). 관련 suite `until-fail:5`(57개) 재실행 중, TSan 트리(`core/build-tsan`, GCC `-fsanitize=thread`, LTO OFF) 빌드 시작. ASan 트리(`core/build-asan`) 구성 완료.
- 2026-09-08 08:20 KST — 상한 3 h 초과. 필수 산출물(원인 확정·수정·회귀 테스트·보고서)은 완료. 남은 검증(관련 suite until-fail:5 재실행, TSan/ASan)은 실행 중이며 끝나는 대로 보고서 §7.1에 기입한다.
- 2026-09-08 08:45 KST — TSan(새 테스트) 통과. suppression 적용/미적용 **모두** ThreadSanitizer 경고 0건 — `/tmp/mp2-tsan.supp`의 `race:receive_once_guarded`·`race:check_read`가 억제하던 race가 이번 수정으로 사라졌음을 확인. stream suite TSan 실행 중.
- 2026-09-08 09:10 KST — TSan stream suite: `test_stream_socket` 11/11 OK, `test_stream_threadsafe` 5/5 OK, `test_stream_packet_progress`는 `test_shutdown_during_drain`(3 s 안에 transport가 fragment를 queue했는지 보는 타이밍 단언)이 부하 중 FAIL하고 TSan 경고 8건 — 전부 `mailbox_t::activate_if_command_pending` ↔ `mailbox_t::reschedule_if_needed`(mailbox 스케줄 상태)로 이번 변경과 무관한 기존 항목. 부하가 빠진 뒤 단독 재실행 예정. ASan 트리 빌드 중.
- 2026-09-08 09:45 KST — **`stream|pipe|wake|hwm|flow|credit|poll` 57개 `--repeat until-fail:5` 100% 통과(0 failed, 315.6 s).** TSan `test_stream_packet_progress`의 `test_shutdown_during_drain` FAIL이 부하 없이도 재현되어, 기존(수정 전) 여부를 가리려 TSan 트리를 수정 없는 상태로 재빌드해 대조 중(수정 patch는 scratch에 보관, `git stash` 미사용).
- 2026-09-08 10:35 KST — TSan 대조 완료: `core/src` 변경을 되돌린 TSan 트리에서도 `test_stream_packet_progress`의 결과가 **완전히 동일**(같은 FAIL, 같은 경고 8건, 전부 `mailbox_t::activate_if_command_pending`↔`reschedule_if_needed`) → 기존 항목 확정. 수정 patch 복원 완료(`git diff --stat -- core/include core/src/libzlink.vers` 비어 있음). ASan 트리 빌드 대기 중.
- 2026-09-08 12:55 KST — **종료**. ASan은 라이브러리 155/162 TU까지 컴파일한 상태에서 시간(8 h 경과, 상한 3 h)을 이유로 중단 — 보고서 §7.2에 이어서 돌릴 명령을 적었다. 최종 상태: 수정 2파일 + 테스트 등록 + 새 통합 테스트, 미커밋. `git diff --stat -- core/include core/src/libzlink.vers` 비어 있음. 보고서 `core-rf-ST-1-report.md` 완료.

# Req/rep completion stall progress

- 2026-09-04 03:39 KST — 작업 시작. 브랜치 `perf/phase2-judge` 확인. 기존 사용자 변경은 `bindings/c/perf/multi/common/perf_multi_socket_reqrep.hpp` 진단 수정과 `doc/plan/c016-worklog/**` 작업물이며 보존한다. 루트/benchmark 지침 확인 후 completion readiness, mailbox wake, socket timeout scheduler를 독립 조사하고 동일 벤치 재현을 시작한다.
- 2026-09-04 03:43 KST — 필수 스펙(`10-hot-path` §3·§4, `05-polling` §3·completion polling, DEALER request timeout/completion 계약) 확인. `ulimit -v 16777216 && cmake --build core/build -j4` 성공(`ninja: no work to do`).
- 2026-09-04 03:44 KST — 원본 재현 5회 중 3회 실패: `3606`(1 socket), `5355`(2), `13231`(4) outstanding. 실패 로그는 `zlink-work/c016/light-results/stall{2,3,5}.log`에 보존.
- 2026-09-04 03:47 KST — 벤치 timeout 시 poller를 우회해 각 socket에 `zlink_completion_recv(DONTWAIT)`를 직접 수행하는 임시 probe 추가. 첫 debug 재현에서 `waiting_requests=4102`, `direct_drained=0`, `remaining_requests=4102`(`probe-debug1.log`). 즉 completion queue에 쌓였으나 poller event만 놓친 경우가 아니며 rf1 `ready_available`/public poller lost-wake 가설을 실증 배제. 임시 probe는 조사 후 제거 예정.
- 2026-09-04 04:02 KST — 정적 race audit 결과 rf1 completion queue cache의 enqueue/dequeue/close는 동일 queue mutex로 직렬화되어 false-stuck interleaving이 없고, rf3 mailbox helper 추출도 기존 activation semantics를 보존함을 확인. socket aggregate timeout의 generation/rearm에도 영구 task 유실 경로는 발견되지 않았으나, 단일 global scheduler가 한 socket의 모든 due request를 동기 publish하는 convoy 가능성은 남아 있음.
- 2026-09-04 04:08 KST — aggregate timeout callback 임시 계측 빌드 및 재현 1회 성공. callback 3,000회가 정상 종료되어 다음 실패 재현에서 멈춘 socket의 armed/unarmed 상태와 callback 도달 여부를 대조한다. 빌드 중 LTO test relink가 완료되었으며 이는 최종 gate로 아직 계상하지 않는다.
- 2026-09-04 04:13 KST — timeout/admission 계측 실패 재현에서 정체 socket은 callback 종료 시 `request_pending=348/349`, `has_send_pending=1`, successor deadline 없음. request submission 거부 76개 표본은 모두 `pipe_message_admission_hwm_full`(request correlation full 아님). 즉 미완 요청은 physical admission 전 send-pending이며 timeout 미등록이 계약상 정상이고, 근본 원인은 pipe byte-credit/activation 진행 정지다.
- 2026-09-04 04:16 KST — 서버 FQ `has_in()` 종료점에서 inactive pipe의 published data/request head를 찾는 임시 probe로 5회 중 3회 실패를 재현했으나 recovery hit 0. 서버 FQ inactive-with-data 가설을 배제하고 probe를 제거했다.
- 2026-09-04 04:23 KST — `8b6c2aa906`의 sub-LWM prefetch-tail wake 억제를 f3 동작으로 되돌린 A/B 10회에서 3회 실패(`2919/1`, `6725/2`, `987/1`). 실패율 변화가 없어 해당 조건을 원인에서 배제하고 원복했다.
- 2026-09-04 04:30 KST — 저비용 HWM transition 계측 실패 재현(`light-results/stall/hwm-trace-6.log`): client socket `0x576a897fc600`의 application writer pipe `0x576a8981d8c0`은 credit을 2,048,256 / 4,096,512 / 6,144,768 byte에서 정상 수신한 뒤 `written=8,055,936`, `peer_read=7,007,616`, `hwm=1,048,576`에서 arm되고 더 진행하지 않았다. 같은 socket timeout aggregate는 물리 admission된 요청 363건을 모두 처리한 뒤 `next_deadline=0`, `has_send_pending=1`; 최종 drain은 2,569건 미완, reply 946,395건이었다.
- 2026-09-04 04:36 KST — reader/accounting 상세 로그를 추가한 5회는 계측 timing 영향으로 모두 성공하여 해당 고비용 로그를 제거했다. 로그 자체를 수정 근거로 사용하지 않는다.
- 2026-09-04 04:40 KST — 수동 동일 HWM(`PERF_MULTI_HWM=1048576`) A/B 5회 중 1회 실패. auto-HWM의 endpoint cache 차이만으로 현상을 설명할 수 없어 단독 원인에서 배제했다.
- 2026-09-04 04:42 KST — writer HWM arm 시 peer `activate_read`를 추가한 A/B는 10회 중 8회 실패. client-side reader wake 누락이 단독 원인이 아님을 확인하고 원복했다.
- 2026-09-04 04:44 KST — 이미 active인 reader에도 강제로 `read_activated`를 전달한 A/B는 5회 중 1회 실패. 증상 빈도만 흔들 뿐 정체를 제거하지 못해 원복했다.
- 2026-09-04 04:52 KST — `hwm-trace-6.log`의 server 프로세스를 역추적했다. server의 session→ROUTER application writer pipe `0x7af0f41885c0` 하나가 `written=4,095,433`, `peer_read=0`, `hwm=4,096,000`에서 최초 HWM arm된 뒤 대응 `activate_write/apply`가 한 번도 없다. peer `0x7af0f4188930`은 ROUTER socket/FQ 측이다. 따라서 server ROUTER가 해당 연결의 request pipe를 처음부터 소비하지 않은 것이 client-side HWM 및 pre-admission outstanding의 상류 정지점이다.
- 2026-09-04 04:56 KST — FQ 상태를 한 번 더 식별하려 한 최소 계측 빌드는 LTO test 전체 relink를 유발해 시간 상한 전에 중단했다. 모든 임시 계측과 A/B 변경을 제거했으며 Core source diff는 0이다. 사용자 benchmark 진단 변경만 보존했다.
- 2026-09-04 05:01 KST — 계측 제거 후 clean source로 재빌드하여 Core object, `libzlink.a`, `libzlink.so.0.15.1` 링크까지 성공했다. 그 뒤 변경과 무관한 LTO test executable relink가 16/87에서 계속되어 시간 상한 준수를 위해 중단했다.
- 2026-09-04 05:03 KST — 근본 원인 확정: `f3be895b3f`의 ROUTER count-1 `xread_activated` fast path가 route-binding token을 확인하지 않아 identity 미게시 anonymous pipe를 adopted/FQ-registered로 오판했다. 첫 activation은 미등록 `_fq.activated()` no-op 뒤 return되어 slow identity adoption을 영구히 잃는다. 두 count-1 fast path에 `router_route_binding_token()!=0` fence를 추가했다.
- 2026-09-04 05:06 KST — 수정된 shared runtime으로 direct comparison runner 5회씩 두 묶음, 합계 10회 연속 rc=0(`fixed-direct.log`, `fixed-direct-2.log`). 원본 5회 중 3회 실패와 대비된다.
- 2026-09-04 05:08 KST — sleep 없는 `test_count1_router_adopts_anonymous_pipe_on_first_activation`을 기존 synthetic pipe/mailbox harness에 추가. focused target build 성공, 실행 10 tests / 0 failures, `git diff --check` 성공. 시간 상한으로 나머지 전체 gate는 미실행.

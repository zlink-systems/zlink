zlink-cpp provides a CMake package:

    find_package(zlink_cpp 0.18.0 CONFIG REQUIRED)
    target_link_libraries(main PRIVATE zlink::cpp)

The binding is static and follows the installed zlink package for Core linkage.

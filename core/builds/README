This directory holds build tools, i.e. tools we use to build the current
code tree. Packaging tools (which take released tarballs or github code
repos) should go into /packaging.

Visual Studio builds are maintained through CMake. Prepared solution files are
not kept in this tree because they duplicate build policy across multiple
Visual Studio versions and do not cover the test matrix.

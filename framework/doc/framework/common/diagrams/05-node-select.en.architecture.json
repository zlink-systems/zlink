{
  "schema_version": 1,
  "diagram_type": "architecture",
  "meta": {
    "title": "Round-robin distribution · adding a node reflected automatically",
    "subtitle": "requests spread across nodes A and B (request 3 back to A) · new node C reflected automatically via a store row",
    "output": "05-node-select.en.html",
    "legend": { "mode": "hidden" }
  },
  "layout": { "mode": "grid", "origin": [50, 150], "cols": 2, "gapX": 150, "gapY": 78, "cellW": 120, "cellH": 60 },
  "components": [
    { "id": "C", "type": "frontend", "label": "calling node", "sublabel": "channel client", "row": 1, "col": 0 },
    { "id": "A", "type": "backend", "label": "processing node A", "sublabel": ":5600", "row": 0, "col": 1 },
    { "id": "B", "type": "backend", "label": "processing node B", "sublabel": ":5601", "row": 1, "col": 1 },
    { "id": "D", "type": "backend", "label": "processing node C", "sublabel": ":5602", "row": 2, "col": 1 }
  ],
  "connections": [
    { "from": "C", "to": "A", "label": "request 1", "variant": "default" },
    { "from": "C", "to": "A", "label": "request 3 (A again)", "variant": "default", "fromSide": "top", "toSide": "left" },
    { "from": "C", "to": "B", "label": "request 2", "variant": "default" },
    { "from": "C", "to": "D", "label": "node added → store row auto", "variant": "dashed" }
  ],
  "cards": [
    {
      "dot": "cyan",
      "title": "Round-robin and automatic reflection",
      "items": [
        "When connected servers have equal weight, new requests are distributed evenly by round-robin — request 1 → A, request 2 → B, request 3 → A again.",
        "Adding a new node C (:5602) is reflected automatically as a descriptor row in the location store, so it joins the candidates without restarting the caller (dashed)."
      ]
    }
  ]
}

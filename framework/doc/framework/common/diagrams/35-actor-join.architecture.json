{
  "schema_version": 1,
  "diagram_type": "sequence",
  "meta": {
    "title": "예약은 `Defer()` 호출이고, join은 handler가 끝난 뒤 시작한다",
    "subtitle": "등록만 한 뒤 handler가 정상적으로 끝나면 join이 시작된다",
    "output": "35-actor-join.html",
    "column_fit": "spread",
    "viewBox": [1320, 760],
    "legend": { "mode": "hidden" }
  },
  "participants": [
    { "id": "Entry", "type": "backend", "label": "Entry Spot (Actor의 handler)" },
    { "id": "Framework", "type": "messagebus", "label": "Framework" },
    { "id": "Record", "type": "backend", "label": "위치 기록" },
    { "id": "User", "type": "external", "label": "가려는 User Spot" },
    { "id": "Callbacks", "type": "backend", "label": "간 쪽 / 떠난 쪽 callback" }
  ],
  "messages": [
    { "from": "Entry", "to": "Framework", "y": 160, "label": "handler 실행 중 JoinSpot(...).Defer() — 등록만, 아직 아무 일도 없음", "variant": "default" },
    { "from": "Entry", "to": "Framework", "y": 235, "label": "handler 정상 종료 → join 시작", "variant": "default" },
    { "from": "Entry", "to": "Framework", "y": 295, "label": "예외로 끝남 → 등록 폐기, join 시작 안 함", "variant": "dashed" },
    { "from": "Framework", "to": "User", "y": 370, "label": "OnActorJoin으로 승인 / 거절", "variant": "default" },
    { "from": "Framework", "to": "Record", "y": 445, "label": "승인되면 위치 기록과 membership을 함께 확정", "variant": "return" },
    { "from": "Framework", "to": "Callbacks", "y": 520, "label": "간 쪽 OnJoinedActor · 떠난 쪽 OnLeaveActor", "variant": "default" },
    { "from": "Framework", "to": "User", "y": 595, "label": "그 뒤 Actor 앞으로 온 message는 User Spot에서 실행", "variant": "return" }
  ],
  "cards": [
    {
      "dot": "cyan",
      "title": "등록과 join은 다른 시점이다",
      "items": [
        "`Defer()`는 지금 join을 실행하지 않고, handler가 정상적으로 끝난 뒤 시작할 join을 등록한다.",
        "handler가 예외로 끝나면 그 등록은 버려진다."
      ]
    }
  ]
}

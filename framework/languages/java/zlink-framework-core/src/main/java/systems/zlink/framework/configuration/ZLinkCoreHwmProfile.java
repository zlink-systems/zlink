package systems.zlink.framework.configuration;

/** Startup profile forwarded to the Core context's HWM planner. */
public enum ZLinkCoreHwmProfile {
    COMPACT,
    LOW_LATENCY,
    BALANCED,
    THROUGHPUT
}

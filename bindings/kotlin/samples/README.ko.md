# Kotlin bindings sample 범위

이 디렉터리는 Java binding을 Kotlin에서 사용하는 raw socket public API 예제만 제공한다.
Actor, Spot, session binding, timer와 Logical Multicast 시나리오의 정본은
`framework/languages/java/samples/kotlin` 아래의 Framework sample이다.

bindings sample에 제거된 Core service API를 다시 추가하지 않는다.
